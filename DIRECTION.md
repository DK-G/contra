# DIRECTION — contra

> 役割: contra の**不変核**。「本当に良くしたいもの(真の目的)」と「逸脱の境界」を定義する、`plan.md` の上位文書。
> 迷ったら必ずここに立ち返る。**ここに反する変更は、スコアや本数が良く見えても却下**。
> 高度: 最上位(WHY) ／ 安定性: ★滅多に変えない（変える時は DECISION_LOG に重い理由）／ 使う瞬間: 着手前・plan更新時・スコア核やゲートに触れる前。
> ★この文書は `spec.md` / `agent.md` の「セッション開始時に読む」に1行で参照を張る（全文常駐はしない＝文脈肥大回避）。

## 1. デザインピラー（売り / コア価値）— 3点

| # | ピラー | 強化する例 | 薄める/逸脱する例 |
|---|---|---|---|
| P1 | **遠いが構造一致**（Track B 中核・Gentner Analogy）。要約でなく「関係性の再構成」で視座を広げる | Purpose-Mechanism 写像の明示、論文固有の転用仮説 | 「どちらも〜を扱う」式のカテゴリ言い換え（hollow） |
| P2 | **質ゲート方式**（本数でなく閾値超え）。`serendipity = purpose_sim × mechanism_dist` | 出力本数は質の結果。飽和は飽和ノート | 固定本数で出す／弱候補で水増し |
| P3 | **二重棄却**（Anomaly=無意味接続 ＋ マイオピア=近接 を両方棄却）＝contrarian の規律 | hollow gate・Anomaly棄却・near-domain cap | 近接で「意外性が低い自明な接続」を出す |

## 2. 非目標（やらないこと）

- **要約ツールにしない**（核は関係性の再構成）。
- **固定本数で出さない**（旧500本/20本固定は廃止。本数先行は質崩壊で実証＝DECISION_LOG 2026-05-29）。
- **near/far をテーマ非依存のグローバル定数でハードコードしない**（テーマ相対が原則。Step 8 で `_EXCLUDED_TRACK_B_DOMAINS` / `_ADJACENT_DOMAIN_TERMS` / `_DOMAIN_PENALTY_TERMS` を全廃）。
- **弱い候補で水増ししない**（飽和は M3 飽和ノート）。
- **stdlib 主義を崩す外部ライブラリを安易に足さない**。

## 3. 真の目的 vs 代理指標（Goodhart レジストリ）★contra の開発史そのもの

> 最適化してよいのは**真の目的**。指標は proxy。proxy を上げて真の目的を下げる変更は却下 or `quality_eval.md` で別検証。

| 真の目的（本当に良くしたいもの） | 代理指標(proxy) | gaming/過適合の兆候 | ガード（実装済み） |
|---|---|---|---|
| 遠いが構造一致で、論文固有の発見に基づく有用な接続 | 出力**本数** | 本数↑で近接/Anomaly混入→質崩壊 | 質ゲート方式・本数は出力（固定本数にしない） |
| 構造類推（Gentner Analogy） | **purpose_sim** 数値 | 抽象カテゴリ一致のみ(hollow)が高得点 | hollow gate(structural_depth)・Anomaly棄却(purpose_sim<0.20) |
| テーマ相対の遠さ | surface_overlap / 完全名 cosine | false-near/false-far・ドメイン語ハードコード | L0/L1 Jaccard 階層距離・field/keywords から実行時導出 |
| 仮説の論文固有性 | 「役に立つ」体裁 | テーマの不安点の言い換え・bloat | HYPOTHESIS=論文固有の発見＋変数付き検証可能・**数値捏造ガード** |

## 4. 採否リトマス（着手前の関門 / escape-clause 型）

1. この変更は P1/P2/P3 のどれを強化するか? → どれも強化しないなら却下 or 保留。
2. 非目標（要約化／固定本数／テーマ依存ハードコード／水増し）に抵触しないか?
3. **スコア核・ゲート閾値・本数挙動**に触れるなら：真の目的（質）も上がるか? `quality_eval.md` の複数テーマ（analogy-rich/poor 混在）で**退行しないか**確認したか?

> 通らない/却下は DECISION_LOG に1行（contra は既に追記運用あり）。

## 5. 最適化の作法（Goodhart 予防）

- 質ゲート閾値を「**通過数を増やす目的で**」緩めない（近接/hollow が混入する）。
- スコア調整は**1軸ずつ**、複数テーマで退行確認（Step 8/9 の教訓）。
- 「最も serendipity が高い1本」を即正解にしない（R5 self-consistency 投票・hollow judge の二重ゲート）。
- **最終審は機械スコアでなく `quality_eval.md` の人間判断（A–E）**。

## 改訂履歴

| 日付 | 変更 | 理由 |
|---|---|---|
| 2026-06-23 | 初版（spec.md §1・DECISION_LOG から identity を抽出・結晶化） | drift と「本数↔質」の再学習を止めるため |

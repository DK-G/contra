# plan.md（マスター仕様書）

> 本プロジェクト名 **contra** は contrarian（逆張り・対置）に由来する。
> 中核は「一見無関係だが構造的に接続しうる遠い論文をテーマに対置し、思考の視座を広げる」こと。
> 設計の理論的根拠は [`docs/research/serendipity_conditions.md`](docs/research/serendipity_conditions.md) に置く（本書はその結論を前提とする）。

## 1. 目的とスコープ

### 1.1 目的
人間の思考は放置すると必ず**狭窄**する（既知の枠＝マイオピアに閉じる）。
contra は、研究テーマに対して**一見関係が薄いが構造的に接続しうる論文を強引に対置する**ことで、
視座を変え・新しいアプローチに気づき・研究を深化させる手助けをする。

解決対象の課題:
- 文献レビューが「正しさ」重視に偏り、疑問形成と発想の飛躍が後回しになる
- 関連論文ばかりを集めるため、思考が既存の枠の内側で完結してしまう
- 初期仮説の前提・弱点・盲点が暗黙のまま進行する

### 1.2 想定ユーザー（正直な優先順位）
1. **作者自身**（第一ユーザー）。まず自分の研究・開発の発想拡張に有益であることを最優先とする。
2. 同種の悩みを持つ大学院生・若手研究者・研究志向の個人開発者（将来の展開先）。

> 商用展開（Web化・課金）は否定しないが、本書では**個人の思考ツールとしての価値を主目的**とし、
> マネタイズは「将来構想（任意）」として末尾に分離する（§12）。

### 1.3 MVP（最小実用製品）の定義 ★再定義
**MVP = Track B のセレンディピティ・ユニットを「1本」出力すること。**

- 1本とは「分野は遠いが、テーマと関係構造が一致する」論文を、4部構成（§7）で厚く記述した1単位。
- ボリュームは目標にしない。**まず質の高い1本**を出す。
- その後、**質のバーを維持できる限り本数を増やす**（§5.2）。本数は「入力」ではなく「出力」である。

### 1.4 スコープ外（当面扱わない）
- 論文の精読支援・全文解析 / 引用関係グラフの可視化
- 研究の正解・結論の提示 / 自動レビュー・査読代替
- Web UI・課金（§12 の将来構想に分離）

## 2. 設計原則・基本方針
- **要約ではなく「関係性の再構成」**を行う。
- **関連論文だけを集めない**。狙うのは「遠いが地続き」。
- **強引でも切り口（接続点）を明示する**。ただし強引さは「関係構造の一致」で裏打ちする（無意味接続=Anomaly は弾く）。
- セレンディピティは直接最適化できない。制御するのは代理変数＝**「ドメインの遠さ × 接続点の本物さ」**。
- **98%は捨て札**でよい。システムの仕事は「ヒットしうる球の質を上げ、ハズレを一瞬で棄却できるようにする」こと。
- 解釈はユーザーに委ね、**材料と仮説**を提示する。
- 質を最初に最大化し、量は質を保てる範囲で後から拡張する。
- Markdown を最終成果物とし、外部ツール（Obsidian / Notion / Git）連携を前提とする。

## 3. 理論的基盤（要約）
詳細は [`docs/research/serendipity_conditions.md`](docs/research/serendipity_conditions.md)。本設計が依拠する6つの知見:

1. **プリペアードマインド/慧眼**: ユーザーは遠いドメインには無防備。ツールが「効き方」を翻訳して慧眼を肩代わりする。
2. **最適認知距離(Goldilocks)**: 学習価値 = 新規性 × 理解可能性 の逆U字。近すぎ（マイオピア）も遠すぎ（理解不能）も棄却。
3. **構造写像理論(Gentner)**: 良い接続は表層属性でなく**関係構造**の一致。狙いは「Analogy」、弾くのは「Anomaly（無意味接続）」。
4. **バイソシエーション(Koestler)**: 独立した2つの枠の交差がアハ体験を生む。課題枠と解決枠を並置する。
5. **情報エンカウンタリング(Erdelez/Makri)**: 「気づき→中断→吟味→獲得」。情報の匂いを放つ提示にする。
6. **セレンディピティ推薦(SRS)**: 構造化されたランダム性。意外性 ≠ 単なる非類似。意外性 × 構造一致を乗算。

## 4. 収集ソース
### 4.1 採用（確定）
- **OpenAlex**（無料・分野横断・abstract取得可・citation付加情報・APIキー不要）。
- abstract あり論文を優先（「1点接続」の識別には本文情報が要る）。
- **GitHub / GitLab API (Track A Git 実装アンカー)**: 実装上の制約やサンプルコードを収集するため、GitHub REST API と GitLab Projects API を用いた Track A 収集に対応。
- **Hugging Face Hub API (Track A model / dataset / demo アンカー)**: ML 系テーマで実際に触れる model / dataset / Space を収集し、model card / dataset card / downloads / license から実用性を評価する。
- **Zenodo / DataCite (Track A research artifact アンカー)**: DOI 付きの研究ソフトウェア・データセット・再現パッケージを収集し、repository だけでは拾えない研究成果物を補完する。

### 4.2 収集ソースの信頼性評価 (byrepo パイプライン)
Git リポジトリなどの外部実装コードベースは品質のノイズ（stale コードやドキュメント不在）が多いため、**マルチディメンショナルな 100点満点の信頼性スコア (4 Pillars)** に基づく評価アルゴリズムを導入し、実用性の高いコードベースのみを選別する：
1. **実装 & ドキュメンテーション信頼性 (30点)**: README における What/How/Why/When などの構成 Heuristic 検知、コードブロックの記述密度。
2. **メンテナンスアクティビティレベル (LMA) (25点)**: `pushed_at` に基づく更新の鮮度。
3. **コミュニティヘルス & エンゲージメント (20点)**: Fork-to-Star 比率、Issue のアクティビティ評価。
4. **セキュリティ & 成熟度 (25点)**: ライセンス明記、CI/CD テスト構成、論文への Citation リンク。
※上記 Heuristics により、スター数偏重を抑止し「Zero Issues Trap（人気に見えて活動が死んでいる罠）」などを自動的に回避する。

Hugging Face / Zenodo / DataCite の artifact は Git repository と同じ指標に押し込まず、card / metadata completeness、license clarity、activity、downloads / likes、DOI / paper / benchmark linkage、gated / no-license などの risk penalty を source type 別に評価する。Zenodo と DataCite は DOI / URL / 正規化 title による重複排除を必須とする。

## 5. 全体構成（1テーマあたり）
### 5.1 二層構造
- **Track B（接続点フィーチャー）＝主役**: 遠いドメインから、関係構造が一致する論文を対置する。MVP はここの「1本」。
- **Track A（アンカー）＝文脈**: テーマに近い論文。Track B の「遠さ」を測る基準点・足場として少数添える（任意）。

> 旧版は A:10 / B:10 の対等構成だったが、思想（contrarian）に合わせ **Track B を中核**に再配置する。

### 5.2 本数は「出力」である（質ゲート方式）
- 固定本数を入力しない。各候補に**質スコア**（§6）を付け、**閾値を超えた候補だけ**を出力する。
- 超えた本数が結果としての出力数。バーを維持できる限り増える。読み物としては最終的に20本程度を目標に置くが、
  **質を割ってまで本数は満たさない**。

## 6. 3段階パイプライン設計
### 6.1 収集 (collect)
- Track B クエリは **「別ドメイン概念 × テーマ核心語」の掛け合わせ**で生成（純粋な別ドメイン検索は被引用トップの汎用総説を呼び寄せて失敗する）。
- 複数の異なる遠ドメインを生成し、ドメインの重複を避ける（教育・gamification への偏りを明示的に抑制）。
- 撤回論文（is_retracted / タイトル "RETRACTED:" 等）は収集段階で除外。

### 6.2 選別 (select) ★セレンディピティの心臓
各候補を2軸でスコアし、**両端を棄却**する:
- **距離スコア（意外性）**: テーマからの意味的距離。近すぎ（マイオピア）は減点、遠すぎ（理解不能）も減点。中程度を最大化。
- **構造スコア（有用性）**: 表層分野でなく**関係構造の一致**（Gentner）。属性も関係も一致しない Anomaly は強制棄却。
- 最終順位 = 距離スコア × 構造スコア（乗算。SRS の意外性 × 有用性に対応）。
- MVP ではこのスコア最上位の **1本** を採用。拡張時は閾値超えを全採用。

### 6.3 提示 (present)
- 4部構成（§7）で記述。課題枠と解決枠を**並置**し、転用仮説でユーザーの慧眼を肩代わりする。
- 「なぜこの遠い論文がここにあるか」を接続点ラベルで即座に伝える（情報の匂い）。

## 7. 各論文の出力仕様（4部構成）★復活
旧ブレインストーミング期の4部構成を正式に復活させる。3行構成では「役に立つ可能性の仮説」が脱落していた。

1. **概要（2〜3文）**: 原著の主張・方法・結果を忠実に。解釈しない。
2. **テーマとの関連性（1文）**: どの**関係構造**で接続するかを明示。Track B は接続点ラベルを付す。
   例: `【接続点: 失敗からの回復ループだけ一致】`
3. **役に立つ可能性の仮説（1〜2文）★中核**: 「この遠い発見の○○を、あなたのテーマの△△に持ち込むとどうなるか」。
   ＝セレンディピティの提案そのもの。慧眼の肩代わり。
4. **注意点 / 転用制約（1文）**: この論文を転用する際に崩れる前提を具体的に。汎用文（「転用に注意」等）は禁止。

### 7.1 付加情報
- 論文リンク（DOI / arXiv / OpenAlex）/ 発表年 / ジャーナル・会議名 / citation数（取得可能な場合）
- Track A を添える場合は関係度ラベル（高〜低）。Track B は距離・構造スコアの要約。

## 8. 出力形式
- Markdown（1テーマ＝1ファイル）。Obsidian / Notion / Git / 論文メモでの即利用を想定。
- 構成: ヘッダー（テーマ概要・入力仮説）→ Track B セクション（質ゲート通過分）→（任意）Track A アンカー。

## 9. ユーザー初期入力設計
### 9.1 必須
- テーマ概要（3〜6文：何を明らかにしたいか / なぜ問題か）
- 理論 / 実験 / 応用の別
- 想定している前提・仮説（2〜5個。正誤不問。思い込みの明示を促す）
- スコープ（分野 / スケール / 時代）

### 9.2 任意（精度ブースター）
- include / exclude キーワード
- 「今いちばん不安な点」（1文）→ 注意点・転用制約の生成に直結

## 10. テーマ別履歴管理
- テーマを `theme_overview` + `keywords.include` のハッシュで識別。
- 採用論文の OpenAlex ID を履歴記録し、次回実行時に収集候補から除外（同テーマ反復での重複回避）。
- Phase 1: ローカル JSON（`data/history/{theme_hash}.json`）。将来 DB 移行（スキーマ互換維持）。

## 11. 技術構成（MVP）
- 言語: Python 3.10+（標準ライブラリ中心、依存最小化）。
- フレームワーク: なし（CLI）。HTTP は `urllib.request`。
- LLM: OpenAI Responses API（クエリ生成・選別スコアリング・4部生成）。後処理に GeminiCLI を任意併用。
- ビルド/デプロイ: なし（ローカル CLI 実行）。

## 12. 将来構想（任意・MVP外）
個人ツールとして成立した後の選択肢として記録（優先度は低い）:
- Web 化（入力フォーム / 進捗表示 / Markdown ダウンロード）、非同期ジョブ、決済
- 収集ソース拡張（§4.2）、履歴 UI・再生成・比較

### 12.1 導入決定された次世代アーキテクチャ要素 (2026-06-09 決定)
byrepo の実用リポジトリ探索により発見・ライセンス検証を完了した、以下の要素を将来フェーズで順次実装する：
- **MCP (Model Context Protocol) サーバー化**: Contra を外部の AI エージェント（IDE 連携アシスタントなど）から呼び出せる共通ツールとして機能させるためのプロトコル標準化。
- **意味的トポロジーアライメント (GloVeの分散表現コンセプト)**: ドメイン間の概念・知識グラフの Wu-Palmer 距離計算に加え、分散表現空間上での幾何学的アライメントを取り入れ、hollow analogy 棄却率を向上させる。
- **エージェント持続メモリ (agentmemory)**: 1回限りの実行に依存せず、過去の探索履歴やユーザーフィードバック、却下された類推（Anomaly）を記憶して探索性能を向上させる記憶モデルのパイプライン統合。


## 13. 企画の強み（まとめ）
- **思想と構造が一致**: contrarian の名の通り Track B（遠い接続）を中核に据えた。
- **理論的裏づけ**: 最適認知距離・構造写像・バイソシエーション等の知見に基づく選別設計。
- **質ゲート方式**: 本数を追わず、ヒットしうる球の質を上げ、ハズレを一瞬で棄却できる構造。
- **4部構成**で「役に立つ可能性の仮説」を主役に戻し、慧眼の肩代わりを明示。
- まず作者自身に有益であることを基準に据えるため、評価基準が明確でぶれない。

# 実地観測ログ — seihai ループからの by* 失敗記録

**この文書の位置づけ。** seihai（FX 自動改善ループ）は contra の by\* ツールを**毎日・実運用のテーマで**呼んでいる唯一の常設利用者である。そこで出る失敗は、開発中のテスト実行では再現しにくい**同一テーマ族での長期反復**という条件下の観測であり、**改善のための一次データ**として扱う。

**分担**:

- **seihai = 観測者**。by\* が (i) 収穫ゼロ、(ii) スコア 0.0 の定型文、(iii) 明らかに無関係な上位結果、のいずれかを返したら、その日の insights に書くだけでなく**ここに1行追記する**。seihai 側では contra のコードを直さない。
- **contra = 被験体**。対処はスケジュールタスク **`contra-failure-remediation`（火・金 21:00）** が担う。ここを読み、失敗様式を1つ選んで直し、`Changelog.md` / `DECISION_LOG.md` に結果を書き、この文書の該当行に対処状況を付ける。

**書式**（1行1観測。数値は可能な限り実測値で）:

```
| 日付 | ツール | テーマ | 失敗様式 | 実測値 | 対処状況 |
```

---

## 未対処の失敗様式

### F-01. bybridge — 巨大ハブ吸着（7週連続・**seihai 側で 2026-08-16 に呼び出し停止**） → **真の機序を特定し対処済み（2026-08-25）。下記「対処済み」節 F-01-R へ**

**症状**: 交差候補の上位が毎回「共有 bridge 1本」で、内容は分野を問わず**汎用の超高被引用ハブ**に潰れる。過程（シード30／bridge プール50／交差候補60）は正常完走するので**故障ではない**。

| 日付 | テーマ族 | 上位に出たハブ（被引用数） |
|---|---|---|
| 〜2026-08-01（5週分） | アルゴリズム取引・戦略生成 | TAM(43,317) / Felsenstein ブートストラップ(41,579) / SEM 二段階法(40,027) / PLS 回帰(9,798) / Efron ブートストラップ(6,162)。**AT&T Labs という書誌データの欠損レコードまで混入** |
| 2026-08-08 | 同上 | 上位10件すべて共有 bridge 1本 |
| 2026-08-11 | 同上（シード配置を変更） | 上位10件すべて共有 bridge 1本（量子化学 DFT）。**シードを変えても同じ** |
| 2026-08-15 | 同上 | 上位3件すべて共有 bridge 1本（集団遺伝学＝中立性検定 7,260／中立群集理論 5,710／ボトルネック検出 4,377）。**セレンディピティ・スコアは3件とも 0.0（構造スコア 0.0）**、生成散文は「転用できるか要検討」の定型文のみ |
| **2026-08-24**（S-26 観測run 2/3） | 同上（非対称ペイオフと枝崩壊） | **旧様式が完全再現**: 交差候補 60/60 が最頻 bridge `W4285719527` 1本を通過（**上位10件占有率 100%**・実際に使われた bridge は1本のみ）。上位は "Using thematic analysis in psychology" / "Long Short-Term Memory" / "lme4" / "G*Power 3" / "Measuring inconsistency in meta-analyses" / "Pittsburgh sleep quality index" ＝**分野横断の汎用方法論引用雲**。**ただし上流に別の故障がある**——シード名簿が `trade` の語彙衝突で国際貿易政策に埋まっていた（F-13）ので、本 run は bridge 段の多様化を**検定できていない**。run 3/3（2026-08-31 予定）で裁定する→ ✅ **2026-08-25 に contra 側で裁定済み**: 名簿が主題側である条件下で同じ様式が再現したので、**F-13 は F-01 の必要条件ではない**。原因は下記 F-01-R（存在しない参照 id） |
| **2026-08-25**（contra 側の再現・remediation day） | 同上（非対称ペイオフと枝崩壊を固定化した `theme_seihai_trade_collision.json`） | **10回目の再現。ただし今回シード名簿は主題側だった**（CAPM / Tail Risk in Momentum Strategy Returns / Forecasting Crashes / Information Aggregation with Asymmetric Asset Payoffs …＝F-13 は起きていない）のに、**交差候補 59/60 が最頻 bridge `W4285719527` 1本を通過**・上位は "Using thematic analysis in psychology"(190,298) / TAM(66,478) / G*Power ＝前週とまったく同じ汎用方法論引用雲。**実際に通行した bridge は 50本中 2本のみ** |
| **2026-09-02**（S-26 再開後の継続観測・seihai r03） | 同上（集約 PBO veto の較正） | **旧様式ではない**: 最頻 bridge の**上位10件占有率 20%**（全体 58%・通行 bridge 12本・シード主題整合 19/19=100%）。最頻 bridge は **Random Forests（130,895）**＝F4＝決定木家系にとっては**主題の方法論的祖先**であって名簿由来の分野外ハブではない。**★新しい様式**: bridge 集中は解けたのに、**返る交差候補 30 件が全て被引用 3,400 以上**（ImageNet 75,666 / phyloseq 23,105 / QuPath 9,000 …）＝**RF ハブが「各分野で最も引用される RF 応用論文」を吸い上げる被引用量吸着が別経路で残存**。ただし収穫はゼロではなく、**Cluster failure（PNAS 2016）が採用され当日の結論の半分を担った**。 |

**診断（seihai 側 S-38）**: 失敗様式は「ホームドメインへの回帰」ではなく **巨大ハブ吸着**。したがって処方は**ドメイン除外ではなく中心性ペナルティ**——bridge プール構築時に被引用数・次数で正規化する、または degree 上限を設ける。実装先は `src/pipeline/bridges.py`。

> ⚠️ **2026-08-18: この診断は F-02 の計器で実測し、否定された。** 中心性ペナルティは**この失敗の原因に当たっていない**。実際の機序は下記 **F-07**（重複した名簿型 proceedings レコード2件が bridge プール50枠を全部埋める）。実測された最頻 bridge の被引用数は **2,128 / 1,003 / 1,107** で、5週分の観測に出てくる 43,317 や 40,027 のような巨大ハブでは**ない**——ただの雑多な proceedings の参考文献リストである。中心性ペナルティを入れていたら、**同じゴミプールの中で順位が入れ替わるだけ**で、8週目も「効かなかった」で終わっていた。**計器を先に入れた判断がそのまま正解だった実例として残す。**

> ✅ **2026-08-21: 真の機序 F-07 を処置した（下記「対処済み」節）。** 同一テーマ・同一パラメータで **最頻 bridge の上位10件占有率が 100% → 0%**、通行 bridge が **6本 → 13本**、最頻 bridge 自体が「Can lifestyle changes reverse coronary heart disease?」(被引用 2,128) から **「Returns to Buying Winners and Selling Losers」(Jegadeesh & Titman・被引用 11,577)＝主題ドメインの文献**に変わった。**ただし F-01 は完全解消ではない**——最頻 bridge が**全候補**に占める割合は **50% → 52% とほぼ不変**で、「候補が1本の bridge にぶら下がる」性質自体は残る（共有bridge 1本のみの候補 60/60 → 56/60）。⇒ **上位窓の汚染は消えたが、候補側の共起の薄さは別軸として残件**。次の担当はここを F-01 の残余として扱うこと。

> ✅ **2026-08-22（3回目の作業・ユーザー承認済み総合対応 C フェーズ）: F-01 残余の2軸を処置。** (i) **上位窓の per-bridge 枠**（同一 bridge 最大2件/10・`diversify_head_by_bridge`）→ 実測で上位10件の最頻 bridge 占有 **100% → 0%**。(ii) **順位のハイブリッド化**（`annotate_hybrid_rank`: 関連度 z × 0.85 + log被引用 z × 0.15）。実装中に計器が新事実——**交差候補60件全件で語彙 fit=0**（ホームドメイン除外の帰結として当然）——を出したため、関連度チャネルを**構造的テーマ結合（候補が通る bridge を何件のシードが引用するか）**に転換した。結果、上位が CSR/EEG/疫学の他分野高被引用論文から **MAP-Elites 応用（Robots that can adapt like animals・Nature）・多目的進化アルゴ×金融・人工免疫系最適化**へ入れ替わり。**9週間で初めて、bybridge の上位がテーマの構造的隣人になった**。全357テスト緑。


**却下済みの対処**: 「共有 bridge が1本だけの交差候補を捨てる（最低2本要求）」は seihai 側 M-16 として起票されたが 2026-08-02 にユーザー裁定で DROPPED（「シードを調整すれば解決する」）。**その後シード配置を変えても再現した（8/11）ので、この前提は実測で否定されている。**

**正直に記録すべき1点（2026-08-15）**: 3件目「対立遺伝子頻度からの最近のボトルネック検出」は**構造的にはその週の問いそのもの**だった（残っているものの組成から、母集団が最近枯渇したことを検出する）。**しかしこの転用はツールが与えたのではなく、読み手がタイトルから作ったものである。** ツールは 0.0 を付けて定型文を返していた。⇒ **「たまたま隣に落ちていた」を「効いた」と数えない。**

**再開条件（2026-08-18 改訂）**: ~~中心性ペナルティ実装後~~ → **F-07 の処方（1シードあたりの bridge 寄与上限）実装後**に seihai 側で再開し、**最初の3回で失敗様式が変わるかを測る**。変わらなければ本テーマ族への恒久的な不適合として除外する。**現時点では再開しないこと**（計器は入ったが、原因側は未処置）。

---

### F-07. bybridge — bridge プール50枠が「重複した名簿型 proceedings 2件」に丸ごと占領される（F-01 の真の機序） → **対処済み（2026-08-21）。下記「対処済み」節へ**

---

### F-02. bybridge — 使ったシードが出力に含まれない（S-11） → **対処済み（2026-08-18）。下記「対処済み」節へ**

---

### F-13. 全 by\* 共通 — **シード／候補の取得段で語彙が衝突し、bridge 段や採点段に入る前に主題から外れる**（2026-08-24 初観測・S-62 と S-26 が同じ機序に落ちた） → **bybridge シード段は 2026-08-28 に三層対処（field 限定＋semantic レッグ＋整合計器）。同分野内ドリフトと byrepo は残**／**★「semantic レッグ供給 0 件」（5回観測）は 2026-09-11 に機序を特定し対処済み＝日本語のテーマ本文を埋め込み検索に入れていた。下記「対処済み」節 F-13-S へ**

**症状**: 検索語がホームドメインとまったく別の分野で確立した術語と同綴りのとき、取得段の名簿がその別分野で埋まる。以降の段（bridge 構築・関連度採点・多様化）は**外れた名簿の上で正しく動く**ので、診断ブロックの数字は健全に見える。

| 日付 | ツール | 衝突した語 | 実際に返ったもの |
|---|---|---|---|
| 2026-08-22 | byrepo | `"admission"` | 3/3 が Kubernetes admission controller（`kubescape`/`kyverno`/`LuaN1aoAgent`）＝ S-62 |
| **2026-08-24** | **bybridge** | **`trade` / `trading`** | **シード20件中18件が国際貿易政策・開発経済・政治経済**（"Chapter 28 Political economy of trade policy" / "Climate Clubs" / "Accounting for Corruption: Economic Structure, Democracy, and Trade" / "In-Kind Finance: A Theory of Trade Credit" …）。主題に触れたのは2件のみ（"Do Funds Make More When They Trade More?"・"Nonparametric estimation of structural models for high-frequency currency market data"） |
| **2026-09-04** | **bybridge** | **`strategy selection` / `complementarity` / `distance` / `correlation`** | **シード20件が国際貿易・労働経済で埋まる**（"The Skill Complementarity of Broadband Internet" / "Estimates of the Trade and Welfare Effects of NAFTA" / "Regional Trade Agreements" / "Oxford COVID-19 Government Response Tracker" / "The Persuasive Effect of Fox News"）。最頻 bridge は **The Skill Content of Recent Technological Change**（労働経済・被引用6,090）。交差候補 **30/30 が主題無関係**（FDI・貿易自由化・ロボットと雇用・国際物流・銀行競争）。★**ただし F-07 対処後の集中度は保たれている**＝最頻 bridge の上位10件占有率 **20%**（旧様式は 100%）・使われた bridge **25 本**（旧様式は 6 本）。**集中は直り、主題適合は直っていない** |
| **2026-08-24** | **byrepo** | `algorithmic-trading` 等 | 4/4 が汎用メガリポジトリ（ML4T 20.6k★ / QuantConnect Lean 21.3k★ / awesome リスト 6.4k★ / HF の RL モデル）。**問い（「reward/risk 比を明示的な設計変数として扱う実装」）に答えたものは1件も無い**のに `theme関連度 1.0` が3件に付いた |
| **2026-08-25** | **bybridge** | 取引・予測の表層語 | **シード20件が「AI×金融予測」の総説と名簿型レコードで埋まった**（"Navigating AI-Driven Financial Forecasting" / "Stock Market Forecasting: From Traditional Predictive Models to LLMs" / "PRIORITY AREAS OF RESEARCH IN THE SCIENTIFIC ACTIVITY OF TEACHERS" / 公衆の場でのスピーチ不安の30年 …）。**テーマ本文の主語は「多様性の維持」と「集合レベル統計の誤診」だが、シード検索はそこを引いていない**。3回目の再現 |
| **2026-08-26** | **byrepo** | `interpretable` / `model-selection` | **4/4 が無関係**。contra 自身が冒頭で「theme 関連度が全アンカーで低い（**最大 0.33**）— キーワードの語彙衝突」と警告。`RinDig/Interpretable-Context-Methodology`（**LLM エージェントのフォルダ構成**＝"interpretable" の衝突）／`keon/awesome-nlp`（**NLP のリンク集**・関連度 0.2）／`atilaahmettaner/tradingview-mcp`（**関連度 0.0**）。問い（「候補集合の構造多様性を認証する実装」）に触れた anchor はゼロ
| **2026-08-26** | **bybridge** | 構造語 `selection` / `candidate` / `batch` | **シード20件が本テーマと無関係**（kNN チュートリアル / Buccaneer タンパク質鎖トレース / EEG 分類レビュー / LiDAR 点群 / 構造ヘルスモニタリング）。★**本日は自己招来の疑いがある**＝S-26 の趣旨に沿ってテーマ文から分野語を**意図的に抜いた**構造記述（"candidate configurations" / "batch-level statistic" / "limited live slot"）で投げたため、semantic 検索が**構造語に反応して機械学習一般へ流れた**可能性。⇒ **次回は分野語を1〜2語だけ残して対照を取る**（この対照が取れれば F-13 の処方が「語を減らす」なのか「語を選ぶ」なのかが決まる）
| **2026-08-27** | **bybridge** | 構造語 `sequential` / `period` / `run` / `retirement` | **シード20件が全てテーマ外**（消費者信用の契約価格 Econometrica / 外国語学習 / ガーナの農業気象情報サービス / トマト市場 / 雑草の水利用 / 慢性耳鳴り S3 ガイドライン / 乳香の農牧経済 / 緑の革命）。**逐次検定・変化点検出・トレーディングのシードは 0 件**。交差候補 30 件も消費者行動・マーケティング・植物キャノピー・fMRI で**関連 0/30** |
| **2026-09-02** | **byrepo** | `backtest-overfitting` / `cscv` / `deflated-sharpe` / `multiple-testing` / `model-selection` | **4/4 が無関係かつ `theme 関連度 0.0`（最大値がゼロ＝8/26 の 0.33 より悪化）**。返ったのは Kaggle の `btc-candles-15m` データセットと `rogii-*` kernel 3本（石油掘削系コンペのノートブック）。**この問題領域で最も標準的な5語を include して 0.0** なので、呼び手の語彙選択ではなく、**GitHub/HF/Kaggle 側に「統計的手続きそのもの」を主題とする実装アンカーを引く経路が無い**ことの観測。seihai 2026-09-02 r03。 |
| **2026-08-28** | **bybridge** | **分野語を意図的に残した対照**（`trading strategy` / `algorithmic trading` を本文に維持） | **★8/26 の「自己招来」仮説を反証する対照が取れた。** 分野語を残しても**シード20件の大半はテーマ外**（電力系統の分散最適化 IEEE TSG / ガウス場と GMRF の SPDE / ニューロモルフィック計算 / cap-and-trade / **GPERF: A Perfect Hash Function Generator** / 実時間モーションプランニング）。⇒ **F-13 の処方は「語を減らす／増やす」ではない**——分野語の有無で名簿は変わらなかった。★**ただし2件は構造的に的中していた**（[The Oracle Problem in Software Testing: A Survey](https://doi.org/10.1109/tse.2014.2372785)・[Apples and Oranges? The Problem of Equivalence in Comparative Research](https://doi.org/10.1093/pan/mpr028)）＝**8/26・8/27 の「関連 0/20」からは改善**。それでも**交差候補 30/30 は無関係**（RNA-seq 発現解析 DESeq2/voom/edgeR・放射線科の CNN・BioBERT・GLM 教科書）で、**最頻 bridge = Generalized Linear Models（被引用 20,450）＝分野外の汎用方法論ハブ**。**上流で当たった2本が 2-hop で洗い流されている** |
| **2026-08-28** | **byrepo** | `strategy similarity` / `anytime-valid` / `sequential testing` / `backtest overfitting` / `trade log` | contra 自身が「**theme 関連度が全アンカーで低い（最大 0.33）**」と警告。3件中2件は主題ドメインではある（`ml4t/diagnostic`＝バックテスト評価の統計検証・`jianweiweng05/qsx-strategy-score`＝取引ログから過剰適合と偶然のタイミングを検出）が、**問い（2本の腕の行動的重複を約定ログから検出する実装）に答えたものは無い**。3件目 `atilaahmettaner/tradingview-mcp`（★4,259）は**関連度 0.23 の市場データ MCP サーバ**＝完全に無関係なのに順位スコア 45.7 で2位と 0.2 差の3位に入った。⇒ **F-03/F-17 の「Reliability が高い無関係」が、関連度係数を掛けた後もまだ上位に残る**（Reliability 91 × 0.23 が Reliability 81 × 0.33 とほぼ並ぶ＝**係数の効きが弱い**） |
| **2026-08-31** | **bybridge** | **`trade` / `trading`（8/24 と同じ衝突語）** | **★F-13 計器が 100% を返しながら名簿が主題外という初の実例＝contra 自身が予告した「残る限界 (i) 同分野内の主題ドリフト」の本番初観測。** 診断は `シード主題整合 (F-13): home分野一致 20/20 = 100%`（home=Economics）。にもかかわらず**シード20件中18件が国際貿易政策・労働経済**（China Shock ×2 / WTO Accession and Performance of Chinese Manufacturing Firms / Trade and Exchange Rate Policies / Growing World Trade / Labor Market Rigidities / Monetary Policy in a Low Interest Rate World …）。主題側は 2/20 のみ（Pairs Trading / Stock Market Volatility の総説）。**貿易政策も Economics なので field 限定では原理的に捕まらない。** ★併せて**新事実**: 診断行は「語彙シード 60 件・**semantic シード 0 件**を統合」と書いている＝8/28 に入れた対処 (C)（テーマ本文の semantic レッグ＝contra 自身の A/B で「語彙シードより明確に主題寄り」と実測された側）が**この run では1件も供給していない**。名簿は純語彙で構成され、衝突がそのまま通った。⇒ **計器が見るべきは分野一致率でなく semantic レッグの供給数**（0 なら「主題整合は未検証」であって 100% ではない）
| **2026-09-01** | **bybridge** | **構造語＋分野語混在**（`selection pressure` / `held-out` / `walk-forward`・home=Economics） | **F-13 の4回目。計器 100%・名簿は主題外**（8/31 と同型で2例目）。シード主題整合は **20/20 = 100%（Economics）** と報告されたが、**実シードは「Determinants of Long-Run Growth」「Special Economic Zones in Africa」「Evaluation of Biogas Plants」「SPIRIT 2013 臨床試験プロトコル」等**で、**主題（GP 探索・バックテスト検証）に当たるのは20件中2件だけ**（"On the impact of streaming interface heuristics on GP trading agents" / "Evolving a robust trader in a cyclic double auction market"）。⇒ **交差候補30件は開発経済学・イノベーション政策・公共経営で埋まり収穫は実質ゼロ**。★**S-26 観測プロトコルの2数値（4回目）**: (i) 最頻 bridge の占有率＝全体 **23%** / **上位10件 20%**（旧様式の 100% ではない＝**多様化の処置は効き続けている**）、(ii) 最頻 bridge の素性＝**「The Second Industrial Divide」（被引用 3,294）＝分野外の汎用ハブ**（主題ドメインの基礎文献ではない）。**(i) は新様式・(ii) は旧様式**という 8/25 以来の分離が4回目も再現。 |
| **2026-09-01** | **byrepo** | `walk-forward` / `purged` / `genetic` / `backtest` / `validation` | **失敗様式 (iii)＋(ii) の同時発生。関連度係数の第3の縮退様式＝「全件 1.0」**。4アンカー全部が **theme関連度 1.0 / 関係度「高」**で並び、**順位は Reliability だけで決まった**（90/85/84/82）。その結果**首位が完全に無関係**（`lululu811/zettaranc-skill`＝中国語の裁量トレード規律 AI スキル）。⇒ **8/26・8/31 に観測した「関連度が全件低い（最大 0.33）」の裏返し**で、**全件同値なら高くても低くても順位に効かない**のは同じ。（なお2位 `ml4t/diagnostic`・3位 `eslazarev/purged-cross-validation` は本当に有用だったので、**収穫ゼロではなく順位が無情報**という区別が要る。）併せて RELATIONSHIP/HYPOTHESIS 節が定型文（「この論文の知見をテーマの具体的局面に転用できるか要検討」）＝**失敗様式 (ii)**。 |
| **2026-09-03** | **bybridge** | **`trade` / `trading`（8/24・8/31 と同じ衝突語）＋ `scope_field` 未解決** | **F-13 の5回目。しかも計器の第3の縮退様式＝「判定不能」**。診断は **`home分野一致 0/20 = 判定不能（home分野未解決）`**（home=`statistical model selection`）で、**F-13 のシード主題整合が fail-open した**。その結果シード20件のうち **7件が国際貿易・開発経済**（"Trade, Growth, and the Environment" / "Is Free Trade Passé?" / "The China Shock" / "Growing World Trade" / "Importing Political Polarization?" / "In-Kind Finance: A Theory of Trade Credit" / "Governance by conditionality"）・**1件が化学**（"Noncoordinating Anions—Fact or Fiction?"）。⇒ **計器の縮退は3様式になった**: (a) 100% と報告しつつ名簿は主題外（8/31・9/01）、(b) 0/20＝判定不能（本日）、(c) 語彙衝突そのもの。**(b) は `scope_field` が OpenAlex の Field に解決しない自由記述のとき起きる**＝呼び手が分野名を書けば書くほど外れる向きなので、**解決可能な Field 名の候補を診断に出すか、未解決を警告に格上げする**のが処方。★**S-26 観測プロトコルの2数値（5回目）**: (i) 最頻 bridge の占有率＝**上位10件 70%**（全体 **95%**）⇒ **8/27・9/01 の 20% から大きく後退し、旧様式（100%）に近い値が初めて戻った**。共有 bridge 1本のみの候補が **59/60 件**・実際に使われた bridge は **5本**。(ii) 最頻 bridge の素性＝**Benjamini–Hochberg「Controlling the False Discovery Rate」（被引用 111,279）**＝**巨大ハブだが、本テーマ（多重検定補正）にとっては主題ドメインの正典**という**初めての両義的な事例**。にもかかわらず**交差候補30件は DESeq2 / limma / STRING / MaxQuant / GTEx / 膠芽腫ゲノム等のバイオインフォ道具論文で埋まり**、主題に効いたのは Benjamini–Yekutieli（依存下 FDR）と Storey（Direct Approach to FDR）の2件だけ。⇒ **「正典を橋にしても、その正典を引用する側は道具利用者の集団なので、橋の質が候補の質を保証しない」**という新しい読み。 |
| **2026-09-03** | **byrepo** | `backtest` / `overfitting` / `deflated-sharpe` / `cross-validation` / `diversity` | **失敗様式 (iii) の再現（9/01 と同型・ただし今回は収穫あり）**。首位が **`jesse-ai/jesse`（暗号通貨トレードボット・8,407★）で theme関連度 0.93**——問い（「クローン候補プールに多重検定補正をどう当てるか」「生成器に別機序を出させる方法」）と**無関係**で、`backtest` という語が README に出るだけで 0.93 が付いた。一方 2位 `eslazarev/purged-cross-validation`（CPCV＋deflated Sharpe の scikit-learn 互換実装）・3位 `ml4t/diagnostic` は**関連度 1.0 で実際に有用**。⇒ **9/01 の「全件 1.0 で順位が無情報」とは別型で、今回は 0.93 と 1.0 の差が小さすぎて無関係リポジトリが首位に立った**。**S-39（Reliability は品質のみを測り主題関連度をスコアに含めない）の実害が、関連度係数を掛けてなお残る**ことの実例。RELATIONSHIP/HYPOTHESIS 節は3件とも定型文＝**失敗様式 (ii) も同時発生**。 |
| **2026-09-10** | **bybridge** | `strategy search` / `backtest overfitting` / `diversity` / `saturation` / `novelty`（home=`algorithmic trading strategy discovery and backtest overfitting`） | **F-13 の6回目。計器の縮退様式 (b)「判定不能」の2例目＋ semantic レッグ供給 0 の2例目（8/31 以来）。** 診断は `分野(Field) 一致 0/20 = 判定不能（home分野未解決）`・`semantic レッグ供給 0 件`。名簿20件は **公衆の場でのスピーチ不安の LDA 研究 / PM2.5 予測 / 「教員の学術活動における研究の優先領域」/ ルーマニアの住宅ローン金融伝染 / 会議 proceedings** 等で、主題（戦略探索の飽和判定）に当たるものは 0/20。⇒ 交差候補 30 件は **Fama-French / Jegadeesh & Titman 系の資産価格古典と、無関係な深層学習応用（T-GCN 交通予測・SignalP 5.0・不整脈検出 CNN・SCENIC）** で埋まり、**収穫ゼロ**。★**S-26 観測プロトコルの2数値（6回目）**: (i) 最頻 bridge の占有率＝**上位10件 50%**（全体 23%・通行 bridge 18本・共有 bridge 1本のみ 36/60）＝9/03 の 70% からは戻ったが 9/01 の 20% より悪い、(ii) 最頻 bridge の素性＝**Jegadeesh & Titman「Returns to Buying Winners and Selling Losers」（被引用 11,637）＝主題ドメインの基礎文献**。⇒ **本日は「橋は主題側・名簿は完全に主題外・収穫ゼロ」という組み合わせ**で、8/25 の裁定（名簿が主題側でも旧様式が出る）の**裏返しの実例**＝橋の質も名簿の質も、単独では収穫を保証しない。★**呼び手側の新しい気づき**: `scope_field` に自由記述の長い分野名を書くほど OpenAlex の Field に解決せず、F-13 計器が fail-open する（9/03 と同じ）。**解決可能な Field 名で呼ぶのが呼び手の責務なら、その一覧を診断に出してほしい**（9/03 の処方の再掲＝2回目の要望）。 |
| **2026-09-10** | **byserendipity**（raw_only・委譲経路） | 3 facet（進化計算/集団遺伝学・創薬ライブラリ設計・生物多様性サンプリング） | **★失敗ではなく成功の記録（対照として残す）。** 3 facet がそれぞれ 50 件返し、ホーム除外・重複後 31/37/38 → 提出 60 件。**全件が facet の意図どおりの分野**で、うち5件を採点して `delegate_finalize` に投げ **接地照合は 5/5 成立**（F-19-R の処方＝材料全欄 echo を継続適用）。**同日 bybridge が収穫ゼロだったのと同じテーマ・同じ呼び手**なので、**「テーマが悪いから収穫が無い」という説明は本日については成立しない**——facet を呼び手が書く経路は当たり、bridge 2-hop 経路は外れた。⇒ **S-67（bybridge の存廃）の裁定材料として、この同日対照は強い**。 |
| **2026-09-10** | **byrepo** | `quality-diversity` / `map-elites` / `novelty-search` / `archive` / `diversity`（`structured:true`） | **F-14 の再現（5回目）＋関連度 1.0 飽和の3例目。ただし収穫はあった。** 4/4 が同一定型文（「関係軸『実装アンカー』で関係度『高』」「この論文の知見を…要検討」）で、**4件すべて theme 関連度 1.0**。内訳は `algorithmicsuperintelligence/openevolve`(78) / `dietmarwo/fast-cma-es`(73) / **`SynaLinks/synalinks-skills`(73)＝コーディングエージェント用スキル集＝完全に無関係** / `icaros-usc/pyribs`(72)。⇒ **主題直撃の `pyribs`（QD 最適化の標準ライブラリ）が、無関係なエージェントスキル集より下位（4位）に置かれた**。関連度が全件 1.0 に飽和した結果、順位は Reliability（README 充実・stars・ライセンス）だけで決まっている（F-14 の 8/31 観測と同一機序の再現）。★**新しい定量**: 無関係候補と主題直撃候補の順位スコア差は **1.0 点**（73.0 vs 72.0）。**関連度項が働いていれば逆転していたはずの距離**。 |
| **2026-09-11** | **bybridge** | `trading strategy` / `backtest` / `counterfactual evaluation`（home=`Economics, Econometrics and Finance`＝解決可能な Field 名で呼んだ） | **F-13 の7回目。名簿は主題側へ戻ったが、交差が起きなかった。** Field を解決可能な名前で渡したので計器は `Field 一致 20/20`・**`Subfield 一致 13/20 = 65%（Finance）`**（★9/04 の F-13-I の Subfield 行と「Field は主題適合ではない」注記が効いている）。シードは Backtesting（JPM 2015）/ All That Glitters Is Not Gold / Quantifying Backtest Overfitting 等で**主題に近い**。ただし **`semantic レッグ供給 0 件`（3例目・8/31・9/10 に続く）**。★**S-26 観測プロトコルの2数値（7回目）**: (i) 最頻 bridge の上位10件占有率 **70%**（全体 25%・通行 bridge 21本・共有1本のみ 35/60）、(ii) 最頻 bridge ＝ **Pairs Trading: Performance of a Relative-Value Arbitrage Rule（被引用 821）＝主題ドメインの文献**。⇒ **交差候補 30 件はすべてホームドメイン内**（pairs trading の ML 応用・Deflated Sharpe・White の Reality Check・データスヌーピング検定）で、**「ホーム外の交差論文」になっていない**。4 件採点して delegate_finalize → **通過 0（saturated）**。**新しい組み合わせ＝「名簿も橋も主題側・しかし出力が交差しない」**。L0 外判定がホーム Field を粗く取ると、同 Field 内の別 Subfield（計量経済）しか出ない疑い。 |
| **2026-09-11** | **byrepo** | `off-policy evaluation` / `trade filter` / `backtest` / `counterfactual` / `ablation` | **8週連続の使える収穫ゼロ（F-20/F-14 系）。** 1位は `amazon/music-off-policy-evaluation-benchmark`（HF dataset・関連度 0.70）＝**repo 名に `off-policy-evaluation` が一致しただけの音楽推薦ログ**。2〜4位は `freqtrade` / `ai-hedge-fund` / `backtesting.py`（関連度 0.33 同点を Reliability が並べる）。問い（取引フィルタの効果を記録済み約定で遡及評価する実装）に答えたものは 0。 |
| **2026-09-11** | **delegate_finalize** | — | **F-21（道具の自己記述と検証器の食い違い）の2例目。** MCP スキーマ上 `assumptions` は `required` に含まれないが、検証器は `assumptions must be 2-5 items` で拒否する（`src/core/input_schema.py` の `validate_and_normalize`）。byserendipity 経路では assumptions を渡していたので踏まず、bybridge の後段で省略して踏んだ。 |
| **2026-09-11** | **byserendipity**（raw_only） | 3 facet（OPE・臨床試験の適格基準・突然変異テスト） | **(a) 応答サイズ**: raw 結果が **26 万字**でツール応答上限を超えファイル退避。材料の全欄 echo（F-19-R の処方）を要求する一方、全欄を含む材料は呼び手のコンテキストに収まらず、**`venue` 等を落として送るしかない**＝本日 8 件全てで「材料欄が欠けたまま送信」の警告。**処方の要求と出力サイズが両立していない。** (b) **percentile_gate が接地済み 8 件中 5 件を落とし、Near/Far facet の候補が全滅・出力は Very Far の 2 件のみ**（S-81 の既知様式。距離の多様性が出力段で失われる）。接地照合は 8/8 成立。 |
| **2026-09-12** | **bybridge** | futility stopping / conditional power / sequential testing / optimal stopping（home=`Economics, Econometrics and Finance`） | **未実行（収穫ゼロではない）。** 3回とも `OpenAlexError: HTTP 429`。★**contra を介さず直接 `api.openalex.org` を叩いても 429**＝contra の不具合ではなく OpenAlex 側のレート制限。直前に byserendipity が 3 facet × 約50件を回している。⇒ **F-28 として起票**（by\* が1つの外部クォータを共有し、先に回したツールが後続を枯らす）。 |
| **2026-09-12** | **byserendipity**（raw_only・委譲経路） | 3 facet（臨床生物統計の群逐次試験／行動生態学の採餌パッチ離脱／逐次決定理論の best-arm 同定） | **成功。3 facet とも返却あり（50/49/48 → 候補 60）・接地検証失敗ゼロ。** post-gate は **6件提出→通過1件**（閾値 0.611）で、**落選5件のうち3件が当方の判断では今週いちばん実務的**＝**F-22 の 7〜12点目**。落選のまま採用。 |
| **2026-09-12** | **byrepo** | futility / group-sequential / conditional-power / best-arm-identification / sequential-testing（`structured:true`） | **主題直撃3本（`keaven/gsDesign`・`Merck/gsDesign2`・`rpact-com/rpact`）＝2週連続で収穫あり。** ただし**関連度 0.0 の `samugit83/redamon`（AI レッドチーム）と `microsoft/agent-framework` が4位・6位に入り、主題直撃の `rpact`（5位）を上回った**。F-17-R の「残る限界」（係数が語の一致であって問いへの適合でない）の再現。 |
| **2026-09-12** | **delegate_finalize** | — | **F-21 の3例目**（`assumptions` がスキーマ上 optional なのに検証器は 2〜5件必須）。★**前日 9/11 に同じ観測が「2例目」としてこの表に記録済みだったのを読まずに追記した**＝**追記先の直近行を読まない**という呼び手側の手順の穴。seihai 台帳 S-117 として起票。 |

★**2026-08-26 の重要な副産物＝失敗の所在が初めて特定できた。** S-11／F-02 の対処（使ったシードを出力に含める）が入っているため、bybridge の**シード20件が可視化**され、それが本テーマと1件も接点が無いことを直接読めた。⇒ 本日の収穫ゼロは**「橋が汎用ハブに吸われた」(F-01/S-38) ではなく「シード検索そのものの失敗」**。**S-11 の起票文が「シードが見えない限り両者を区別できない」と書いたその区別が、S-11 の実装によって初めて可能になり、今日答えが出た。**★併せて S-26 の観測項目も記録: **(i) 最頻 bridge の上位10件占有率 20%**（実際に使われた bridge 13本＝**bridge 段の多様化は効いている**）／**(ii) 最頻 bridge = Optimization by Simulated Annealing（被引用 44,995）＝分野外の巨大ハブ**。**「上位は多様化したが収穫はゼロ」は旧様式の再現ではなく新しい観測**であり、**bridge 段の処置がすべて効いていても、シードが主題から外れている限り下流は正しく動いて収穫ゼロを返す**。

**✅ contra 側の対処（2026-08-28・部分実装＝bybridge シード段の三層化。bynote 調査に基づく）**:

bynote 調査（NotebookLM `Contra Search Query Precision` に Deep Research 10ソース追加）と一次資料の読解で、機序を2段特定した:

1. **シード検索はテーマ本文を一度も見ていない**。`keywords.include` 数語の prefix ladder を `title_and_abstract.search` に投げるだけで、テーマの主語（「多様性の維持」「集合レベル統計の誤診」）は検索に入らない。
2. **home field 限定が未配線だった**。`StructuredQuery.field_ids` は実装済みなのにシード検索のクエリビルダが渡しておらず、さらに**0件時の generic-search フォールバックが全分野横断**——8/27 の耳鳴りガイドライン・乳香の農牧経済がシードに入った直接の経路。文献側の裏付け: 短い語彙クエリの多義衝突には (a) DB 層の分野フィルタ (b) 長文擬似文書クエリ (c) 取得後の分布乖離検知が標準処方（Clarity score / query difficulty prediction 系）。

**実装（加算的・可逆・各層フラグつき）**:

- **(B) field 限定の配線**: シード段の全クエリ（ladder・PRF 拡張・**フォールバック含む**）に `primary_topic.field.id` を付与。`seed_field_scope:false` で旧挙動。`scope.field` が解決しないときは無限定のまま（fail-open）
- **(C) semantic シードレッグ**: テーマ本文（overview+goal+why）を `search.semantic` へ投げ、home field を**残す側**でクライアント選別（`keep_home_field`・未分類は保持）、語彙シードと公平配分マージ（F-18 と同じ round-robin）。`seed_semantic:false` で無効。取得失敗は空リスト＝語彙シードが常に床（fail-open）
- **(A) シード主題整合の計器**: 名簿の `primary_topic.field` 分布 vs home field の一致率＋**上位分野の名指し**を診断ブロックに常時出力（`シード主題整合 (F-13)` 行）。一致率 50% 未満で警告。分野未解決時は 0% でなく「判定不能」（S-68 と同型の教訓）

**実測（8/28 の r05/F9 テーマ・実 API・同一セッション A/B/C）**:

| 条件 | home分野一致 | 上位分野 |
|---|---|---|
| A: 語彙のみ・限定なし（旧） | **10/20 = 50%** | Economics 10 / Decision Sciences 6 / Business 1 |
| B: 語彙 + field 限定 | 20/20 = 100% | Economics 20 |
| C: B + semantic 統合（新既定） | **20/20 = 100%** | Economics 20 |

semantic レッグ単独は 14件・全件 referenced_works 保持（13〜24本）＝bridge シードとして成立し、中身が語彙シードより明確に主題寄り（"Algorithm Switching: Co-Adaptation in the Market Ecology" / "Computation of Implementation Shortfall by **Sequence Alignment**"＝約定列の整列比較そのもの）。MCP 本番経路のエンドツーエンドでも確認: 最終シード20件に semantic 由来が5件入り、F-12/C(iii)/F-01 の既存ゲートと共存、診断に F-13 行が出る。

**残る限界（正直に記録）**: (i) **同分野内の主題ドリフト（8/24 の trade→貿易政策型）は field 一致率では捕まらない**——貿易政策も Economics。計器はこの型では警告せず、シード題名の目視（F-02 の表）が引き続き検出手段。semantic レッグがこの型への実質的な処方だが、**収穫（交差候補の主題適合）が改善したかは seihai の次回 run が判定する**。(ii) field 限定は隣接分野の準主題シード（Decision Sciences の GAN トレーディング等）を落とす——semantic レッグの未分類保持と `seed_field_scope:false` が逃げ道。(iii) byrepo の同型問題（GitHub 検索の語彙衝突）は**未対処**——GitHub 側に field フィルタ相当が無く、別の処方が要る。

**⇒ seihai 側への申し送り**: 次回 bybridge run の診断ブロックに `シード主題整合 (F-13)` 行が出る。**一致率と上位分野を insights に記録してほしい**。シードが主題に寄ったのに交差候補がなお無関係なら、故障はさらに下流（bridge 選定 or 2-hop の性質）に局在化する——F-07→F-12→F-13 と2段さかのぼった切り分けの次の一手がそこで決まる。

**示唆**: 関連度の自己申告値は**語がマッチしたか**を測っていて、**問いに答えているか**は測っていない。S-39 の対処（関連度をランキングに入れた）は「無関係な優良リポジトリが上位に来る」を直したが、**「語だけ合っている無関係」は素通りする**。⇒ 呼び手側の暫定処方は S-62 と同じ「問いを選ぶ」だが、**ツール側にも「取得段の名簿が主題ドメインに属するかの事後チェック」を置けるはず**（例: シード名簿の concept 分布と theme の scope_field の乖離を診断ブロックに出す。今回なら "International trade"/"Political economy" が上位に立つので一目で分かる）。

---
#### ★2026-09-04 — **F-13 の対処で入れた計器（`シード主題整合`）が、外れた名簿を「100% 一致」と申告する** → **対処済み（2026-09-04・同日）。下記「対処済み」節 F-13-I へ**

**実測**（seihai r05・テーマ＝戦略選択における成果冗長性の測り方）: 診断ブロックは

> `シード主題整合 (F-13): home分野一致 20/20 = 100%`・`上位分野 Economics, Econometrics and Finance 20`（語彙シード 60 件・semantic シード 2 件を統合・field 限定あり）

と申告した。**その 20 件の実体が上の表の行**（NAFTA・ブロードバンド・COVID 政策トラッカー・Fox News）である。

**⇒ 何が起きているか**: 2026-08-28 の三層対処（field 限定＋semantic レッグ＋整合計器）のうち **field 限定は設計どおり効いている**——名簿は確かに全件 `Economics, Econometrics and Finance` に入っている。**破れているのは「Field が一致すれば主題も近い」という前提**のほうで、この Field は国際貿易・労働経済・開発経済・金融工学を1つに含むほど広い。**計器は Field 一致率を測っており、主題適合は測っていない**。

**なぜ悪質か**: `100%` は緑の表示なので、**呼び手は名簿を見に行かない**。8/24 に F-13 を起こした時の症状（名簿が外れる）は**そのまま残っているのに、それを検出するために入れた計器が「正常」と言う**。9/01 の別観測では同じ計器が `home分野一致 0/20 = 判定不能`（`scope_field` 未解決で fail-open）に落ちており、**この計器は「外れているのに 100%」と「判定不能」の両方の縮退様式を持つ**ことになる。

**contra 側で見るとよい点**: (a) Field 一致率と併せて、**シード名簿とテーマ本文の意味的距離**（テーマの `theme_overview` 埋め込みとシード title/abstract のコサイン中央値など、既に semantic レッグで計算している量の再利用）を出す。(b) `Economics, Econometrics and Finance` のように広い Field では **subfield / primary_topic レベルまで降りて一致率を出す**。(c) **緑の単一スカラを出さない**——`20/20 = 100%` は「調べた結果、問題なし」に見えるが、実際に測ったのは1つの粗い属性だけ。

**呼び手側の教訓**: 診断が緑でも**シード名簿の実体を毎回1回は目視する**。本日それをしたから捕まった（seihai `S-67` の裁定材料へ計上）。

### F-14. byrepo — **2)関連性・3)仮説の欄が全件同一の定型文**（2026-08-24 初観測・`structured:true` 経路） → **関連度の飽和と、証拠行の欠落は対処済み（2026-09-12）。下記「対処済み」節 F-14/20-R へ。散文そのものの定型性は残**

**症状**: `structured: true`（キー無し・決定論整形）で呼ぶと、4件すべての「2) テーマとの関連性」が `関係軸「実装アンカー」で関係度「高」の関連性がある。`、「3) 役に立つ可能性の仮説」が `この論文の知見をテーマの具体的局面に転用できるか要検討。`（リポジトリなのに「この論文」）、「4) 注意点」が**呼び手が書いた concern の丸写し**だった。

**実測（2026-08-24）**: 4/4 が同一文。順位スコアだけがリポジトリ固有で、**散文はどのリポジトリについても同じことを言う**。

**なぜ「仕様どおり」で済ませないか**: 委譲フロー（案X・2026-08-22）は「LLM 役はエージェント自身が担い、contra は収集と決定論検証を担う」。その分担なら**散文の欄は空にするか、決定論的に埋められる事実（stars/更新年/ライセンス/Issue 信号）だけを置く**のが正しい。**内容の無い断定文を置くと、呼び手が読み飛ばす習慣を作る**（今回、順位スコア 83.0・関係度「高」という表示に対し実体は書籍のコード集だった）。⇒ 処方案: `structured` 経路では 2)3) を出力しないか、`（委譲経路: 関連性と仮説は呼び手が書く）` のような明示のプレースホルダにする。

**再現（2026-08-25・2回目）**: テーマ＝GP 生成器の構造多様性。3件とも同一文（「関係軸『実装アンカー』で関係度『高』」「この論文の知見を…要検討」「4) 注意点＝呼び手の concern 丸写し」）。**今回は上位3件が主題に正面から当たっていた**（operon / openevolve / radiate）ので、**散文が無内容でも順位自体は有用だった**——つまり本様式の害は「順位が悪いとき」ではなく「**読み手が散文を読む習慣を失う**」ことに集約される、という 8/24 の読みを支持する。

**再現（2026-09-01・3回目）＋落選幅が最大になった例**: 5件提出（serendipity 実測 **0.237 / 0.282 / 0.589 / 0.701** と通過1件）→ **落選4件すべて `percentile_gate(serendipity)`・閾値 0.739**。**0.701 は 8/24 の閾値 0.598・8/25 の 0.647 なら通っていた値**である。⇒ 3回とも「提出件数が増えるほど閾値が上がる」向きで動いており（6件→0.598 / 4件→0.647 / 5件→0.739）、**閾値は件数の単調関数ではなく提出分布の分位**であることを支持する。本日は**遠い候補（mechanism_dist 0.83〜0.91）を3件並べた**ため上側が厚くなり、**遠い候補どうしが互いを落とした**。⇒ **呼び手側の含意（8/24 の運用判断を強化）**: 落選は質の判定ではないので**落選候補も読んで示唆に使う**。本日も落選3件（squashed stabilizing selection / ParTI / Quantifying Selection Pressure）が**通過1件より設計に効いた**ので、insights には通過・落選を同じ表に並べて書いた。

**再現（2026-08-31・3回目）＋関連度の第2の縮退様式**: テーマ＝「sim で測った量のうち live へ持ち越されるものをどう見分けるか」。4/4 が同一定型文（「関係軸『実装アンカー』で関係度『高』」「この論文の知見を…要検討」）。★**今回の新しい点は関連度の側**: 4件すべてが **theme 関連度 1.0（満点）** を自己申告した。内訳は `coding-kitties/investing-algorithm-framework`(93.0) / `atilaahmettaner/tradingview-mcp`(91.0) / `rsandx/AlphaSuite`(83.0) / `OnePunchMonk/AgentQuant`(82.0) ＝**汎用のトレーディングフレームワーク4本**で、目的（「backtest 推定値と live 実現値の差を実際に記録している実装」「固定 reward/risk の含意する損益分岐勝率を選別変数として露出している実装」）に答えたものは0件。TradingView の MCP サーバまで関連度 1.0 になっている。⇒ 8/26〜8/28 の「全件 0.33 で頭打ち」と本日の「全件 1.0 で飽和」は**同じ欠陥の両端**で、**関連度項は語の一致を測っており、問いへの適合を測っていない**（F-13 末尾の示唆と同一機序）。順位スコア＝Reliability×関連度係数は、係数が全件 1.0 になった時点で **Reliability の順位そのもの**に縮退する


**再現（2026-09-02・4回目）＋定型文と関連度ゼロの重畳**: テーマ＝「相関した候補集団に対する集約 PBO veto の較正」。4/4 が同一定型文で、かつ**順位スコアが 14.7 で完全に横並び**（`theme関連度 0.0` が全件に掛かるため、Reliability の差だけが残るはずが 42/100 も同値）。⇒ **定型文（F-14）と関連度ゼロ（F-13）が同時に起きると、出力は「4件とも同じ文・同じ点数」になり、読み手が優劣を判断する手掛かりが一切残らない。** seihai 側の当日の収穫はゼロ。
---
#### 再現（2026-09-04・**語彙衝突が無いテーマでの収穫ゼロ＝7週連続**／seihai r05 担当日）

**テーマ**: 「戦略のリターン系列の相関で戦略集合を選ぶ／短標本の相関 shrinkage／構造でなく成果でアンサンブル member を選ぶ多様性指標」。`keywords_include` は `strategy correlation` / `backtest` / `portfolio diversification` / `shrinkage covariance` / `strategy selection`。

**★このテーマを選んだ理由**: 2026-08-22 の S-62 起票時、seihai 側は「**今週の失敗はツールの欠陥ではなく適合の問題**（`"admission"` が Kubernetes に衝突した＝F-13）」と留保をつけた。本日のテーマは **(i) 語彙衝突が無く**、**(ii) `ledoit-wolf` / `PyPortfolioOpt` / `riskfolio-lib` / hierarchical risk parity など実在の実装が多数ある**領域なので、**留保が正しいかを分離できる**。

**返ってきたもの**:

| 順位 | repo | 関連度 | 順位スコア | Reliability | stars | 実体 |
|---|---|---|---|---|---|---|
| 1 | `dtquocbao/ai-market` | 高 **0.67** | 54.8 | 70 | 374 | 「AI Market Analysis System」。README のみ・相関/shrinkage の記述なし |
| 2 | `HKUDS/Vibe-Trading` | 低 **0.33** | 53.3 | 94 | 32,389 | 汎用トレーディングエージェント |
| 3 | `atilaahmettaner/tradingview-mcp` | 低 **0.33** | 52.1 | 92 | 4,347 | TradingView の MCP サーバ |
| 4 | `coding-kitties/investing-algorithm-framework` | 低 **0.33** | 51.0 | 90 | 2,042 | 汎用バックテスト框架 |

**⇒ 診断（S-62 の留保が外れる）**: 衝突が無くても収穫はゼロだった。**上位4件は「トレーディング一般で star が多い repo」であり、5つの keyword のうち相関・shrinkage・多様性を扱うものは1件も無い**。関連度が **0.67 / 0.33 / 0.33 / 0.33** と事実上2値へ潰れているため、**順位を決めているのは Reliability（≒stars・保守性・ライセンス）**である。⇒ これは F-03（関連度がランキングに効かない・2026-08-21 対処済み）が**別の経路で再発している**か、あるいは**関連度の粒度が粗すぎて対処が効かない**かのいずれか。**F-17-R で `関係度` ラベルの正体が Reliability だったと判明した件と併せて読む価値がある**。

**contra 側で見るとよい点**: (a) keyword が repo の name/description/topics に当たったかを **keyword ごとに**返す（現在は合算の 0.33/0.67 なので、どの語も当たっていないのか1語だけ当たったのかが呼び手に見えない）。(b) 全候補の関連度が 0.33 に潰れた実行は「**このテーマに対して byrepo は無情報**」と申告する（F-15 と同じく、順位を出さずに saturation を宣言する経路）。

**呼び手側の帰結**: seihai は S-62（byrepo を毎日から条件付きへ）に本実測を追記する。**7週連続で使える収穫ゼロ**。


**再現（2026-09-07・5回目）＋「関連度係数がほぼ効いていない」の定量**: テーマ＝「best-of-N のバックテスト統計に対する選抜補正済み推定量の実装」（`structured:true`・track_a_count 5・pool 40）。**5/5 が同一定型文**（「関係軸『実装アンカー』で関係度『低』の関連性がある」「この論文の知見をテーマの具体的局面に転用できるか要検討」）。ヘッダの関連度警告（最大 0.33）は正しく出た。
**新しい定量**: 順位スコアは 36.7 / **32.9** / **32.2** / 31.5 / 31.1。**2位＝唯一の主題直撃実装 `mnemox-ai/deflated-sharpe`（関連度 0.33）**、**3位＝`atilaahmettaner/tradingview-mcp`（関連度 0.0・TradingView の MCP サーバ）**。**その差は 0.7 点。** 4位 `investing-algorithm-framework`（0.0）、5位 `jesse`（0.0・仮想通貨bot）も同様。⇒ **`Reliability × 関連度係数` という式で、関連度係数が順位を実質的に決めていない**（stars と README の充実で決まる Reliability が支配する）。F-20（略語が部分一致を拾う）とは別の層の問題で、**そちらは係数の値の誤り、本件は係数の重みの不足**。なお1位 `eslazarev/purged-cross-validation`（関連度 0.13・CPCV + deflated Sharpe の scikit-learn 互換実装）は**主題直撃なのに関連度が 0.13**＝係数の値自体も当たっていない。

### F-15. delegate_finalize — **percentile_gate の閾値が提出バッチ内の分位なので、採点に正直な分散を付けるほど落選する**（2026-08-24 初観測・様式の記録であって欠陥断定ではない）

**実測（2026-08-24）**: 6件を採点して提出（serendipity 実測 0.359 / 0.378 / 0.511 / 0.531 / 0.577 と通過1件）。落選5件はすべて `percentile_gate(serendipity)` — **閾値 0.598**。ツール側の指示は「2桁小数・格子値回避・同点になりそうなら purpose_pct で差を付ける」＝**分散を付けることを要求している**のに、分位ゲートは**分散を付けた側を落とす**。

**呼び手側の運用（seihai 側で決めた）**: post-gate の通過/落選は**描画の制御であって読解の可否ではない**。落選候補も材料として読み、示唆に使う。**ただし出力だけを見る運用に切り替えると収穫が 1/6 に見える**ので、この様式は記録しておく。

**判断材料**: 分位が「バッチ内」なのか「履歴分布」なのかで意味が変わる。前者なら**1件だけ提出すれば必ず通る**ことになり、ゲートとして機能していない可能性がある（未検証・contra 側で確認されたい）。

**再現（2026-08-25・2回目）**: 4件提出（serendipity 実測 0.559 / 0.581 / 0.636 と通過1件）→ **落選3件すべて `percentile_gate(serendipity)`・閾値 0.647**。8/24 は6件提出で閾値 0.598、本日は4件提出で閾値 0.647＝**提出バッチが小さいほど閾値が上がった**。⇒ **「バッチ内分位」という読みを支持する方向の2点目**（n=2 なので断定はしない）。なお **0.636 は閾値 0.647 に 0.011 差で落ちている**——この候補（型の数を数える多様性統計が長期応答を予測する／Genetics 2013）は seihai 側で**採用扱いにして示唆に使った**。

**✅ contra 側からの回答（2026-08-25・実装を読んで確認。挙動は変更していない）**: **「バッチ内分位」で確定**。`_percentile_gate`（`src/pipeline/classify.py:780`）は**その回の提出候補のスコア列だけ**を降順に並べ、`k = max(1, int(n × 0.30))` 番目を閾値に採る（履歴分布は一切参照しない）。ここから、観測された数字が計算で再現する:

| 提出数 n | `int(n×0.30)` | 実効 k | 閾値の正体 |
|---|---|---|---|
| 1〜3 | 0 | **1** | 最高スコアそのもの |
| **4**（8/25 観測） | 1 | **1** | 最高スコアそのもの＝**0.647**。実測の閾値と一致 |
| **6**（8/24 観測） | 1 | **1** | 最高スコアそのもの＝**0.598**。実測の閾値と一致 |
| **7**（8/31 観測） | 2 | **2** | 7件提出で**ちょうど2件通過**・閾値 **0.719**。`int(7×0.30)=2` の予測と一致し、**通過本数が提出本数の関数である**ことが n=7 でも確認された（品質の関数ではない） |
| 7 | 2 | 2 | 2位のスコア |

⇒ **委譲フローで現実的なバッチサイズ（3〜6件）では、この「分位ゲート」は事実上「最高スコアの1件だけ残す」top-1 セレクタとして動いている**（n≥7 で初めて2件通る）。seihai 側の推論「1件だけ提出すれば必ず通る」も**そのとおり**（n=1 → 閾値＝自分のスコア → `>=` で通過）。8/25 に 0.636 が 0.647 に 0.011 差で落ちたのは、**0.636 が低すぎたのではなく、同じバッチに 0.647 が居たから**である。

**再現（2026-08-26・3回目）**: **6件提出**（serendipity 実測 0.361 / 0.505 / 0.507 / 0.518 / 0.58 と通過1件 0.63）→ **落選5件すべて `percentile_gate(serendipity)`・閾値 0.629**＝**通過した1件のスコアそのもの**で、上表の「n=6 → 実効 k=1」と完全に一致（8/24 の 0.598 に続く2例目の直接確認）。★**呼び手側の実害の具体例**: 本日いちばん有用だった候補（[閉検定は FDP 制御の許容類である](https://doi.org/10.1214/20-aos1999)＝**バッチ veto を部分集合の同時上界へ読み替える処方**）は、**近い分野（統計）ゆえ mechanism_dist を 0.58 と正直に採点した結果 percentile で落ちた**。**門は serendipity（距離×構造）を最大化する設計どおりに動いており、有用性の門ではない**——欠陥ではなく仕様だが、**落選分を読まない運用にすると、最も使える一本を捨てる**という含意が実例で確認された。

**したがって「分散を付けるほど落選する」の正確な形はこうなる**: 分散そのものが罰されているのではない。**通過数が提出数にほぼ依らず1件に固定されている**ので、正直に差を付けるほど「2位以下」が明確になり、落選が目立つ。⇒ **本日は仕様の確認のみで、挙動は変更していない**（このゲートの意図が「top-1 選抜」なのか「品質下限」なのかは設計判断であり、勝手に変えると呼び手の運用を壊すため）。**人間の判断事項として起票する**: (a) 現状のまま＝top-1 セレクタと呼び直して呼び手に明示するか、(b) `top_pct` を `k = max(1, round(n × top_pct))` に変える／絶対下限 `floor` 主導に戻すか。seihai 側の運用（落選候補も材料として読む）は、どちらに倒れても妥当なまま。

---

### F-19. delegate_finalize — **接地契約は「呼び手が送った材料」に対して照合されるので、材料欄を echo し忘れると逐語一致していても全件が接地失敗になる**（2026-09-01 初観測・委譲経路） → **対処済み（2026-09-01・同日）。下記「対処済み」節 F-19-R へ**

**症状**: `byserendipity raw_only` が返した候補を採点し、`theme_quote` / `source_quote` を**候補の abstract から逐語で**抜いて `delegate_finalize` へ渡したのに、**5件すべてが「`source_quote` が候補の title/abstract に存在しない（逐語一致が必要）」で接地検証失敗**（散文が棄却され構造整形に置換）。

**原因（呼び手側の運用ミス・contra の欠陥ではない）**: 候補オブジェクトから `id` と自分の採点欄だけを送り、**`title` / `abstract` / `year` / `venue` / `cited_by_count` を echo していなかった**。照合は**contra が保持する原本ではなく、呼び手が送った材料**に対して走るので、`abstract` が空なら**どんな正しい引用も一致しない**。全欄を echo して再投入したところ、**同じ引用文字列・同じスコアで5件とも接地成立**した。

**実測（2026-09-01・テーマ＝「選択圧の一部が沈黙しても探索は正常に見える」）**:

| 投入 | 材料欄 | 接地検証 | 通過 |
|---|---|---|---|
| 1回目 | `id` ＋ 採点欄のみ | **5/5 失敗** | 1（散文は棄却され定型文で描画） |
| 2回目 | 全欄 echo（`concept_tags` は上位5件に間引き） | **5/5 成立** | 1（同じ候補・同じスコア） |

**なぜ誰でも踏むか**: contra は `⚠ 材料欄が欠けたまま送信されています（title, abstract, ...）` と `接地検証失敗: source_quote が候補の title/abstract に存在しない` を**別々のブロックで**出し、**両者の因果を書かない**。前者は「描画が空欄になる」という体裁の問題に読め、後者は「呼び手の引用が捏造だった」と読める。**実際には前者が後者の原因**であり、しかも後者のメッセージは呼び手に**自分の散文を疑わせる**方向に働く（本件でも一度「自分の抜粋が間違っているのか」を確かめ直した）。

**contra 側への申し送り（処方）**: 接地失敗のメッセージに**材料欠落との因果を含める**。例＝「`abstract` が送られていないため照合できません（材料欄の欠落が原因です）」。照合対象のフィールドが空文字のときは「不一致」ではなく**「照合不能」**として区別する。

**seihai 側の運用（本日から適用）**: raw 材料は**プログラムで抜き出してそのまま echo する**（手で書き写さない・採点欄だけを足す）。⇒ 台帳 **S-90**。

---


**4点目の実測（2026-09-07・同一バッチを2回投げて閾値が動いた）**: 同じ7候補を、1回目は材料欄の echo 漏れつきで、2回目は全欄 echo で投げた。**通過本数は両回とも 2 = int(7 × 0.30)**（バッチ内分位説の3点目→4点目）。**しかし閾値は 0.669 → 0.678 に動き、`Uniformly minimum variance conditionally unbiased estimation …`（Biometrika 2018）の採否が反転した**（1回目 通過 / 2回目 **0.676 対 0.678 で落選**）。呼び手の採点は2回目にわずかに変えているので閾値の変化はそれで説明できるが、**要点は「0.002 差で本命が落ちる」という感度の高さと、採否を決めているのが候補ではなくバッチであること**。**この日のテーマが「極値だから選ばれた項目の推定値をどう補正するか」だったので、選抜器が主題を自演した形になった。**

### F-16. byserendipity — **raw_only の facet を日本語で書くと、候補が「日本語で書かれた文献」で埋まる**（2026-08-25 初観測・言語崩壊）

**症状**: A2 距離プロトコルに従って自作した3枚の facet の `pseudo_abstract` を**日本語**で書いて `raw_only` で呼んだところ、返った候補 **36件が全件、日本語の機関リポジトリ／国内学会誌レコード**だった。内訳は射出成形品の内部組織、黒鉛酸性硫酸塩層間化合物、醤油醸造の副産物利用、精密切削における錐の磨滅、ガラス繊維強化 PET の熱処理…＝**主題（進化的探索の集団多様性／集合レベル統計の誤診）に触れたものは0件**。

**同一テーマで facet だけ英語に書き直して再実行**: 候補60件が返り、**定量遺伝学の選抜下多様性維持**（Lande & Arnold の相関形質選択・optimum contribution selection・allelic diversity）と**創薬の化合物ライブラリ多様性設計**（scaffold diversity・structure vs performance diversity・Tanimoto 0.85 棄却の実効性）で埋まった＝**主題の関係構造に正面から当たる集合**。**同じテーマ・同じ距離設計・違うのは facet の言語だけ**。

**機序の推定**: OpenAlex の `search.semantic` に日本語テキストを投げると、**関係構造ではなく「日本語である」という表層で近傍が決まる**（日本語アブストラクトを持つレコードは分野を問わず互いに近い）。ホームドメイン除外は正しく効いているので、**除外の後に残るのが「日本語で書かれた無関係文献」になる**。

**なぜ新様式か**: F-13（語彙衝突）は**同綴りの別分野術語**で名簿が外れる様式で、**語を選び直せば直る**。本件は**語ではなく言語**で外れており、**テーマをどう書き直しても日本語である限り再現する**。また bybridge には C(iii) で**シード言語ゲート（既定 `en`・除外数を診断に明示）**が入ったが、**byserendipity の raw_only 経路には同等の防御が無い**（facets はエージェントが自由記述する）。

**処方案（contra 側）**: (a) `raw_only` の facets に**言語チェック**を置き、非英語の pseudo_abstract を検出したら診断ブロックで警告する（fail-open でよい＝落とさず知らせる）。(b) ツールの `facets` 説明に「**pseudo_abstract は英語で書くこと**」を明記する（現在の説明文は "in that domain's own vocabulary" とだけ書かれており、言語について何も言っていない）。(c) bybridge の `seed_language` に相当する**候補側の言語ゲート**を byserendipity にも用意する。

**呼び手側の暫定運用（seihai 側で決めた）**: **facet は必ず英語で書く**。日本語で書いた回は収穫ゼロを疑う前に言語を疑う。

**副次観測（F-11 関連）**: 両実行とも取得診断が「OpenAlex リクエスト3件中／リトライ上限まで失敗1件」を報告した。**3枚のうち1枚（統計的多重検定・モデル選択）は2回とも取れていない**＝fail-soft は正しく働いたが、**その距離段の情報は空のまま**。距離3段階のうち1段が恒常的に落ちるなら、A2 プロトコルの「3距離を埋める」という設計意図は達成されていない。

---

### F-03. byrepo — 関連度がランキングに効かない（S-39） → **対処済み（2026-08-21・同日2回目）。下記「対処済み」節へ**

---

> **2026-08-21 追記（同日2回目の作業）**: F-04 / F-05 / F-06 / F-08 は**散文生成（LLM）経路の失敗**で、処方はプロンプト契約の変更（テーマ側/ソース側の記述を提出テキスト・README/abstract からの抜粋に限定する）。本日の環境には OPENAI/ANTHROPIC キーが無く「実装したらその場で走らせて出力を見る」を守れないため着手を見送った。**次回この4件に着手する場合はキーの有効性を先に確認すること**（キーはローテーション待ちの疑いあり）。

### F-05. byrepo — 「概要」が**ソースでなく呼び手のテーマ**を要約して返る（2026-08-18 初観測・F-04 と同一機序／★key-free 経路は 8/22 解消確認済み・委譲経路は接地契約で保護・LLM 経路は残）

**症状**: 返却された各アンカーの「1) 概要」が、**そのリポジトリ／ノートブックの内容ではなく、呼び手が送ったテーマ文の言い換え**になっている。呼び手はソースを開かない限り、これを「関連する資料が見つかった」と読む。

**実測（2026-08-18）**: `azxc9595/visual-graphs-of-philosophy`（哲学の可視化グラフ）の「1) 概要」が —— 「本研究は、**遺伝的プログラミングによる売買ルールの自動生成における候補集合の相関問題**を探求し、過剰適合指標が閾値を超える原因を分析する。特に、生成段での多様性を強制する手法が効果的であることを示唆している」。**これは送信したテーマ文そのもの**であり、当該ノートブックはこの主題を扱っていない。`impratiksingh/unsupervised-learning`（MNIST 系の教師なし学習ノート）の概要も同様に「データの多様性を保ちながら効果的な特徴を学習する手法に焦点」と、テーマ側の語（多様性）を主題として貼り直している。

**F-04 との関係**: F-04（byserendipity が「2) 関連性」の**テーマ側**命題を捏造する）と**機序が同じで向きが逆**。F-04 はテーマ側の命題を作り、F-05 は**ソース側の内容**を作る。どちらも「出力の散文が、入力テーマを条件にした生成であって、ソース本文に接地していない」ことの現れ。⇒ **F-04 の対処案（テーマ側命題を提出テキストからの抜粋に限定する）と対になる規約が要る＝ソース側の記述も README/abstract からの抜粋に限定し、抜粋できないなら概要を出さない。**

**なぜ危険か**: F-01（bybridge）は構造スコア 0.0 で**失敗を自己申告**していた。F-04/F-05 は**成功の形で失敗する**。しかも F-05 は概要・関連性・仮説の3節すべてが整合した散文になるため、**ソースを開かない読者には検証の取っ掛かりが無い**。

---

### F-06. bynote — 「他ドメインの類推」が全件ホームドメイン内（2026-08-18 初観測／★本番経路では 2026-08-22 に処方実装済み＝委譲プロトコルの A2 距離3段階。LLM 経路は非主流化）

**症状**: `bynote_link_concepts` の「2. 類推可能な**他の**抽象的ドメインの例」が、テーマのホームドメイン（進化計算・機械学習）そのものから3件返る。

**実測（2026-08-18）**: テーマ＝GP 生成器の候補相関と集合レベル PBO veto。返却された3ドメイン＝**(1) 機械学習モデルのハイパーパラメータ最適化 / (2) 進化的アルゴリズムにおける個体選択 / (3) ネットワークトポロジーの最適化**。(2) は**テーマそのもの**（GP＝進化的アルゴリズムの個体選択）で、距離ゼロ。(1) も同じ「探索と過剰適合」の圏内。Serendipity Bridge の問い（「どのような外部要因を導入すれば生成器の出力を変えられるか」）も、テーマ文が既に問うている内容の再掲。

**bybridge/byserendipity の失敗様式との違い**: あちらは**遠くに行って構造を捏造する**。bynote は**遠くに行かない**。同じ「距離×構造の乗算選別」の両端で別々に壊れている＝**距離項と構造項が独立に故障しうる**ことの実地例。

**対処案**: 出力ドメインに対して、テーマの `scope_field` との**分野距離の下限**を課す（同分野のラベル違いを弾く）。少なくとも「テーマ文に既出の語で記述できるドメイン」は候補から外す。

---

### F-04. byserendipity — 「関連性」がテーマに存在しない命題を相手に立てられる（2026-08-17 初観測／★**委譲経路は 2026-08-22 に接地契約で処方実装済み**・LLM 経路は残）

**症状**: 返却された論文の「2) 関連性」欄が、**テーマ側の命題として提出していない文**を引いて対応づけを主張する。スコアは 0.0 ではなく中位（0.56）で、接続点ラベルには自ら「**構造対応ゆるめ・思考のタネ**」と書かれる。

**実測（2026-08-17・テーマ＝「単一軸の事前登録停止規則が、規則の見ていない軸での劣化に対して恒久的に沈黙する」）**:

| # | 返却論文 | スコア | 主張された対応 | 実測 |
|---|---|---|---|---|
| 1 | The precision–fragility paradox: 生成AIが顧客生涯価値を上げつつ在庫切れを増やす（2025・被引用 **0**・venue "Frontiers in Research"） | 0.56（距離 0.80 / 構造 0.70） | 論文側〈顧客セグメンテーションが供給チェーンの機敏性を制約する〉↔ テーマ側〈**需要の変動が在庫管理の難易度を高める**〉 | **テーマ側の命題が捏造**。提出したテーマに「需要」「在庫」は一語も無い。実際に対応させるべきテーマ側の命題（停止規則の盲点）とは無関係 |
| 2 | Agile Decision-Making in Unstable Markets: A Soft Set Paradigm（2026・被引用 **0**） | 0.56（距離 0.80 / 構造 0.70） | 論文側〈パラメータの変化が意思決定を駆動する〉↔ テーマ側〈市場の変動が意思決定の基準を決定する要因〉 | 対応は成立するが**両側とも汎用命題**で、どんなテーマにも当たる。ただし「4) 注意点」だけは的を射ている（「動的重み付けは変化がリアルタイム観測可能であることが前提」＝こちらは観測されない次元が問題） |

**F-01（bybridge の巨大ハブ吸着）との違い**: bybridge は**構造スコア 0.0 で定型文**を返す＝**失敗を自己申告していた**。byserendipity は**構造スコア 0.70 を付けて、テーマ側に存在しない命題との対応を主張する**＝**失敗が成功の形で返る**。呼び手にとってはこちらの方が危険で、読まずに採用すると架空の類推が insights に入る。

**診断（仮説）**: 距離 0.80 が先に立ち（＝ホームドメインから遠い＝小売/意思決定論）、構造スコアはテーマ全文でなく**要約された断片**に対して付けられている疑い。だから「需要の変動」のような、テーマに無いが**テーマの近傍で自然に生成できる**命題が対応の片側に置かれる。

**対処案**: 「2) 関連性」のテーマ側命題を**提出テキストからの抜粋に限定する**（生成させない）。抜粋できない場合は対応を主張させず、構造スコアを付けない。**被引用 0・新設 venue の2件が同スコアで並んだ**点も併せて、距離項に被引用/venue の下限を効かせるか要検討。

---

### F-08. bynote — 類推は遠いが**構造が空**（2026-08-19 初観測・F-06 の裏面／★本番経路では 2026-08-22 に処方実装済み＝A3 実文献接地必須。LLM 経路は非主流化）

**症状**: 「2. 類推可能な他の抽象的ドメインの例」が**ホームドメイン外**（医療試験・製品テスト・教育方法）から返るのに、対応づけが**話題レベル**にとどまり、機序を1つも運んでこない。

**実測（2026-08-19）**: テーマ＝「稼働中の2腕が実は同一介入であることを、照合キーが jitter で壊れている状況下でどう検出するか／入口専用スクリーニングが既存在庫を監査しない設計問題」。返却された3類推＝(1) 医療試験（プラセボと実薬の差が小さい場合）、(2) 製品テスト（微細な仕様変更）、(3) 教育方法の比較。**3件とも「2つのものがほぼ同じだと差を測りにくい」という同語反復**で、テーマが投げた2つの機序（**照合キーの jitter による exact-match の零化**、**入口専用スクリーニング**）のどちらにも触れていない。Serendipity Bridge の問い2件も「データ収集方法を変えたら結果はどう変わるか」等の一般論。

★**同日の対照が決定的**: 同じ問いを byserendipity（raw_only・自作 facet 3枚）に投げたところ、facet (i) 医療試験から **[Trials to assess equivalence](https://doi.org/10.1136/bmj.313.7048.36)**（BMJ 1996・被引用 1,032）が返り、これは **「優越性デザインでは同等性を結論できないので、非分離を止めるには専用の事前規則が要る」** という**運べる機序**を持っていた。⇒ **bynote が挙げた「医療試験」というドメインは正しかった。欠けていたのはドメイン選択ではなく、そのドメインの中の実際の文献に降りる段**。

**F-06 との関係**: F-06 は**遠くに行かない**（類推が全件ホームドメイン内）。F-08 は**遠くに行くが何も持ち帰らない**。⇒ 「距離項と構造項が独立に故障しうる」（F-06 の記述）の**構造項側の単独故障**が、これで実地観測された。両方揃って初めて乗算選別の意味が出る。

**対処案**: bynote の類推に**最低1件の実文献接地を要求する**（ドメイン名だけでなく、そのドメインで当該構造を扱った文献を1本挙げさせる／挙げられなければその類推を出さない）。実装としては bynote の類推段から byserendipity の retrieval を1回呼ぶのが最短。

---

### F-09. delegate_finalize — 材料の部分 echo が沈黙して劣化し、落とした候補を名指ししない → **対処済み（2026-08-21・同日2回目）。下記「対処済み」節へ**

---

---

### F-10. byserendipity — 判定側が「因果対応なし」と言っている候補に構造スコアの最高位が付く → **対処済み（2026-08-21・同日2回目）。下記「対処済み」節へ**

---

### F-11. 全 by\* 共通 — OpenAlex を**匿名プール・リトライ無し**で叩いている（2026-08-21 起票／**(1)(3) 同日対処済み・(2) polite pool のみ据え置き**）

**症状（潜在）**: `OpenAlexClient.get`（`src/openalex/client.py:42`）は **HTTP エラーを一切リトライしない**——`urllib` の例外をそのまま `OpenAlexError` に包んで送出する。加えて `mailto` は **CLI の `--mailto` でしか渡らず、MCP 経路（`src/mcp_server.py` の `CollectConfig()` 既定）では常に `None`**＝OpenAlex の**匿名プール**を使っている。匿名プールは polite pool（mailto 付き）より共有レートが厳しい。

**実測（2026-08-21）**: F-07 の作業中、匿名リクエストが **HTTP 429 Too Many Requests** を1回返した。**同じクエリに `mailto` を付けると即座に成功**。その後の匿名リクエスト3連は成功したので、**429 は恒常ではなく一過性**——本日の bybridge 実行4回はすべて完走している。⇒ **今日の実害は無い**。

**なぜ起票するか**: シード収集経路（`_page_through` → `collect_and_filter`）は `OpenAlexError` を握り潰さないので、**一過性の 429 が実行中に当たると、その回は落ちるか途中で切れる**。呼び手から見るとこれは「**収穫ゼロ**」と区別がつかない——seihai が記録してきた収穫ゼロの一部が、実は取得失敗だった可能性を**現状の出力では否定できない**。

**対処状況（2026-08-21 当日・ユーザー裁定「(1)を今実装」）**: **(1) 実装済み** — `OpenAlexClient.get`（`src/openalex/client.py`）に 429/5xx/タイムアウト系の指数バックオフ・リトライ（既定2回・`max_retries=0` で旧挙動）を入れた。**429 以外の 4xx はリトライしない**（間違ったリクエストの反復は無意味）。新規 `tests/test_openalex_client_retry.py` 6ケース＋実経路1回で検証。**(2) polite pool 移行（`mailto`）は据え置き** — メールアドレスを外部サービスへ毎リクエスト送る判断を伴うため、リトライ後もなお 429 が頻発すると分かってから再検討（そのときは役割アドレスの用意が前提）。**(3) 同日対処済み（2回目の作業で実装）** — `RUN_STATS`（`src/openalex/client.py`）がツール実行1回ぶんの requests/retried/gave_up を数え、MCP 層が毎回リセット＋異常時のみ「⚠ 取得診断」行を**結果本文に**追記する。リトライで回復した場合は「結果への影響なし」、上限まで失敗した場合は「収穫ゼロではなく取得失敗の可能性」と明記＝呼び手が誤読しない文面にした。

**追加観測（2026-08-25・F-01-R の作業中）**: 本日の remediation 作業で同一 IP から bybridge を繰り返し実行した結果、**OpenAlex が持続的に HTTP 429 を返す状態に入った**（リトライ3回を使い切って失敗・4分待っても回復せず）。**2026-08-21 の記録と食い違う点が1つある**——当時は「同じクエリに `mailto` を付けると即座に成功」だったが、本日は **`mailto` 付きでも同じく 429** だった。⇒ **polite pool 移行は「持続的なレート制限」に対する解にならない可能性がある**（一過性の 429 に対しては依然有効かもしれない）。据え置き判断そのものは変えないが、**「mailto を入れれば解決する」という前提は今日の実測で支持されなかった**ことを記録する。実務上の含意: **同一テーマの連続実行（before/after 比較のような作業）は間隔を空ける必要がある**。

**再現（2026-09-04・F-13-I の作業中／2回目）**: 本日の remediation でも同じ状態に入った。シード段の再現実行と対照2本（計 5〜6 リクエスト程度）と `/subfields` の取得を行ったあと、**API 全体が持続的に 429**（`works` の `per-page=1&select=id` すら 429）。**30 分以上・2分間隔のリトライ 6 回すべて 429**。8/25 の観測（持続的レート制限は mailto では解けない）を否定する材料は出ておらず、本日は mailto を試していない（鍵・アドレスの新規利用は人間の判断事項のため）。⇒ **実務上の含意が1段強くなった**: 「間隔を空ける」だけでなく、**remediation 作業では実 API を1回だけ叩いて名簿をファイルに落とし、以降の before/after はそのファイルで回す**（本日はそれで F-13-I の実測を完遂できた）。



### F-12. bybridge — シード取得段の全滅（F-07 対処後に故障が上流へ移動・2026-08-22 初観測／S-26 の観測run 1/3） → **対処済み（同日）。下記「対処済み」節へ**

**症状**: 交差候補 0件・bridge プール **0本**。旧様式（F-01 の巨大ハブ吸着・F-07 の名簿型 proceedings による枠占領）とは**別物**で、上位の偏りではなく**シードそのものが主題と無関係**。

**実測（2026-08-22・テーマ＝「固定容量下の参入制御と退出規則の非対称性／戦略選択・多重比較補正」）**:

- **シード 20/20 件が日本語の機関リポジトリ（IRDB）レコード**。内容は高知市方言のアクセント資料、地方テレビ局の編成戦略、オージェ電子分光、詩、大学紀要、博士論文審査要旨など——**主題ドメインとの関連は皆無**。
- **20件すべて 被引用 0 / bridge 寄与 0本**。
- bridge プール **0本** → 交差候補 **0件**。出力の診断ブロック自身が「候補の `referenced_works` が取得できていない可能性」と記している。

**S-26 の観測プロトコルへの回答（run 1/3）**:

| 観測項目 | 結果 |
|---|---|
| (i) 最頻 bridge の上位10件占有率 | **測定不能**（bridge 0本） |
| (ii) 最頻 bridge が主題ドメインか名簿由来か | **測定不能** |

⇒ **「3回とも旧様式なら恒久除外」の条件は満たしていない**（今回は旧様式の再現ではない）。seihai 側は観測run 2/3・3/3 を続行する。

**機序の推定（seihai 側からの仮説・contra 側で検証されたい）**: 被引用 0 の機関リポジトリ・レコードは OpenAlex で `referenced_works` を持たないことが多く、**構造上 bridge を1本も生成できない**。つまりシード選択に「**このシードは bridge を1本でも出せるか**」という**生存確認（liveness）が無い**。関連度の問題より前に、**出力を持ち得ないシードが20枠を占めている**。

★**seihai 側で同型の欠陥を潰した実例がある**（転用可能な処方として置く）: 2026-08-16、`vol_sma` を period 無しで使った戦略設計2本が **4000バー全部で0発火**していた。バックテストは走り、取引0本で「測って落ちた」として永久に記録される——**「候補が弱い」とも「候補が無い」とも違う第3の沈黙**。処方は **採用の前に生存確認を必須手順にする**（0発火の設計を1本も入れない）。**bybridge のシード選択に同じ関門を置けば、この観測は起こり得ない**: `referenced_works` が空のレコードはシードに採らない。

**併記観測（同日・失敗ではないが記録する）**:

- **byrepo は「1) 概要」がリポジトリ自身を要約していた**（テーマの鸚鵡返しではない）＝ **F-05 は解消を確認**。Impl/Doc・LMA・Comm・Sec の内訳も実値を持つ（28/25/13/25 等）＝ **F-08 の「全件 0」も解消を確認**。
- **ただし byrepo の上位3件は依然 3/3 無関係**（`kubescape` / `kyverno` / `LuaN1aoAgent` ＝すべて Kubernetes セキュリティ／ペンテストツール）。**機序は今回はっきり特定できた＝語彙衝突**: テーマ側の `"admission"`（参入制御）が **Kubernetes の admission controller** と衝突した。theme 関連度は **0.30〜0.33 と自己申告**しているのに上位として返っている。⇒ **これは欠陥というより適合の問題**（seihai 側は S-62 として「問いにコード成果物が含まれるときだけ呼ぶ」へ運用を変えた）。**contra 側への示唆があるとすれば「自己申告した関連度が低いとき、順位ではなく警告で返す」**。
- **bynote は4週連続で「他ドメイン類推」が一般類推**（教育制度／医療の治療選択／採用プロセス）＝ **F-06 の再現4回目**。話題としての「選抜」で寄せており、構造距離が出ていない。
- **byserendipity は今週も唯一の実収穫**（Management Science 2020 の入院受入バイアス論文が実際に処方へ転用された）。**ただし返った2件のスコアが両方とも厳密に 0.56（距離0.80×構造0.70）** ＝ **F-10 の併記観測「当たりが全部同点で順位付け不能」は、causal cap の実装後も残っている**（今回は2件なので実害は無い）。

---
### F-17. byrepo — **`関係度` ラベルと `関連度係数` が逆転して表示される**（2026-08-27 初観測・`structured:true` 経路／F-03 対処の副作用の疑い） → **真因を特定し対処済み（2026-09-01）。下記「対処済み」節 F-17-R へ**

**症状**: 出力の各アンカーに付く定性ラベル `関係度`（高/中/低）と、同じ行に併記される数値 `theme関連度` が**逆向き**になる。読み手はラベルを先に読むので、**唯一関連していたアンカーが「中」、完全に無関係なアンカーが「高」**と提示される。

**実測（2026-08-27・テーマ＝「K回連続の負の期間で退場させるカウンタを較正済みの逐次検定へ置き換える実装／退場ルールの偽陽性率を no-change null 下で測るコード」）**:

| # | リポジトリ | 表示ラベル | 併記された `theme関連度` | 順位スコア | seihai 側の実判定 |
|---|---|---|---|---|---|
| 1 | `IFCA-Advanced-Computing/frouros`（★261・ドリフト検出ライブラリ） | **中** | **1.0** | 62.0 | **★唯一明確に有用**（CUSUM/Page-Hinkley/ADWIN/DDM を統一 API で持つ） |
| 2 | `facebookresearch/Kats`（★6,460） | **高** | 0.33 | 51.0 | 弱く関連（CUSUM 系 detector を含む） |
| 3 | `SCStelz/security-investigator`（★239） | **高** | 0.33 | 47.0 | **完全に無関係**（MCP を使ったセキュリティ調査ツール） |

**⇒ ラベルと係数の対応が壊れている**（係数 1.0 → 「中」／係数 0.33 → 「高」）。**順位スコア自体は正しく frouros を1位に置いている**ので、壊れているのは**表示層のラベル付け**だけの可能性が高い。

**F-03（関連度がランキングに効かない・2026-08-21 対処済み）との関係**: F-03 の処方で関連度係数が順位に入るようになり、**本日は順位が正しく機能した**（無関係な security-investigator は3位へ落ちた）。**しかし定性ラベルはその係数と結び付いていない**ように見える。⇒ **F-03 の対処時に、ラベル生成側の姉妹経路が数え上げられなかった疑い**（seihai 側の台帳が繰り返し記録している「姉妹経路の網羅チェック」漏れと同型）。

**なぜ実害が大きいか**: S-39 の既知の欠陥（Reliability Score がテーマ関連度を含まない）は**数値を読めば気づける**が、**ラベルの逆転は「読み手が正しく読んだ結果として誤る」**。本日 seihai 側は3件すべてを自分で読み直したので実害は出ていないが、**ラベルを信頼する運用なら無関係な1件を最上位の示唆として採用していた**。

---

### F-18. byserendipity — **A2 の3距離 facet のうち最遠の1枚が収穫 0 で、プロトコルが実質2距離へ潰れる**（2026-08-27 初観測・F-06 の裏面／`raw_only` 委譲経路） → **原因を特定し対処済み（2026-08-28）。下記「対処済み」節 F-18-R へ**

**★結論を先に**: OpenAlex の足切りでもホームドメイン除外でもなく、**contra 自身の収集ループが3枚目の facet を1度も検索していなかった**（候補上限 60 に達した時点で break していたため）。seihai が記録した「facet は3枚なのにリクエストは2件」がその直接の署名だった。

**症状**: 委譲経路で呼び手が A2 の距離3段階（Near / Far / Very Far）の facet を3枚自作して渡したのに、**返る候補が近い側の facet に極端に偏り、最遠の facet からは1件も返らない**。呼び手から見ると「多様な3距離で引いた」ことになっているが、実体は2距離である。

**実測（2026-08-27・テーマ＝「小標本の符号の K 回連続で恒久的な劣化を宣言する構造」）**:

| facet | 距離 | ドメイン | 返った候補数（全60中） |
|---|---|---|---|
| 1 | Near | 統計的工程管理・産業の状態監視（run rules） | **43** |
| 2 | Far | 臨床試験の中間解析・futility stopping | **17** |
| 3 | **Very Far** | **保全生物学・目撃記録からの絶滅推定** | **0** |

**⇒ 残った2つは互いに「統計方法論」という近い隣人**なので、**距離の広がりは名目上3段階・実質1段階**に近い。

**★ただし機序は F-16（日本語 facet で言語崩壊）とは別**: facet 3 は英語で書かれ、約80語の擬似アブストラクトも他2枚と同じ形式・同じ密度だった。**呼び手側の書き方の差では説明しにくい。**

**seihai 側からの仮説（contra 側で検証されたい）**: (a) OpenAlex `search.semantic` が facet ごとに返す件数が**上限一律でなく類似度で足切りされている**ため、主題語彙から遠い facet ほど閾値を割って全滅する。(b) ホームドメイン除外がドメイン判定を誤って facet 3 を巻き込んだ。**(a) なら「遠い facet ほど落ちる」＝A2 プロトコルの目的（遠さを稼ぐ）と検索の挙動が正面から衝突している**ことになるので、切り分けの価値が高い。

**回避策として seihai 側が本日やったこと（有効だった）**: **Very Far だけを単独の facet として別呼び出しで投げ直した**（ドメイン＝サッカーの監督解任と平均への回帰）。**28件が返り、そのうち2本は本日の結論に直接効いた**（[firing a coach の計量経済評価](https://doi.org/10.1080/0003684022000015946)・[noise-signal ratio in the firing of football managers](https://doi.org/10.1177/1742715011420315)）。⇒ **遠い facet は「引けない」のではなく「近い facet と同居させると負ける」**可能性が高く、これは (a) 仮説を支持する。**当面の運用処方＝Very Far は単独呼び出しで引く。**


**★2026-08-28（2回目・r05/F9）＝同一 facet の単独再走で機序がほぼ確定した（決定的対照）**

| facet | 距離 | ドメイン | 返った候補数（全60中） |
|---|---|---|---|
| 1 | Near | 臨床試験の非劣性・assay sensitivity | **44** |
| 2 | Far | 群集生態学・限定類似性／機能的冗長性 | **16** |
| 3 | **Very Far** | **ソフトウェア工学・等価変異体（equivalent mutants）** | **0** |

**同じ日に、facet 3 の擬似アブストラクトを（語をほぼ変えずに）単独 facet として投げ直したら 42 件返り、上位20件すべてが主題（等価変異体の検出・コスト）だった**——`Are mutants a valid substitute for real faults` / `Automatically detecting equivalent mutants and infeasible paths` / `The Impact of Equivalent Mutants` / `(Un-)Covering Equivalent Mutants` 等。**うち1件は本日の delegate_finalize を通過して結論に直接効いた**（[(Un-)Covering Equivalent Mutants](https://doi.org/10.1109/icst.2010.30)＝カバレッジ変化を等価判定の代理にする・精度75%/再現56%）。

⇒ **「遠い facet は引けない」のではなく「近い facet と同居させると引かれない」**が、**同一日・同一文言・同一エージェント**という条件下で再現した。8/27 の (a) 仮説（同居時の相対的足切り）を支持し、(b) 仮説（ホームドメイン除外の巻き込み）は**単独走で 42 件返った時点で否定される**（除外規則は facet 構成に依存しないため）。

**★併せて、診断ブロックが欠損を「影響なし」と申告していた**: 3 facet の run の取得診断は `OpenAlex リクエスト 2 件中 / 一時失敗→リトライ 1 回（リトライで回復済み・結果への影響なし）` と出た。**facet は3枚なのにリクエストは2件**で、しかも「結果への影響なし」と明示している。呼び手は**出力に facet 別の内訳が無い限りこの欠損に気づけない**（今回は3件しか採点しなかったので気づいたが、60件を機械的に採点していたら気づかない）。⇒ **処方の提案は2つ**: (1) **facet 別の返却件数を診断ブロックに常時出す**（0件の facet を名指しする）、(2) **facet 数とリクエスト数が一致しないときは「影響なし」と書かない**。これは seihai 側の S-68（「情報が無い」を中立値で表現するとテストにも人にも検出できない故障になる）と同型なので、**seihai 側の教訓としても引く**。

---

### 併記記録（2026-08-27・**S-26 の bybridge 観測 run 2/3**）

S-26 が記録を指示している2項目:

| 観測項目 | 結果 |
|---|---|
| (i) 最頻 bridge の上位10件占有率 | **20%**（全体 43%）⇒ **旧様式 F-07 の署名「100% 占有」は再現せず。多様化の処置は効いている** |
| (ii) 最頻 bridge が主題ドメインか名簿由来か | **"The Adaptive Decision Maker"（被引用 3,456・行動意思決定論の一般ハブ）＝名簿由来の分野外ハブ**。主題ドメイン（逐次変化点検出）の基礎文献ではない。しかも**これを引くシードは 20件中 1件のみ** |

**⇒ run 2/3 の判定: 旧様式の再現ではない。** ただし**収穫はゼロ**（交差候補 30 件で関連 0）。S-26 が「上位は多様化したが収穫はゼロ、は旧様式の再現ではなく新しい観測として区別して書け」と指示している型そのもの。

**★故障の所在は上流へ移った**: 本日の不発は bridge 段の偏りではなく、**その一段上流のシード検索**（上表 F-13 の 2026-08-27 行）。**bridge プールの多様性が回復したので、多様化した先が全部無関係だと分かるようになった**——F-07 対処 → F-12（シード全滅）→ 本日（シードは取れるがテーマ外）と、**故障が同じ方向へ2段階さかのぼっている**。⇒ **run 3/3（次回の r04 担当日）で本テーマ族への適合を最終判定する。** 現時点の暫定所見は「**bybridge の 2-hop 機構自体は健全になったが、シード検索がこのテーマ族の語彙を掴めない**」。

---

### 併記記録（2026-08-27・delegate_finalize の診断メッセージが原因を取り違える）

**これは主として呼び手（seihai）側の操作ミスだが、エラーメッセージの帰属が誤解を招くので記録する。**

**起きたこと**: 1回目の `delegate_finalize` で、呼び手が候補材料の `title` / `abstract` / `year` / `venue` / `cited_by_count` を echo せず、`id` とスコアと散文だけを送った。結果、**6件すべてに「`source_quote` が候補の title/abstract に存在しない（逐語一致が必要）」**が付き、**接地検証失敗として散文が全件棄却**された。

**実際の原因**: `source_quote` は**正しく原文から取った逐語**だった。**照合先の abstract が提出されていなかった**ので、空文字列に対して照合し「存在しない」と報告された。材料欠落の警告は**別途正しく6件分出ていた**（F-09 の対処が効いている）ので、情報は揃っていた——**2つの警告が独立に並ぶだけで、因果が示されていなかった**。

**なぜ記録するか**: 「引用が原文に無い」は**呼び手の捏造を疑わせる最も強い文言**であり、実際には**呼び手が材料を送り忘れただけ**でも同じ文言が出る。**呼び手がその区別を自力で付けられないと、自分の正しい引用を捏造と誤認して書き直す**（本日は原因に気づいて全欄 echo で再送し、2回目は接地検証を全件通過した）。

**処方の提案（contra 側）**: 照合先の `abstract`（および `title`）が空/未提出の候補については、`source_quote が存在しない` ではなく **`照合先の abstract が未提出のため検証不能`** と別文言にする。**「検証に失敗した」と「検証できなかった」を区別する**——これは seihai 側の台帳が M-45／M-59／S-40 で繰り返し立てている一般則（「そのラベルは、測れなかった場合と区別できているか」）と同じもの。

---

### 併記記録（2026-08-29 土曜・週次指針テーマ「対照そのものの妥当性をどう確かめるか」）

観測者は seihai の土曜ルーティン。テーマは**分野語をほぼ持たない抽象記述**（統計的品質管理／測定妥当性）で、`scope_field` は `measurement validation and control design in automated evaluation systems`。

#### 1. F-17 の2回目——ただし像が違う。**ラベルは係数と「逆転」しているのではなく、係数と無関係に「高」を返している疑い**

| # | リポジトリ | 表示ラベル | 併記された `theme関連度` | 順位スコア | Reliability | seihai 側の実判定 |
|---|---|---|---|---|---|---|
| 1 | `iOfficeAI/OfficeCLI`（★29,453） | **高** | **0.0** | 31.1 | 89 | 完全に無関係（Office 文書自動化） |
| 2 | `nteract/semiotic`（★2,691） | **高** | **0.0** | 30.8 | 88 | 完全に無関係（React 可視化） |
| 3 | `open-multi-agent`（★6,838） | **高** | **0.0** | 30.1 | 86 | 完全に無関係（TS エージェント基盤） |

**⇒ 3件とも係数 0.0 でラベル「高」。** 8/27 は「係数 1.0 → 中／0.33 → 高」で*逆転*に見えたが、**本日の3点を足すと「ラベルは係数を参照していない（定数 `高`）」という読みの方が単純**。**8/27 の 中/高/高 も、順位1位だけを「中」にする別ロジックが噛んでいる可能性**があり、いずれにせよ**ラベルは読んではいけない**。

**★併せて機序が1つ見えた: 関連度係数には床がある。** 順位スコア **31.1 = Reliability 89 × 0.35**（30.8 = 88 × 0.35 / 30.1 = 86 × 0.35）。⇒ **`theme関連度 0.0` でも係数は 0.35 までしか下がらないので、無関係でも Reliability が高ければ上位に残る。** F-03 の処方（関連度を順位に入れる）は入っているが、**床のせいで「関連度ゼロを排除する」力を持たない**。**処方の提案**: 係数の床を 0 付近まで下げる、または `theme関連度 == 0.0` の候補は返さず「該当なし」と報告する（**収穫ゼロと明示される方が、無関係な3件より呼び手には有用**）。

**なお本日のテーマでは、このラベルは主題そのものの実例になっていた**——seihai 側の今週の結論は「**恒真に同じ値を返す観測は、無いのと同じで、しかも『あるように見える分だけ悪い』**」であり、`関係度: 高` はまさにそれ。

#### 2. byserendipity — OpenAlex 504 は**contra 内蔵の3回リトライでは回復せず、単独再投も1回目は失敗した**（F-11 の系。**ただし診断表示は F-18-R の対処が効いている**）

- **1回目（3 facet 同時）**: facet 1・3 は各 50件返却、**facet 2（software engineering / test oracle quality）が `HTTP Error 504` で 3 attempts 失敗**。
- **2回目（facet 2 を単独で再投・pseudo_abstract は初回と同じ ~80語）**: **同じく 504 で 3 attempts 失敗**。
- **3回目（同じ facet を、pseudo_abstract を約 40 語へ短縮して再投）**: **成功・45件返却**。

**⇒ (a) 「単独で投げ直せ」という案内は正しいが、1回では足りないことがある。(b) 失敗が facet の内容ではなく*要求の長さ*に依存する疑いがある**（同一ドメイン・同一意図で、短くしたら通った）。**処方の提案**: semantic 検索の長い pseudo_abstract を分割/短縮して投げる、またはリトライ時に自動で短縮する。

**★対処が効いている点も明記する**: 2回目の応答は「**結果が薄い場合は『収穫ゼロ』ではなく取得失敗の可能性があります（再実行を推奨）**」と**取得診断（リクエスト1件／リトライ2回／上限まで失敗1件）を数字つきで**返した。**8/28 に F-18 で記録した「収穫0なのに『結果への影響なし』と申告する」挙動は解消している。** 呼び手はこの表示だけで「テーマ飽和」と「取得失敗」を切り分けられた。

#### 3. bybridge — **S-26 の観測 run 5**（裁定 S-67 は保留のまま。seihai 側では継続も除外も決めていない）

| 観測項目 | 結果 |
|---|---|
| (i) 最頻 bridge の上位10件占有率 | **20%**（全体 43%）⇒ **5回とも旧様式（100% 占有）は再現せず。多様化の処置は効き続けている** |
| (ii) 最頻 bridge が主題ドメインか名簿由来か | **`The WEKA data mining software`（被引用 17,892）＝分野外の巨大ハブ。5回連続で「分野外ハブ」** |

**★ただし収穫は改善した（前3回は実質1件、本日は 30件中2件が構造的に的中）**: [Holmström 1979 "Moral Hazard and Observability"](https://doi.org/10.2307/3003320)（追加信号を決定規則に入れてよい必要十分条件＝呼び手の当日の中心的発見に直接効いた）と [Gneiting 2007 "Probabilistic Forecasts, Calibration and Sharpness"](https://doi.org/10.1111/j.1467-9868.2007.00587.x)（較正だけなら無内容な予測でも満たせる）。

**★シード段も改善している**: 20件中4件が主題に正面から当たっていた（`Defining the best quality-control systems by design and inspection` / `Predicting and eliminating built-in test false alarms` / `Test Design Under Falsification`(Econometrica) / `A nonmanipulable test`(Ann. Statist.)）。**8/26 の「kNN チュートリアル・タンパク質鎖・EEG・LiDAR」で埋まる状態とは別物。**

**⇒ run 5 の判定: 上流（シード）と上位窓はどちらも改善済みで、残る損失は 2-hop 段が主題適合シードの寄与を洗い流すことに局在する。** 8/28 の「シード2件は構造的に的中したのに 2-hop で洗い流された」の**2回目の再現**。

**★F-13 について呼び手側の発見**: 診断が **「home分野一致 0/20 ＝ 判定不能（home分野未解決）」** を返した。渡した `scope_field`（`statistical quality control and measurement validation`）が OpenAlex の Field に解決できず、**`seed_field_scope` が fail-open してスコープ無しで走っていた**。**これは呼び手の使い方の問題でもある**——次回は解決可能な分野名（例: `Mathematics` / `Computer Science`）を渡して対照を取る。**処方の提案（contra 側）**: 分野が解決できなかったときは診断の1行だけでなく、**「シードスコープは無効です」と失敗の帰結を明示する**（現在の「判定不能」は、判定できないのが F-13 指標なのかスコープ機能そのものなのかが読み分けにくい）。

#### 4. delegate_finalize の誤帰属——**2回目。処方が未実装のため、別の日の別の呼び出しで同じ穴に落ちた**

2026-08-27 の併記記録と**完全に同じ様式**。本日は **12件**すべてに `source_quote が候補の title/abstract に存在しない（逐語一致が必要）` が出た。

**★今回は決定的な対照が取れた**: 呼び手は**同じ12件・同じスコア・同じ散文・同じ引用**のまま、**材料欄（title/abstract/year/venue/doi/cited_by_count）を echo しただけ**で再送した。結果は **接地検証 12/12 通過**（棄却ゼロ）。⇒ **1回目の全件棄却は 100% 材料欠落によるもので、引用の正しさとは無関係だった**ことが実証された。

**なぜ2回記録するか**: 8/27 の処方（照合先 abstract が未提出のときは「**検証不能**」と別文言にする）は**まだ入っていない**。そして**「引用が原文に無い」は呼び手の捏造を疑わせる最も強い文言**なので、**呼び手が自分の散文を疑って書き直す**という誤った是正に進みうる（本日は再送の対照を取ったので避けられた）。**材料欠落の警告自体は 12件分正しく出ている**ので、**同じ入力から2つの異なる原因説明が同時に出ている**状態でもある。

#### 5. F-15（percentile_gate のバッチ依存）——3点目の実測

| 日 | 提出件数 | 閾値 |
|---|---|---|
| 2026-08-24 | 6 | 0.598 |
| 2026-08-25 | 4 | 0.647 |
| **2026-08-29** | **12** | **0.68** |

本日は **12件提出 → 3件通過**。**[Holmström 1979] は 0.679 対 閾値 0.68 の 0.001 差で落選**した。⇒ **良い候補を多く出すほど閾値が上がり、限界の候補の生死はバッチの構成で決まる。**

**★呼び手側の所見（対等な立場として）**: seihai は今週、**自分の系の同じ欠陥**（集団統計 PBO で個体を全滅させる／座席容量で候補を落とす）を診断して指針に書いた。**相対ゲートは較正が要らないから採用され、その代償として採否の決定権が候補からバッチへ移る。** contra の post-gate も同じ形をしている。**「このゲートは候補の性質を見ているか、バッチの性質を見ているか」を出力に明示する**のが、両者に共通の処方になりうる。

---

### F-16. byserendipity(raw_only) — **facet 1枚が HTTP 400 で落ちると A2 の距離段が1つ丸ごと消える**（2026-08-31 初観測。S-73 の後継＝**申告は改善済み**） → **400（長さ）側は対処済み（2026-09-12）。下記「対処済み」節 F-16-R へ。504 の一過性障害は未対処＝単独再投の規約が引き続き有効**

**症状**: 3枚の facet を渡した raw_only 実行で、facet [1]（Near＝`Offline reinforcement learning and off-policy evaluation`）が `取得失敗 (request failed: HTTP Error 400: Bad Request)`。残る2枚（Far＝臨床予測モデルの transportability／Very Far＝育種の G×E）は正常に 30/28 件を返し、合計 58 件が材料として返った。

**★S-73 からの改善（明記する）**: 8/28 は「facet が 0 件でも診断ブロックが『結果への影響なし』と申告する」ことが問題だった。**本日は contra が自分から名指しした**——「⚠ 収穫0の facet: …。この距離段は今回の材料に含まれていません（**A2 の3距離が実質的に潰れている状態**）。その距離を確実に引くには、当該 facet を単独の呼び出しで投げ直してください」。呼び手が誤読しようのない申告で、**処方まで書いてある**。

**呼び手が処方どおり再投した結果（同一セッション・実 API）**: 同じ距離段（`Offline policy evaluation and offline model selection`）を**単独 facet・短い pseudo_abstract**で投げ直すと **返却 50 / ホーム除外後 35 / 提出 35 件**で成功。中身も主題ど真ん中（"How Well do Offline Metrics Predict Online Performance of Product Ranking Models?" / "Predicting online performance of job recommender systems with offline evaluation"）。

**⇒ 診断**: 400 は**ドメイン依存ではなくペイロード依存**。失敗した呼びと成功した呼びの差は (i) facet 3枚→1枚、(ii) `structure` 本文が長文→1文、(iii) pseudo_abstract がやや短い、の3点で、**同じドメイン語で成功している**以上、語彙衝突でもドメイン除外でもない。OpenAlex `search.semantic` へ渡す連結クエリ（`structure` + `pseudo_abstract`）の**長さかエンコードが上限を越えている**のが最有力。

**contra 側で見るとよい点**: (a) 連結クエリの長さを送信前に測り、上限超なら**切り詰めて再送**する（現在は 400 をそのまま facet 失敗にしている）。(b) 400 に限り**1回だけ短縮リトライ**すれば、呼び手の手動再投と同じ結果が自動で得られる。(c) 3枚渡した実行の中で1枚だけ落ちるのは、**同一実行内でクエリ長が facet ごとに違う**ことを示唆する（`structure` は共通なので pseudo_abstract 長が効いている）。

**呼び手側の教訓**: 距離段の欠損は「収穫が減る」ではなく「**A2 プロトコルの前提が崩れる**」。Near が欠けた材料だけで採点すると、遠い候補ばかりが並んで `mechanism_dist` の分散が縮み、post-gate の分位閾値（F-15）まで動く。**落ちた facet は必ず単独で投げ直す**。

---

#### 再現（2026-09-04・**長さ仮説が全強度で確認された**／seihai r05 担当日）

**症状は 8/31 より強い形で出た**: A2 の3距離 facet（(1) 群集生態学の保護区設計 / (2) センサ配置と環境モニタリング網設計 / (3) 育種の交配親選抜）を渡した `raw_only` 実行で、**3枚すべてが `取得失敗 (request failed: HTTP Error 400: Bad Request)`**。診断は「semantic 収集で候補が0件でした（facet 3 件・ホーム収束/非空ゲートまたは履歴除外 0 件で全滅）。**facet をより遠い/具体的なドメインへ見直すか、テーマが飽和している可能性があります**」と返した。

**★8/31 の (a)(b) 処方をそのまま呼び手が手で実行したら直った**: **facet のドメインも枚数も変えず、`structure` 本文と3枚の `pseudo_abstract` をそれぞれ約半分の長さに縮めただけ**で再投 → **返却 50/50/50・ホーム除外後 45/35/33・提出 20/20/20＝合計 60 件**。**同じドメイン語・同じ3枚・同じ履歴で成功した**ので、8/31 の診断「400 はドメイン依存ではなくペイロード依存」は**単独 facet の特殊事情ではなく一般に成立する**。

**★今回いちばん害があったのは 400 そのものではなく診断文である**: 診断は原因候補として「facet が近すぎる」「テーマが飽和している」の2つを名指しし、**長さには一言も触れない**。呼び手はこの日、**「このテーマはもう飽和した」と誤って結論する寸前だった**（seihai 側は当日の insights にそう書きかけている）。⇒ 8/31 の処方 (a)(b) に加えて **(d) 400 の診断文から「飽和/近すぎ」の推測を外し、まず「クエリ長」を挙げる**。原因が特定済み（8/31）でメッセージだけが未追随という状態は、**contra 自身が seihai へ何度も指摘している「診断は出るが因果が結ばれない」型**（F-19/S-73）そのもの。

**併記（同日2回目の呼び出し）**: 別テーマ（bynote 代替の接地用・3 facet）では facet [2]（`Information retrieval result diversification`）が **`取得失敗 (request failed after 3 attempts: HTTP Error 504: Gateway Timeout)`**。他2枚は 50/50 で成功。⇒ **F-11 の (2) polite pool 据え置きと合わせて読む**＝3回リトライでも 504 は抜けられない場合がある。呼び手は A3（実文献接地）の規約に従い、**接地できなかった距離段の類推を出力しない**という形で欠損を吸収した（＝プロトコルは守られたが、その距離段の知見は失われた）。


---

---

### F-26. bybridge — **bridge 集中は解けたのにシード名簿が 60→2 件に潰れ、「多様な無関係」が返る**（2026-09-09 初観測・S-26 観測プロトコル 6回目／seihai r03 日次） → **対処済み（2026-09-12）。下記「対処済み」節 F-26-R へ**

**この日の指標は旧様式（F-24）を明確に否定している**: 最頻 bridge の上位10件占有率 **40%**（全体 17%）・通行 bridge **16本**・候補あたり平均 1.52 本。F-23/24-R の対処は効いている。

**にもかかわらず全滅した。真の律速は上流**:
- `seed_language` ゲートが **49件**、`referenced_works` 空の生存確認が **9件** を除外し、**語彙シード 60 件のうち生き残ったのは 2 件**。
- **semantic レッグ供給 0 件**＝診断ブロックが自ら「テーマ本文は検索に一度も入っていない＝主題適合は未検証」と申告。
- 生存した2シード = 「What Could the Statistical Synthesis of Research Findings Contribute to the Disciplined Inquiry for Education?」（The Annual Report of Educational Psychology in Japan・被引用1・**bridge 寄与 45 本**）と**無題**のリハビリテーション医学誌レコード（被引用0）。
- 結果、通行 bridge の上位は臨床歩行分析・IMU・ISB キネマティクス勧告で埋まり、交差候補30件は DeepLabCut・Xsens MVN・ウェアラブル歩行計測・メタ分析異質性になった。テーマ（多段スクリーニングの原因帰属）とは無関係。

**F-12（シード取得段の全滅）との違い**: F-12 は `referenced_works` 空レコードが**枠を占有**する故障で、除外して直った。今回は**除外そのものが正しく働いた結果として名簿が2件になった**——除外は正しく、補充が無い。日本語テーマで語彙シードの大半が日本語誌に落ちるため `seed_language:'en'` が大量に刈り、**それを補うはずの semantic レッグが 0 件だった**という二重故障。

**★S-26/S-67 の裁定に直接効く観測**: 3回の観測プロトコルが測っていた `bridge 集中度` は、**このテーマ族では律速指標ではなかった**。集中度が健全でも名簿が2件なら出力は全滅する。**2件の名簿から作った bridge の上で多様性を回復させても、返るのは「多様な無関係」である。** ⇒ 観測プロトコルの主指標は `bridge 集中度` ではなく **`生存シード数` と `semantic レッグ供給数`** に置き換えるべき（両方とも診断ブロックに既に出ている）。

**同型性の記録（seihai 側の当日主題と一致）**: seihai の 2026-09-09 r03 は「`pbo_veto` という上位ラベルが、実際には律速でなかった段を律速として報告していた」を実測した日で、**bybridge の本日の失敗はまったく同じ形**（集中度という測れる指標を測り、律速でない段を監視し続けていた）。観測プロトコルが、それ自身が検出しようとしていた誤りを犯していた。

**contra 側の処方候補（実装していない）**: (i) 生存シードが閾値（例: 5件）を下回ったら**出力を返さずその旨を返す**（今の挙動は「2件から作った結果」を通常出力と同じ顔で返す）、(ii) semantic レッグ 0 件を**警告でなくエラー扱い**にする、(iii) `seed_language` の刈り取りが過半を超えたら自動で言語ゲートを緩めて再取得する。

> ✅ **2026-09-11: 二重故障の片方（semantic レッグ 0 件）は機序を特定し対処済み**（下記「対処済み」節 **F-13-S**）。日本語テーマ本文をそのまま埋め込み検索に入れていたため、同言語の日本語文献が返り、選別段で全滅していた。非英語本文は英語 keywords（または呼び手の `seed_semantic_text`）に差し替えるようにした。**(i)(iii) の「生存シード 2 件でも通常出力の顔で返す」「言語ゲートの刈り取り」は未対処のまま。**

---

### F-16. 再現と規約の初成功（2026-09-09・seihai r03 日次）

`byserendipity raw_only` の facet 1（machine learning / adaptive model selection pipelines）が **`HTTP Error 504: Gateway Timeout`** で 0 件。**S-112 の規約（0 件 facet は必ず単独再投で切り分けてから解釈する）どおりに同じドメイン語で単独再投したところ 34 件が返り**、当日の bynote の Near 距離（責任帰属の Shapley 按分）がその中の1本で接地した。**規約が実際に距離段を救った初回の記録。** ⇒ F-16 の「上流の一過性障害」側の再現であり、**呼び手側の規約で回避可能**であることの実証。

---

### F-20. 再現（2026-09-09・seihai r03 日次・3週連続の収穫ゼロ）

`keywords_include: ["model-selection","pipeline","filtering","provenance","counterfactual"]` に対し、返った4アンカーは `semantica-agi/semantica`（Graph-Native AI インフラ・**関連度 0.67**）、`BiomedSciAI/causallib`（因果推論・**関連度 1.0**）、`alibaba/open-code-review`（**0.6**）、`Emily2040/seedance-2.0`（**AI 映画制作パイプライン**・**0.7**）。F-20 の初観測（関連度 0.0 のまま Reliability だけで上位）とは違い、**今回は関連度が高く出ている**——`pipeline` / `filtering` / `model-selection` が README に出現するだけで満点が付く。**関連度 1.0 の `causallib` すら主題（多段フィルタの原因帰属）を扱っていない。** ⇒ **F-20 の一般形＝「関連度係数は語の出現を測り、主題適合を測らない」**。S-39 が `Reliability Score` について指摘した構造欠陥が、関連度側にも同じ形で存在する。seihai 側では byrepo の収穫ゼロが**3週連続**（8/26・9/02・9/09）。

---

### F-25. bybridge — **`diversify_head_by_bridge` の「1 bridge あたり上位10件中2件まで」枠は、候補が複数の bridge を引用すると効かない**（2026-09-08 起票・contra 側で発見。F-23/24-R の対処中に可視化された）

**機序（コード読解で確定・実測で裏付け）**: `src/pipeline/bridges.py` の枠は、候補が引用する bridge の**すべてが満杯のときだけ**その候補を後回しにする（`all(counts[b] >= cap for b in cited)`）。したがって候補が {最頻 bridge, 小さい bridge X} の2本を引用していれば、**最頻 bridge の枠が満杯でも X に空きがあるかぎり着席し、しかも着席時に最頻 bridge のカウントも増える**。⇒ 枠は「最も混んでいる bridge」を抑えない。

**なぜ今まで見えなかったか**: 枠を入れた 2026-08-22 当時、候補あたり平均共有 bridge は **1.00 本**だった（＝どの候補も bridge 1本しか引用していないので、枠は意図どおり効く）。F-23/24-R で取得段を per-bridge 配分にしたところ平均が **1.85〜2.08 本**に上がり、**枠の前提が崩れた**。実測: F-24 フィクスチャで最頻 bridge の上位10件占有率が **20% → 30%**、strategy_generation フィクスチャで **0% → 50%** と、プール集中度が下がったのに**上位窓の集中は上がった**。

**⇒ これは F-23/24-R の副作用ではなく、元からあった枠の欠陥が新しい条件下で表面化したもの。** 上位窓の中身自体は改善している（後述の対処済み節の head-10 比較を見ること）ので、**「上位窓占有率」という指標が、平均共有 bridge が 1 を超えた条件では以前と同じ意味を持たない**点に注意。**S-26 の観測記録で上位10件占有率を経時比較するときは、同じ行の「候補あたり平均 bridge」も併せて読むこと。**

**contra 側の処方候補（2026-09-08 時点）**: 枠の判定を「引用する bridge のうち**最も混んでいる**1本」で行う（`any` ではなく最頻 bridge 基準）か、着席時のカウント加算を「その候補を通した bridge」1本に限る。**どちらも上位窓の見た目を変えるので、head-10 の中身の before/after を必ず並べて判断すること。**

> ⚠ **2026-09-12: 実装して実測し、既定にはしなかった（＝試して外れた処方として残す）。** `diversify_head_by_bridge(strict_cap=True)` として最頻 bridge 基準の枠を実装し、同一候補プール上で `strict_cap` と `per_bridge_cap` だけを切り替えて比較した（実 API・2テーマ）。
>
> | プール | legacy cap2 | strict cap2 | strict cap3 | strict cap4 |
> |---|---|---|---|---|
> | strategy_generation の上位10件占有率 | **100%** | 20% | 30% | 40% |
> | retrigger_hysteresis の上位10件占有率 | 40% | 20% | 10% | **0%** |
>
> **数値は意図どおり改善したが、中身は単調に悪化した。** retrigger では legacy の上位10件にあった **Detection of false investment strategies / FORMALIZED DATA SNOOPING BASED ON GENERALIZED ERROR RATES / Estimating Stock Market Betas**（いずれも主題直撃）が窓から押し出され、代わりに **ESG preferences / Corporate culture / Annual report readability / CSR と株主価値**が入った。strategy_generation では **GP の他分野適用 10/10** だった窓が **GP 5 件＋イノベーション経営論（Absorptive Capacity・Dynamic capabilities・Psychological Safety）**になった。
>
> ⇒ **F-23/24-R でプールの成り立ちを直した後は、上位窓占有率は上位の質の代理指標ではない。** 枠を強めることは「bridge の身元で並べ替える」ことであり、**主題直撃の候補を降格させる方向に働く**。⇒ **既定は旧挙動のまま（`strict_cap=False`）**、opt-in として実装だけ残した。**S-26 の観測プロトコルで上位10件占有率を主指標として読むのは、この日をもってやめてよい**（F-26 が既に「主指標は生存シード数と semantic レッグ供給数」と書いた結論と同じ向き）。

---

### F-20. byrepo — **略語のキーワードが無関係語の部分一致を拾い、関連度 0.0 のまま Reliability だけで上位に載る**（2026-09-05 初観測・seihai 週次指針） → **(i) 部分一致は対処済み（2026-09-12）。(ii) 係数の床は未対処＝下記 F-29 として分離**

**症状**: `keywords_include: ["cuped", "variance-reduction", "confidence-sequence", "e-value", "ab-testing"]` で実行したところ、**5位に `OpenPrinting/cups-filters`（印刷サブシステムのフィルタ群・関連度 0.0・Reliability 91/100・順位スコア 31.8）** が入った。**`cuped` が `cups` に部分一致した**もの。6位は `huggingface/transformers`（関連度 0.0・stars 164,793）。

**⇒ 2つの機序が重なっている**: (i) **略語キーワードの部分一致**——F-13 の語彙衝突が bybridge のシード段ではなく **byrepo の検索段で、しかも略語の部分文字列**で起きた初例（`cuped`→`cups`）。(ii) **関連度係数の床**——8/29 に観測した「関連度 0.0 でも係数に床があるので実質 Reliability だけで順位が決まる」が再現。**関連度 0.0 と自己申告している項目が、上位6件のうち2件を占めている。**

**★ただし本日は収穫ゼロではない（正直に記録する）**: `sb-ai-lab/HypEx`（関連度 0.67・因果推論+ABテストのツールキット・Apache-2.0・stars 130）と **`david26694/cluster-experiments`（"Power analysis and AB test analysis library"・MIT・stars 56）** は実際に呼び手の用途（CUPED 型分散削減・検出力計算）に当たる。**8/29 までの「7週連続で使える収穫ゼロ」は本日途切れた。**

**★しかし順位は有用性を追っていない**: 最も直接的に用途に当たる `cluster-experiments` は **関連度 0.33・ラベル「低」・3位**、一方で関連度 0.0 の印刷ライブラリが5位に居る。**関連度ラベル（高/低）と実際の有用性の相関は、本日のサンプルでは見て取れない。**

**contra 側で見るとよい点**: (a) キーワードのマッチを**単語境界**で取るか、短い略語（4文字以下）は部分一致を禁じる。(b) 関連度 0.0 の項目を順位に載せるなら**別枠にする**か、係数の床を外す。

---

### F-21. 全 by\* 共通 — **道具の自己記述（MCP スキーマの説明文）が、その道具の検証器と食い違う**（2026-09-05 初観測・9/11 と 9/12 に再現＝3例） → **対処済み（2026-09-12）。下記「対処済み」節 F-21-R へ**

**症状**: `byrepo_search` の `approach_type` の説明文は **"Type of approach (e.g., theory, experiment, system-building, application)"** と書いてあるが、`system-building` を渡すと **`InputValidationError: approach_type is invalid`** で拒否される。検証器は `APPROACH_TYPES = {"theory", "experiment", "application"}`（`src/core/input_schema.py:38`）＝**スキーマ自身が例示した4値のうち1つが集合に無い。**

**害**: 呼び手（エージェント）は説明文を読んで値を決めるので、**説明文にある値を選ぶと必ず落ちる**。本日は1回ぶんの往復が失われた。**エラーメッセージは「invalid」としか言わず、許容値の集合を返さない**ので、呼び手はコードを読むまで正解を知れない。

**contra 側で見るとよい点**: (a) 説明文と `APPROACH_TYPES` を**同じ定数から生成**する（`enum` にすれば MCP クライアント側で検証される）。(b) 少なくともエラーメッセージに許容値を列挙する。**同じ形の問題は `scope_scale` / `scope_time_range` にもありうる**（どちらも説明文に既定値だけ書いてある）。

**呼び手側の教訓（seihai の S-104 として起票）**: **道具の説明文・既定値・診断表示は、その道具の検証器や実装と食い違いうる。詰まったら説明文ではなくコードを読む。** seihai 側で同型の失敗を既に3件記録している（S-89 = タスク仕様書の「再コンパイルで自動リロード」が誤り／S-96 = 欠測を異常値 `single_param` で表す既定値／F-13-I = 粗い属性1つを緑のスカラで申告）。

---

### F-22. delegate_finalize — **セレンディピティ・スコアが `距離 × 構造` なので、近い分野の当たりは有用でも構造的に通過できない**（2026-09-05 初観測・設計上の帰結であって較正不足ではない）

**症状**: 同一の候補集合を**2回**投入した。**7件提出 → percentile 閾値 0.474 → 2件通過／4件提出 → 閾値 0.554 → 1件通過。** 両方の実行で、呼び手が**最も実務価値が高いと判断した候補**——[CUPED](https://doi.org/10.1145/2433396.2433413)（Deng et al. WSDM 2013・被引用228・オンライン統制実験の分散削減）——が **実測 0.383 で落選**した。

**機序**: CUPED は呼び手のテーマ（対比較の分散削減）に対して **purpose_sim 0.89** と極めて近いが、**まさに近いがゆえに mechanism_dist を 0.43 としか付けられない**。セレンディピティ・スコアは距離と構造の積なので、**距離が低い候補は構造がどれだけ深くても上限が低い**。⇒ **「有用だが近い」は原理的に通れない。**

**なぜこれが害か**: 呼び手のテーマは「速さ（相関を上げる）と探索（腕を違わせる）のジレンマ」で、**CUPED はそのジレンマを解消する**（相関を*腕どうしの類似*ではなく*事前期間の共変量*から取れば、腕は違ったままでよい）。**その週の意思決定を最も動かした1本が、post-gate では最下位近くに落ちた。**

**⇒ これは F-15（percentile 閾値が提出数で動く）と別物**: F-15 は**相対性**の問題、本件は**スコアの定義**の問題。**閾値をどう動かしても近い当たりは通らない。**

**contra 側で見るとよい点**: (a) セレンディピティ・スコアとは別に **`utility` 系の順序**（purpose_sim 主導）を併記し、post-gate の落選候補にも「近いが有用」ラベルを残す。(b) 少なくとも**落選内訳に purpose_sim を併記**する（現在は serendipity 実測値と閾値だけなので、落選理由が「遠くない」なのか「浅い」なのか読めない）。

> ✅ **2026-09-12: 計器を実装した（スコアの定義は変えていない）。** 落選内訳の各行に `purpose_sim × mechanism_dist` と**律速がどちらか**を併記し、`purpose_sim ≥ 0.70` かつ距離が律速の落選を **『近いが有用』として名指し**、「距離 × 構造である以上どの閾値でも通らない／採否の最終判断は呼び手にある」と出力に明記するようにした（詳細は「対処済み」節 F-22-I）。**スコアの定義（距離 × 構造）は設計上の帰結として維持**しているので、F-22 の本体（近い当たりが通過できない）は未対処のまま残す。

**呼び手側の対処（本日実施）**: **CUPED を落選のまま採用した。** 本ルーティンでは LLM 役は呼び手エージェントであり、**contra の post-gate は決定論的検証であって採否の最終権限ではない**（案X の分担）。**落選を理由に有用な文献を捨てない。**

---

#### 再現・追記（2026-09-05・seihai 週次指針）

- **F-16（長さ仮説）＝前向きに確認された。** 8/31 と 9/04 は「落ちてから短くして直した」事後確認だったが、**本日は最初から3枚とも pseudo_abstract を約80語・`structure` を5文に抑えて投げ、3枚とも成功（返却 50/50/50 → 提出 60候補）**。⇒ **処方は予防としても効く。** 距離段の欠損ゼロで A2 プロトコルが初めて完全な形で回った。
- **F-19（材料欄 echo 忘れ）＝再演し、処方で即復旧した。** 材料欄（title/abstract/year/venue/cited_by_count）を落として投げたところ **7/7 が「照合不能」**。**全欄 echo して同じ引用のまま再投したら全件成立。** F-19-R の診断文（「引用の誤りではなく材料欄の欠落が原因」と明記する）は**呼び手を正しい修正に一発で導いた**＝**対処済みの評価は妥当**。ただし**再演したという事実自体が、呼び手側の手順として定着していないことを示す**ので、seihai 側の運用手順に明記した。
- **F-13 ＝ 逆向きの乖離を初観測。** bybridge のシード名簿診断は `分野(Field) 一致 20/20 = 100%`・上位トピック `Sports Analytics and Performance 4` と出て、**一見「主題外のスポーツ分析で埋まっている」ように読める**。しかし実体を読むと、その4件は *Parameter Estimation in Large Dynamic Paired Comparison Experiments*（JRSS-C 1999）／*Urnings: A New Method for Tracking Dynamically Changing Parameters in Paired Comparison Systems*（JRSS-C 2021）／*Receiver operating characteristic analysis for paired comparison data*（JRSS-A 2024）等で、**呼び手のテーマ（対比較）の方法論的正典そのもの**だった——OpenAlex が Bradley–Terry 系の文献を Sports Analytics に分類しているだけである。⇒ **9/04 の事例（診断が 100% と申告し実体は NAFTA・Fox News）とは*逆向き***。**トピックラベルは主題適合を過大にも過小にも誤る**ので、**診断ブロックの数値だけで名簿の質を判定しない**（呼び手はシード表の実タイトルを読むこと）。
- **bybridge の S-26 観測プロトコル ＝ 6回目で初の明確な成功。** 最頻 bridge の上位10件占有率は 20 / 20 / 20 / 70 / 20 / **0**％（本日が最良・全体では 25%・使われた bridge 19本・候補あたり平均 1.33本）。最頻 bridge は **Heckman "Sample Selection Bias as a Specification Error"（被引用 29,158）**＝巨大ハブだが**呼び手の主題（勝者選抜のバイアス）に正面から当たる**。**実収穫あり**＝[Comparing Predictive Accuracy](https://doi.org/10.1080/07350015.1995.10524599)（Diebold–Mariano 1995・**2つの競合予測の精度差を、予測誤差が同時点で相関していてよい前提で検定する**＝呼び手が手作りしている対比較統計の正典）、[Rank-Order Tournaments as Optimum Labor Contracts](https://doi.org/10.1086/261010)（Lazear–Rosen 1981）、[A Practitioner's Guide to Cluster-Robust Inference](https://doi.org/10.3368/jhr.50.2.317)（Cameron–Miller 2015）。⇒ **「3回とも旧様式なら恒久除外」の条件は満たされていない。除外しない。**

### F-23. bybridge — **シードが主題直撃でも、最頻 bridge が「ソフトウェア引用」になって交差候補を丸ごと持っていく**（2026-09-07 初観測・F-13 とは別段の故障） → **プール段の機序を特定し対処済み（2026-09-08）。下記「対処済み」節 F-23/24-R へ**

**観測（2026-09-07・seihai r01 担当日）**: テーマ＝「極値だから選ばれた候補の真値推定／選抜補正」（`materials:true`・seed_count 20・bridge_count 20）。

**シード段は今回きわめて良好**——F-13 は**起きていない**。Field 一致 20/20、Subfield 一致 7/20、semantic レッグ供給 11件、そして名簿に **[Inference on Winners](https://doi.org/10.1093/qje/qjad043)（QJE 2019）**・**[Data-Snooping, Technical Trading Rule Performance, and the Bootstrap](https://doi.org/10.1111/0022-1082.00163)（J Finance 1999・被引用 1,057）**・**How "Backtest Overfitting" in Finance Leads to False Discoveries**（Significance 2021）が入った。**主題に正面から当たるシードが揃った初めての回。**

**それでも交差候補30件は主題と無関係だった**: lme4 / limma / phyloseq / pROC / brms / ape / phytools / GSVA / REFMAC5 / WEKA / bibliometrix / dropout / LASSO / elastic net / LARS / compressive sampling / implicit association test …。実質的に主題に触るのは **[Trim and Fill](https://openalex.org/W2033585778)**（出版バイアス補正＝選抜で消えたものを残った標本の形だけから復元する）**1件のみ**。

**診断値**: 最頻 bridge が交差候補の **62%**（上位10件では **40%**）／候補あたり平均 bridge 1.02本／共有 bridge 1本のみの候補 **59/60**／実際に使われた bridge **3本**。**最頻 bridge = `R: A Language and Environment for Statistical Computing`（被引用 353,393）。**

**★これが F-01（phantom bridge）とも F-13（シードが外れる）とも違う点**: bridge は実在し、シードも主題に合っている。**外れているのは「共有参照＝知的祖先」という前提**である。**方法論寄りの論文どうしが最も高頻度で共有する参照は、思想ではなく道具（統計ソフトウェア）**であり、道具を共有する集合は分野を横断するが**主題は横断しない**。F-01 の処置（実在しない id の除去）は「参照が本物か」を見るが、**「参照が思想か道具か」は見ていない**。

**contra 側で見るとよい点（seihai は処方を決めない）**: (a) bridge プールを作るとき、**ソフトウェア／データセット／汎用手法パッケージの記述子を持つ works を bridge 候補から外せるか**（OpenAlex の `type` や `primary_topic` で識別できる可能性）。(b) より一般に、**bridge の被引用数が桁外れ（例: 10万超）のものは「共有された祖先」ではなく「全員が使う道具」である**という仮説が、この1例で立つ。ただし **2026-08-18 に「中心性ペナルティ」は実測で棄却済み**なので、**被引用数そのものではなく種別で切る**方が前回の反証と整合する。(c) 判断材料として: 8/29 の成功回（Diebold–Mariano / Heckman が通った回）の最頻 bridge は**巨大ハブだが文献**だった。**本件は文献ですらない**という点が新しい。

**S-26 / S-67（観測プロトコル）への記録**: 上位10件占有率 **40%**（旧様式の 100% からは改善が持続）／最頻 bridge は**主題ドメイン外かつ道具**。**「上位は多様化したが収穫はゼロ」の新しい下位様式**として、field_observations に区別して記録する（S-26 の裁定文が求めた区別）。

---

#### 再現（2026-09-08・**504 の単独再投で距離段が完全復旧**／seihai r02 担当日）— F-16

**症状**: A2 の3距離 facet（(1) 制御工学のチャタリング／(2) 行動生態学のパッチ離脱／(3) 細胞神経生理学の順応・不応期）を渡した `raw_only` 実行で、**facet [3] のみ `取得失敗 (request failed after 3 attempts: HTTP Error 504: Gateway Timeout)`**。他2枚は 50/49 件を返し、合計 60 件が材料になった。診断は 8/31 の改善どおり「⚠ 収穫0の facet: cellular neurophysiology。…A2 の3距離が実質的に潰れている状態」と自分から名指しした。

**★9/04 と違う点＝今回は長さ仮説では説明できない**（504 であって 400 ではない）。**にもかかわらず処方は同じで効いた**: facet [3] を**同じドメイン語・ほぼ同じ pseudo_abstract のまま単独で投げ直す**と **返却 50 / ホーム除外後 39 / 提出 39 件**で成功し、中身も主題ど真ん中（"Refractory Sampling Links Efficiency and Costs of Sensory Encoding to Stimulus Statistics" / "Optimal information gain at the onset of habituation to repeated stimuli"）。**この距離段は当日の bynote（A3 実文献接地）の Very Far を単独で成立させた**＝失われかけた知見の量は小さくない。

**⇒ 呼び手側の一般化（seihai は当日 S-112 として起票）**: **ゼロ件の facet を「その距離は不毛」と読んではいけない。** 意味的な収束（`home_converged`）と上流の一過性障害（400/504）は、**出力の見た目が同じ「0件」**なので、**必ず単独再投で切り分けてから解釈する。** 9/04 は診断文の推測（飽和/近すぎ）が呼び手を誤結論の寸前まで連れて行ったが、本日は**呼び手が先に単独再投を規約化していたので実害が出なかった**——**F-16 の処方が呼び手側の手順として定着した初めての回**として記録する。

**contra 側で見るとよい点**: 504 は3回リトライでも抜けなかったが**単独呼びでは1回で通った**。⇒ 失敗が**リトライ回数ではなく同時実行のペイロード総量**に依存している可能性（F-11 の polite pool 据え置きと合わせて読む）。

---

### F-24. bybridge — **旧様式（分野外の巨大ハブ1本が交差候補を独占）が、シード名簿の劣化を伴って明確に再現した**（2026-09-08 観測・S-26 観測プロトコル 5回目） → **対処済み（2026-09-08）。下記「対処済み」節 F-23/24-R へ**

**観測（2026-09-08・seihai r02 担当日）**: テーマ＝「トリガが持続する水準なので失敗直後に同じ行動が再点火し、評価器はそれを1回分としか課金しない」（`materials:true`・seed_count 20・bridge_count 4）。

**診断値**: 最頻 bridge が交差候補の **85%**（上位10件では **40%**）／候補あたり平均 bridge **1.00** 本／共有 bridge 1本のみの候補 **60/60**／実際に使われた bridge **5本**。**最頻 bridge = `Theory of the firm: Managerial behavior, agency costs and ownership structure`（Jensen & Meckling・被引用 71,804）。**

**交差候補30件の中身**: Separation of Ownership and Control / Law and Finance / The Capital Structure Puzzle / Agency Theory: An Assessment and Review / Corporate Ownership Around the World …＝**全30件がコーポレートガバナンスと資本構成のメガハブ**。主題（水準トリガの反復発火・sim↔live 乖離）に触るものは**ゼロ**。

**シード名簿（F-13）**: Field 一致 20/20＝100% だが **Subfield 一致 7/20＝35%**。名簿に "Informal Cross-Border Trade and Trade Facilitation Reform in Sub-Saharan Africa"・"India's Trade Integration"・"Jobs for the Heartland"・"Climate Change Policy and Economics" が入り、主題直撃は "Data-Snooping, Technical Trading Rule Performance, and the Bootstrap"・"A Bayesian Approach to Measurement of Backtest Overfitting"・"Backtesting and Profitability Analysis of Algorithmic Trading Strategies" の3件程度。**⇒ 9/07（F-23）はシード段が良好だったのに交差が外れた回、本日はシード段も交差段も外れた回**であり、**2つの故障は独立に起きる**ことが2日で示された。

**★F-23 との違い（区別して記録する＝S-26 の裁定文が求めた区別）**: 9/07 の最頻 bridge は **`R` の引用＝道具**だった。**本日の最頻 bridge は文献であり、しかも経済学の正典**である。⇒ F-23 で立てた仮説（「被引用が桁外れの bridge は道具である」）は**本件を説明しない**。本日の様式は**F-01 対処前の「旧様式」そのもの**（分野外の巨大ハブ文献1本が候補集合を決める）で、**上位10件占有率 40% は 100% より低いものの、プール全体では 85% 集中**している。⇒ **上位窓の多様化は「上位の見た目」を直したが、プールの成り立ちは直していない**、というのが5回分の観測の要約。

**S-26 / S-67 への記録（seihai 側の裁定意見）**: S-26 の事前登録は「3回とも旧様式なら本テーマ族への不適合が確定＝恒久除外」。**5回目の本日は旧様式の両条件（1本のbridgeへの集中・分野外ハブ）を満たす**ため、**観測者としての結論は「不適合が確定した」**。ただし**裁定はユーザーの判断項目（保留 D）なので seihai 側では除外を実行しない。**

---

#### 再現（2026-09-08・byrepo）— F-03 / F-05 系の様式が2週連続

**観測**: テーマは上記と同じ（`structured:true`・`track_a_count:4`・`keywords_include` を `backtest / live-trading / signal / execution / reconciliation` と主題語で具体化）。返った4件は **HKUDS/Vibe-Trading（★32,977）/ OpenByteInc/QuantDinger / coding-kitties/investing-algorithm-framework / agutinbaigo28/trading-backtest-kit**＝**いずれも「トレーディング一般で星の多い repo」で、主題（水準トリガの反復発火・sim↔live 乖離の検出）に触るものはゼロ**。

**数値**: 4件中3件が **theme関連度 1.0** で並び、順位は実質 Reliability だけで決まった（92.8 / 90.0 / 90.0 / 80.0）。RELATIONSHIP と HYPOTHESIS は全件定型文（「関係軸『実装アンカー』で関係度『高』の関連性がある」「この論文の知見をテーマの具体的局面に転用できるか要検討」）。**9/01 と同一の様式で、2週連続の再現。**

**⇒ 呼び手の読み**: `keywords_include` を**分野語ではなく主題語**にしても様式が変わらなかったので、**関連度の分母が「分野に属するか」で飽和している**疑い（F-20 の略語問題とは別で、本件のキーワードは略語ではない）。**F-03 は 2026-08-21 に対処済みとされているが、`structured:true` 経路で関連度が全件 1.0 に張り付くと、対処後のランキング式でも関連度項が定数になり実質 Reliability 単独に戻る。**

---

### F-29. byrepo — **関連度係数の床（0.35）が高いので、Reliability の差が関連度の差を飲み込む**（2026-09-12 起票・F-20 (ii) の分離）

**F-14/20-R の実測で分離された残り半分。** 順位スコア＝`Reliability × (0.35 + 0.65 × 関連度)` なので、関連度が 0.2 でも係数は 0.48、1.0 でも 1.0——**係数の可動域は 2.1 倍しかない**。一方 Reliability は 30〜95 まで動くので、**3 倍以上の差がつく**。

**実測（2026-09-11 の実テーマ・実 GitHub API）**: `freqtrade`（関連度 **0.20**・Reliability 89 → 42.7）が `morganstanley/MSML`（関連度 **0.40**・Reliability 54 → 32.9）に勝つ。**関連度を正しく測れるようにしても順位は1つも動かなかった。**

**逆転に必要な床の値（計算）**: `54 × (f + (1−f)×0.4) > 89 × (f + (1−f)×0.2)` を解くと **f < 0.098**。⇒ 現行 0.35 を **0.1 近くまで下げる**必要があり、これは「語彙一致ゼロのアンカーもランク可能に保つ」という床の当初目的（2026-08-21 の F-03 対処）と正面から衝突する。

**⇒ 判断が要る**: (a) 床を下げる（関連度がほぼ支配的になる＝語彙マッチが粗いテーマで良質な実装を落とすリスク）、(b) 関連度を乗算でなく**足切り**に使う（例: 関連度 0.2 未満は別枠へ）、(c) Reliability の可動域を圧縮する。**どれも byrepo の全順位を変えるので、実テーマ複数本での before/after が必要**。次の担当か人間の判断で。

---

### F-27. bybridge — **semantic シードレッグの home Field 選別が、主題にいちばん近いシードを捨てる**（2026-09-11 起票・contra 側で発見。F-13-S の計器で初めて見えた） → **同日対処済み。下記「対処済み」節 F-27-R へ**

**機序（実測）**: F-13-S で semantic レッグの問い合わせを英語にしたところ、返る 50 件は全件英語・主題直撃になった。**それでも `home Field 外` の段で 24〜42 件が落ちる**。実測（home=`Economics, Econometrics and Finance`・実 API）:

| フィクスチャ | 検索文 | 返却 | abstract 無し | **home Field 外** | 供給 |
|---|---|---|---|---|---|
| strategy_generation | keywords | 49 | −7 | **−42** | 0 |
| strategy_generation | 英語擬似アブストラクト | 50 | −9 | **−40** | 1 |
| retrigger_hysteresis | 英語擬似アブストラクト | 46 | −9 | **−37** | 0 |
| trade_collision | 英語擬似アブストラクト | 50 | −12 | **−31** | 7 |

**落ちたものの中身**: strategy_generation を Field 選別なしで引くと 41 件が残り、上位は *Is Technical Analysis in the Foreign Exchange Market Profitable? A Genetic Programming Approach*（Decision Sciences）/ *Comprehensibility & Overfitting Avoidance in Genetic Programming for Technical Trading*（Computer Science）/ **Behavioural GP diversity for dynamic environments**（Computer Science）/ *Cooperative Coevolution of Technical Trading Rules*（CS）…＝**テーマ（GP 売買ルール生成の候補相関と行動多様性）の正典そのもの**。OpenAlex はこれらを Economics ではなく CS / Decision Sciences に分類しているので、**Field 選別が主題直撃を落とし、Economics に分類された周辺文献だけを残す**。

**なぜ放置されていたか**: 8/28 の設計は Field 選別を「語彙レッグの同綴異義語対策」として導入し、semantic レッグにも同じ選別を掛けた（「残す側でクライアント選別」）。**semantic レッグは埋め込みの近さで既に主題を絞っているので、粗い Field 選別を重ねる理由が語彙レッグほど無い**——F-13-I 自身が「Field は主題適合ではない」と書いた属性で、主題適合を測る側のレッグを刈っている。これまでは日本語問い合わせで**そもそも英語の主題文献が返っていなかった**（F-13-S）ので、この段で何が落ちているかは見えなかった。

**処方候補（今日は実装していない＝1日1件の規約＋設計判断が絡む）**: (a) semantic レッグだけ Field 選別を外す（または「home Field ＋ 上位 N 件は Field 不問」）。(b) 選別を Field でなく **Subfield/Topic の近さ**で行う。**ただし下流2箇所と相互作用する**——(i) F-13 の Field 一致率計器が CS 分類のシードを「分野外」と数える、(ii) 交差候補の L0 外判定（ホーム除外）がシードの Field を基準にしているなら、除外の境界が動く。9/11 の seihai 観測「名簿も橋も主題側・しかし出力が交差しない」（F-13 の7回目）はこの (ii) の境界の粗さを疑っているので、**F-27 と一緒に設計するのが筋**。⇒ 次の担当か人間の判断で。

---

## 対処済み

### F-26-R. bybridge — 生存シードの**下限**と、**一度だけ広く取り直す**再取得 — **対処済み 2026-09-12**

**F-26 の処方候補 (i)(iii) をそのまま実装した**（(ii) semantic 0 件の原因表示は F-13-S で対処済み）。

- **(iii) 先に広く取り直す**: 生存シードが下限を割り、かつ語彙シードが満杯だった場合（＝ゲートは正しく、**供給が足りなかった**場合）、**`seed_count × 9` で1回だけ取り直す**。2026-09-09 の実データ形（60件中 49 が言語ゲート・9 が空参照で落ちて 2 件）は、この再取得で埋まる型である。
- **(i) それでも足りなければ返さない**: 下限（既定 **5 件**）未満なら、交差候補を作らず**拒否**して内訳を返す——語彙/semantic の取得数と取得ラウンド、言語ゲートで落ちた数、`referenced_works` 空で落ちた数、semantic レッグ内訳、そして**打ち手4つ**（`seed_language: null` ／ `seed_semantic_text` に英語擬似アブストラクト ／ キーワードを英語の標準語彙へ ／ 承知で回すなら `min_seeds: 0`）。
- **下限の根拠**: 2026-09-09 の失敗は **2 件**の名簿から 30 件の交差候補（臨床歩行分析）を**通常の出力と同じ顔で**返した。同時期の健全な run は 20/20。**5 は失敗側と健全側の帯の間**にあり、`min_seeds` で呼び手が上書きできる。

**なぜ「返さない」が正しいか**: F-26 の害は収穫ゼロではなく、**出力の見た目が健全な run と区別できないこと**だった（呼び手は丸一日、bridge 段の失敗として読んでいた）。⇒ **区別がつかない出力を返すくらいなら返さない**。S-68（情報の無さを正常値で表さない）の出力版。

**検証**: 新規回帰3件（下限割れで拒否し全ゲートの内訳と打ち手を名指しする／`min_seeds:0` で無効化できる／**供給不足のときだけ広く取り直して下限を越える**）。既存の bybridge 経路テスト（合成シード1〜3件）は `min_seeds:0` を明示して自分の契約を守るよう更新。**455 → 458 tests: 458 pass。**

**seihai 側への申し送り**: **bybridge が「結果を返さない」ことがある**——これは故障ではなく F-26 の対処。返ってきた内訳を insights に記録してほしい。日本語テーマで頻発するなら `seed_language: null` か `seed_semantic_text` の常用が実務上の答えになる（どちらも呼び手側の運用判断）。

---

### F-14/20-R. byrepo — 関連度が**語の出現数**でなく**キーワード網羅率**になり、一致キーワードが出力に出る — **対処済み 2026-09-12**

**3つの欠陥が同じ関数の3つのコピーに入っていた**（`git_collect` / `hf_collect` / `kaggle_collect` がそれぞれ自前の `_theme_fit_score` を持っていた）。共有モジュール `src/pipeline/theme_fit.py` に統合して直した。

1. **部分一致**: `"cuped" in strong` が `OpenPrinting/cups-filters` に当たっていた（9/05・印刷サブシステムが CUPED クエリの5位）。⇒ **正規化した語境界一致**（英数字以外を空白に畳んでから前後を空白で挟んで照合）。
2. **区切り文字の無視**: `"quality-diversity"` が散文の "quality diversity" に当たらず、主題の標準ライブラリが取り分を失っていた。⇒ 両側を同じ規則で正規化。
3. **飽和**: スコアが `credit × 10` の上限 30 だったので、**キーワード3本当たれば上限**に達し、それ以上の差が消えていた（9/10・4件が全部 1.0 で順位が Reliability 単独に戻り、無関係なエージェント用スキル集が pyribs の上に 1.0 点差で座った）。⇒ **関連度＝呼び手のキーワードの網羅率**（credit ÷ キーワード数）。3/5 は 0.6、5/5 で 1.0。

**実測（実 GitHub API・9/10 と 9/11 の実テーマ・同一取得を新旧2つの式で採点）**

*9/10「quality diversity」*

| repo | 関連度(旧) | 関連度(新) | 順位スコア(旧) | 順位スコア(新) | 一致 |
|---|---|---|---|---|---|
| openevolve | **1.00** | 0.60 | 78.0 | 57.7 | 3/5 |
| **icaros-usc/pyribs** | **1.00** | **0.80** | 72.0 | **62.6** | 4/5 |
| QDax | **1.00** | 0.73 | 68.0 | 56.2 | 4/5（部分含む） |
| Agent-Skills-for-Context-Engineering（無関係） | 0.33 | **0.20** | 44.8 | 37.9 | 1/5 |

⇒ **順位が `openevolve > pyribs > QDax` から `pyribs > openevolve > QDax` へ。** 9/10 に seihai が報告した「主題直撃の pyribs が無関係なスキル集より下位（4位）」は、**実データで解消を確認**した。

**F-14 側の対処（証拠行）**: 一致したキーワードと**どの面（名前/説明/topics か README か）で当たったか**を `source_meta` に持たせ、structured と LLM の両経路の出力に **`一致キーワード 4/5: quality-diversity（名前/説明/topics）/ archive（README・部分 0.4）`** として印字する。**散文が定型文であること自体は直していない**が、**定型文の隣に、その repo に固有の決定論的な証拠**が並ぶようになった。一致 0 件は「なし（0/5）」と明示（S-68 の規約）。

**効かなかったこと（正直に）**: **9/11 のテーマ（off-policy evaluation）では順位が1つも変わらなかった。** `freqtrade`（`backtest` だけ一致・関連度 0.20・Reliability 89）が `morganstanley/MSML`（off-policy evaluation + counterfactual・関連度 0.40・Reliability 54）より上のまま。⇒ **これは飽和ではなく係数の床の問題**で、下記 **F-29** として分離起票した。

**検証**: 新規回帰6件（略語の部分一致が 0 になること／ハイフン語が散文に当たること／3本当たっても飽和しないこと／**実際に観測された逆転（pyribs 対 skills 集）が反転すること**／証拠行の描画／一致0件の明示）。旧スケール（絶対ヒット数）を固定していた既存テスト8件を新契約（網羅率）へ更新。**449 → 455 tests: 455 pass。**

**seihai 側への申し送り**: (1) **関連度の数値の意味が変わった**——「何本のキーワードが当たったか」ではなく「**宣言したキーワードのうち何割を満たすか**」。したがって**キーワードを増やすと1本あたりの重みが下がる**（5本宣言して2本一致は 0.4）。(2) 出力に `一致キーワード` 行が出るので、**関連度の数値を疑う前にこの行を読める**。(3) 9/11 型（高 Reliability × 低関連度が勝つ）は未解決で、F-29 として起票済み。

---

### F-16-R. byserendipity — semantic クエリを**送信前に切り詰め**、400 なら**1回だけ短縮して再取得**する — **対処済み 2026-09-12**

**呼び手が手でやっていた復旧（「同じドメイン語のまま、短く」）を自動化した。** 8/31（単独 facet）と 9/04（3枚とも）で確認済みの処方で、原因は 2026-09-11 の実測どおり**長さ**（英語 1,575 字で 400・1,437 字は成功）。

**実装**: `cap_semantic_query`（`src/pipeline/serendipity_query.py`）が `structure + pseudo_abstract` を **1,200 字で文末切り詰め**、`build_semantic_query` が常に通す。それでも 400 が返った場合は **600 字に縮めて1回だけ再取得**し、成功したら facet の `status` に「400 のためクエリを短縮して再取得・F-16」と残す（黙って直さない）。

**実測（実 API・9/04 の長さ条件を再現）**: 旧経路が送っていた **2,175 字 → HTTP 400**。切り詰め後 **1,118 字**。本番経路は **返却 50 → 候補 42 件**で、中身も主題側（*The Importance of Knowing When to Stop* / *A GENERALIZED DROP-THE-LOSER URN FOR CLINICAL TRIALS* / *A modified sequential recognition machine using time-varying stopping boundaries*）。

**★9/04 にいちばん害があった診断文も直した**: 0 件のときのメッセージは「facet が近すぎる」「テーマが飽和している可能性」の2つしか名指しせず、呼び手は**生きているテーマを飽和と結論しかけた**。新しい文面は **status を読む順序**を指示し、`400 →クエリ長`／`429 →共有クォータ（F-28）`／`504 →一過性・単独再投`／**`home_converged` になって初めて「近すぎる・飽和」を疑う**と書く。

**残る限界（正直に）**: **504 は本日も出た**（同じ probe の1回目）。リトライ回数は増やしていない（F-11 の据え置き判断を尊重）ので、**単独再投の規約（S-112）は引き続き有効**。

---

### F-28-R. 全 by\* 共通 — 枯れた 429 を**「共有クォータ枯渇」と名指し**する — **対処済み 2026-09-12（診断のみ）**

**資源競合そのものは直していない。** 直したのは「呼び手が 429 を候補やテーマの性質と読まないようにする」ところだけ。リトライを使い切った 429 は、従来 `request failed after 3 attempts: HTTP Error 429` という汎用文で返っていた。現在は **「この IP の共有クォータ枯渇＝クエリや候補の問題ではない。by\* は同一クォータを共有するので直前に別の by\* を回していると起きやすい（F-28）。時間をおいて再実行」**と返す。seihai が contra を介さず直接叩いても 429 だった切り分けが、そのまま文面の根拠になっている。

**未対処**: (c) by\* 横断のトークンバケット／残量申告は**実装していない**（複数プロセス・複数セッションをまたぐ資源なので、contra 内の状態では正しく数えられない）。**呼び手側の規約（bybridge を先に回す）が当面の処方**であり、これは seihai 側の判断事項。

---

### F-27-R. bybridge — semantic レッグの off-Field 結果を**捨てずに後方へ回す** — **対処済み 2026-09-12**

**起票の翌日に処置した**（F-13-S の計器で初めて見えた段なので、計器→処方が直接つながった例）。

**捨てられていたものの実体（実 API・3フィクスチャ・home=`Economics, Econometrics and Finance`）**:

| フィクスチャ | 選別なしの供給 | Field 選別が残す数 | **落ちていたものの中身** |
|---|---|---|---|
| trade_collision | 30 | 6 | **heavy-tail の rare-event simulation / importance sampling 群**（Decision Sciences 13・Mathematics 7）＝テーマ（稀な大勝ちの枝を刈ると期待値が落ちる）の方法論そのもの |
| strategy_generation | 42 | **0** | **GP の bloat・過剰適合・behavioural diversity**（Computer Science 31）＝テーマ（GP 生成候補の相関と多様性）の正典 |
| retrigger_hysteresis | 28 | 14 | 共分散ペナルティによる backtest overfitting 回避 / MCS とモデル選択 / 執行の RL |

⇒ **「Field が外れている」と「主題から外れている」は無関係**であることが、3本とも同じ向きで出た。OpenAlex は手法寄りの仕事を CS / Decision Sciences / Mathematics に置く。

**何を変えたか**（加算的・可逆）: `collect_seeds_semantic_report(..., keep_offfield=True)`＝**home Field の結果を先頭に、off-Field をその後ろに並べる**（ハード除外をやめる）。名簿に何件入るかは従来どおり公平配分マージが決める（semantic レッグは名簿の半分が上限）。MCP に `seed_semantic_keep_offfield`（既定 true・false で旧挙動）を追加。診断の内訳行は `home Field 外 42（捨てずに後方へ・F-27）` と書き分け、取得構成行に **「名簿に残った semantic 由来 N 件＝このレッグは分野ラベルではなく主題の近さで選ばれるので Field 一致率の分母に入れて読まないこと」** を併記した。

**エンドツーエンド実測（MCP ハンドラ・同一テーマ・`seed_semantic_keep_offfield` だけ切替）**

*strategy_generation（Field ラベルが主題とずれているケース）*

| | before（ハード除外） | after（後方へ回す） |
|---|---|---|
| semantic 供給 | **0** | **42**（名簿に 11 件着席） |
| 名簿の Field 分布 | Economics 20 | Computer Science 11 / Economics 9 |
| 交差候補の除外根拠（`dominant_field_ids`） | `['20']` | **`['17','20']`＝CS も home として除外側に回る** |
| 最頻 bridge | An Evolutionary Theory of Economic Change（21,727） | **Koza『Genetic Programming』（13,275）＝主題の正典** |
| 交差候補の上位 | Absorptive Capacity / Dynamic capabilities / Toward a knowledge-based theory of the firm …＝**イノベーション経営論で全滅** | **GP を別領域で使った文献群**（ゲノム比較ベンチマーク Bowtie2 / 流量予測 / コンクリート強度 / 2-D strip packing / 高速道路の事故予測 / 降水ダウンスケーリング / 飽和透水係数 / 降雨流出モデルの同定） |

⇒ **9週間で初めて、bybridge の交差候補が「同じ手法を別分野で回している集団」になった。** 呼び手のテーマ（GP 探索の過剰適合と多様性維持）に対して、水文学や材料が同じ手法で踏んだ問題は転用の素材になりうる。**ただしこれは「構造類推」ではなく「同一手法の他分野適用」なので、収穫として使えるかの判定は seihai 側の実運用に委ねる。**

*retrigger_hysteresis（Field ラベルが元から主題と合っているケース）*: 名簿は Economics 18 / Mathematics 1 / Decision Sciences 1 と**ほぼ不変**、除外根拠も `['20']` のまま、交差候補の上位も首位から5件までほぼ同一（Deflated Sharpe / オプション価格 / CSR …）。⇒ **Field ラベルが既に正しいテーマでは、この変更はほとんど何もしない**（保守的）。

**効かなかった／注意すべきこと（黙って消さない）**:

- **bridge 集中度は悪化した**（strategy_generation: 最頻 bridge 全体 35% → 42%・**上位10件 80% → 100%**）。名簿が主題側に寄ると、主題の正典（Koza）を全員が引くので当然こうなる。**F-25 のとおり上位窓占有率は単独で読めない**指標であり、**中身は上記のとおり別物になっている**。とはいえ 100% は高いので、次の担当は F-25（`diversify_head_by_bridge` の枠が複数 bridge 引用で効かない）をこの条件で再測すること。
- **Subfield 一致率の計器は下がる**（10% → 5% / 70% → 65%）。名簿に CS が入れば Finance 一致率は当然下がるので、**この数値だけで退行と読まない**ための注記を取得構成行に入れた。**計器そのものを「レッグ別の一致率」に作り直すのは未実施**＝残件。
- **F-22 / F-15 / 交差候補の採点側は一切触っていない。**

**seihai 側への申し送り**: (1) bybridge の名簿に**ホーム分野外のシードが混ざるようになった**——これは仕様変更であって故障ではない。(2) その結果 `dominant_field_ids` が変わるので、**交差候補の除外境界も動く**（strategy_generation 系では CS も除外側に回った）。(3) 診断の Subfield 一致率が下がって見えるが、semantic 由来の件数が併記されるのでそこを読んでほしい。(4) **S-26/S-67 の裁定材料としては、9/11 の「交差しない」に対する直接の反証が初めて出た**（同じテーマ族で交差候補が別分野になった）。裁定は人間の判断。

### F-21-R. 全 by\* 共通 — 自己記述を**検証器の定数から生成**し、拒否メッセージに許容値を載せる — **対処済み 2026-09-12**

**3例の内訳**: (1) 9/05 `approach_type` の説明文が例示した `system-building` を検証器が拒否、(2) 9/11・(3) 9/12 `assumptions` がスキーマ上 optional なのに検証器は 2〜5 件必須。**いずれも呼び手は説明文どおりに書いて落ちており、1回ぶんの往復が失われている。**

**本日さらに2件の同型を発見（実装中に露見）**: (4) `theme_overview` の説明文は「3-6 sentences」だが検証器は **200〜1200 字**を強制する（本日の検証中に実際に踏んだ）、(5) `scope_scale` は説明文が「Scale of study.」だけで、許容値 `small/large/theoretical` がどこにも書かれていない。

**何を変えたか**（加算的・シグネチャ非変更）:

1. **スキーマを検証器の定数から生成**（`src/mcp_server.py` の `_approach_type_prop` / `_scale_prop` / `_time_range_prop` / `_assumptions_prop` / `_overview_prop` / `_keywords_prop`）。`APPROACH_TYPES` 等を import して `enum` / `minItems` / `maxItems` / `minLength` / `maxLength` を出すので、**MCP クライアント側が呼び出し前に弾ける**し、両者が二度と乖離しない。`_enum_prop` は既定値が許容集合に入っていることを assert する。
2. **`assumptions` を4ツールすべての `required` に入れた**（検証器が必須なので、スキーマがそう名乗る）。
3. **拒否メッセージが許容集合と拒否された値を載せる**（`src/core/input_schema.py` の `_reject`）。`approach_type is invalid` → `approach_type is invalid: 'system-building' (allowed: application, experiment, theory)`。`assumptions` は `(got 0)`、`theme_overview` は `(got N)` を併記。
4. `bynote_link_concepts` の `theme_overview` は**任意の背景テーマ**なので長さ制約を付けていない（検証器も通さない）。**同じ名前でも契約が違う箇所を巻き込まない。**

**乖離防止のテスト（これが本体）**: `tests/test_input_schema.py` に4件——(a) 各ツールの `enum` が検証器の集合と**厳密一致**、(b) **スキーマが提示する全値の直積が実際に検証を通る**（＝「提示したのに拒否される」を構造的に不可能にする）、(c) `assumptions` が全スキーマで required かつ境界が定数と一致、(d) 拒否メッセージが許容集合と値を含む。**433 → 437 tests: 437 pass。**

**seihai 側への申し送り**: `approach_type` / `scope_scale` / `scope_time_range` が `enum` になったので、MCP クライアントが不正値を送れなくなる。`assumptions` は**必須として明示**された（従来も実質必須だった）。**呼び出し側の書き換えは不要**——今まで通っていた呼び出しはすべてそのまま通る。

---

### F-22-I. delegate_finalize — 落選内訳に**律速の因子**を書き、「近いが有用」を名指しする — **対処済み 2026-09-12（計器のみ）**

**直したのはスコアではなく読み取り。** F-22 は「セレンディピティ＝距離 × 構造」という**定義の帰結**なので、閾値を動かしても近い当たりは通らない。**変えたのは、落選が「遠くないから」なのか「浅いから」なのかを呼び手が読めるようにした点だけ。**

**実装**: (1) `_serendipity_cause`（`src/pipeline/classify.py`）が `purpose_sim` / `mechanism_dist` と**律速側**（小さい方の因子）を返し、`purpose_sim ≥ 0.70` かつ距離が律速なら `near_but_useful` を立てる。(2) `_record_rejections` が任意の `detail_of` を受け取り、セレンディピティ系3フロア（percentile_gate / output_floor / not_selected）の落選行に併記する。(3) `src/mcp_server.py` が各行に `（構造 purpose_sim 0.89 × 距離 mechanism_dist 0.5 ＝ 律速は距離）` を描画し、該当があれば **★『近いが有用』N 件**の節で名指しして「どの閾値でも通らない／採否の最終判断は呼び手にある」と明記する。

**閾値 0.70 の較正（きりの良い数ではなく実データで決めた）**: 9/12 の6件バッチと 9/05 の CUPED を再生して実測。

| 候補（seihai の評価） | purpose_sim | mechanism_dist | 積 | 新ラベル |
|---|---|---|---|---|
| Futility stopping in clinical trials（**今週の中心的収穫**） | 0.83 | 0.505 | 0.419 | **近いが有用** |
| Quantifying the bias …（**自分の処方への反証装置**） | 0.89 | 0.50 | 0.445 | **近いが有用** |
| Informing the selection of futility thresholds | 0.78 | 0.49 | 0.382 | **近いが有用** |
| Optimal Giving-Up Times and the MVT | 0.66 | 0.827 | 0.546 | （律速は構造） |
| Foraging as an evidence accumulation process | 0.64 | 0.881 | 0.564 | （律速は構造） |
| CUPED（9/05・呼び手が最有用と判断） | 0.89 | 0.43 | 0.383 | **近いが有用** |

⇒ **呼び手が「実務的に最も効いた」と書いた候補だけにラベルが付き、遠いが浅い2件には付かない。** 0.70 は 0.66/0.64（付けたくない側）と 0.78〜0.89（付けたい側）の帯の間にある。

**効かなかった／やらなかったこと**: (a) **スコア定義は変えていない**——`utility` 系の第2順序（purpose_sim 主導）の併記は F-22 の処方 (a) として残す。(b) 閾値の調整もしていない（F-15 の相対性問題とは別物で、動かしても解けないため）。(c) **落選を出力に昇格させることもしていない**——post-gate は決定論的検証であって採否の最終権限ではない（案X の分担）ので、判断材料だけを増やした。

**検証**: 新規回帰3件（落選行が両因子と律速を持つ／低 purpose の候補にラベルが付かない境界 0.69 対 0.70／MCP 経路の出力に名指しが出る）。**437 → 440 tests: 440 pass。**

**seihai 側への申し送り**: 落選内訳に『近いが有用』が出たら、**それは「捨てろ」ではなく「スコアの定義上は通らないが、採否は呼び手が決める」の合図**。9/05 に CUPED を落選のまま採用した判断がそのまま正しい。

### F-13-S. bybridge — semantic シードレッグの**供給 0 件の真因＝日本語のテーマ本文を埋め込み検索に入れていた** — **対処済み 2026-09-11**

**経緯**: `semantic レッグ供給 0 件` は **8/31・9/04・9/09（F-26）・9/10・9/11 の5回**観測されていた。9/04 の計器改修（F-13-I）で「0 なら未検証」と明示はされたが、**なぜ 0 なのかは診断にも出ず**（失敗は MCP サーバの stdout に `print` されるだけ）、呼び手は「取得失敗、または全件が field 選別で落ちた」という2択の推測しか受け取れなかった。

**機序（実 API・同梱フィクスチャ3本・home=`Economics, Econometrics and Finance`＝seihai 9/11 と同じ条件）**:

1. **言語が意味より先に効く。** seihai のテーマ本文は日本語で、semantic レッグはそれをそのまま `search.semantic` に投げていた。返る 50 件は **ja 24/50・46/49・45/50**＝日本語文献が近傍を占める（F-16 の byserendipity 言語崩壊と同じ現象が bybridge 側でも起きていた）。日本語レコードは abstract 無し・Field 未分類/分野外が多く、**abstract 選別（−9/−42）と home Field 選別（−41/−8）で全滅→供給 0**。旧経路の関数を直接叩いて **4/4 回で 0** を再現した。
2. **504 が重なる。** 日本語本文（327〜501 字＝URL で 3〜4KB）は `max_retries=0` で **3/3 が 504**、本番の3回リトライでも 6 回中 1 回は抜けられなかった。ただし 504 は短い英語 keywords（92 字）でも出たので**長さ依存ではなく一過性**と判定。
3. **長さの硬い上限**: 英語 **1,575 字で HTTP 400、1,437 字は成功**。テーマ本文は最大 1,200 字＋goal＋why なので、英語テーマでも上限を越えうる。

**何を変えたか**（加算的・可逆・MCP シグネチャは任意引数の追加のみ）:

- `semantic_seed_query_text`（`src/pipeline/collect.py`）: 検索文の選択順を **呼び手の `seed_semantic_text` ＞ テーマ本文（ラテン文字が 7 割以上なら）＞ 英語の `keywords_include`（本文が非英語のとき）＞ 本文（キーワードも無いとき）**にした。1,200 字で文末切り詰め。
- `collect_seeds_semantic_report`: 同じ取得に**段ごとの内訳**（検索文の出所・字数・返却件数・言語構成・abstract 無しで落ちた数・home Field 外で落ちた数・供給数・取得失敗の理由）を付けて返す。旧 `collect_seeds_semantic` はこれの薄い包みとして残した。
- MCP `bybridge_collect` に任意引数 **`seed_semantic_text`**（英語の擬似アブストラクト約 80 語・byserendipity の `facets[].pseudo_abstract` と同種）を追加。`seed_semantic` の説明文に「非英語本文は英語 keywords に差し替える」を明記（F-21 の教訓＝説明文と挙動を一致させる）。
- 診断に **`semantic レッグ内訳`** 行を追加（`render_semantic_leg`）。例: `検索文=英語 keywords_include（テーマ本文が非英語のため差し替え）・80 字・テーマ本文の非ラテン文字率 86% → 返却 50（言語 en 49 / de 1） → abstract 無し −22 → home Field 外 −14 → 供給 14`。取得失敗なら理由（504 等）を名指しする。0 件警告の文言も「内訳行を見よ」に変更。

**before / after 実測（semantic レッグ単体・同一セッション・実 API）**:

| フィクスチャ | before（日本語本文） | after 既定（英語 keywords） | after（`seed_semantic_text`） |
|---|---|---|---|
| trade_collision | **0**（ja 24・Field 外 −41） | **6**（en 50/50） | 7 |
| strategy_generation | **0**（504×3） | **0**（en 49/49・**Field 外 −42**） | 1 |
| retrigger_hysteresis | **0**（ja 45・abstract 無し −42） | **14**（en 49/50） | 0（Field 外 −37） |

**エンドツーエンド（MCP ハンドラ経由・retrigger_hysteresis・同 Field）**: `seed_semantic:false`（＝旧状態の供給 0 と等価）→ 既定、で比較。
- 名簿: before には *An optimal hybrid framework for carbon price prediction* / *Crude Oil Shocks and Saudi Stock Returns* / *monetary policy regime switches … term structure models* / *Role of Fintech in the growth of Indian stock broking* が入っていた。after は semantic 由来の **A Bayesian Approach to Measurement of Backtest Overfitting / Hysteresis in Price Efficiency and the Economics of Slow-Moving Capital / Correctness of backtest engines / Ultra-High-Frequency Algorithmic Arbitrage** が入り、**Subfield 一致 65% → 70%**。
- bridge 集中度: 最頻 bridge 25% → 17%（上位10件 30% → 30%）・通行 bridge 27 → 19 本・平均共有 bridge 2.08 → 1.88。
- **交差候補の上位はほぼ不変**（両方とも首位 *The Deflated Sharpe Ratio*、以下オプション価格・CSR・ESG などファイナンス内）。

**効かなかった／注意すべきこと（黙って消さない）**:

- **下流の「交差しない」問題（9/11 の F-13 7回目）は直っていない。** 本日の変更はシード段の供給の是正で、交差候補は依然ホームドメイン（ファイナンス）内に留まる。
- **英語擬似アブストラクト（`seed_semantic_text`）は keywords より良いとは限らなかった**（7/1/0 対 6/0/14）。どちらも**2つ目の律速＝home Field 選別**で大半が落ちている（**F-27 として起票**）。英語化は必要条件であって十分条件ではない。
- 英語 keywords で埋め込み検索すると、語彙レッグと同じ語を使うので**語彙衝突の対抗馬としての独立性は弱い**（本文の主語が入らない）。呼び手が英語の擬似アブストラクトを渡すのが本来の形で、keywords は床。
- 504 は一過性で今回も出た。**リトライ回数を増やす処置はしていない**（F-11 の据え置き判断を尊重）。

**検証**: 新規回帰 8 件（非英語本文→英語 keywords への差し替え、`seed_semantic_text` の優先、英語本文は従来どおり、キーワード無しの非英語本文で ⚠、1,200 字の文末切り詰め、段ごとの内訳の数え上げ、504 の名指し、MCP スキーマに任意引数として出ること）。既存の MCP 経路テストのスタブ名を新関数へ更新。**425 → 433 tests: 433 pass。** probe は `scripts/` に入れず scratchpad で実行。

**seihai 側への申し送り（人間判断事項）**: (1) 次回の bybridge run から診断に **`semantic レッグ内訳`** 行が出る。**0 件のときはこの行の「どの段で何件落ちたか」を insights に記録してほしい**。(2) テーマ本文が日本語のままでも英語 keywords で自動代用されるが、**`seed_semantic_text` に英語の擬似アブストラクト（約80語）を渡すのが推奨**——byserendipity の facet で既にやっている作業と同じ。これは seihai 側 SKILL の呼び出し手順の変更なので**人間の判断**。(3) S-26/S-67 の裁定には影響するが決定打ではない（交差段は未改善）。**contra 側から seihai のコード・SKILL・ドキュメントは一切触っていない。**

---

### F-23/24-R. bybridge — プール集中の機序＝**プール全体を1本の `cites:` フィルタに OR で流していたので、取得が bridge の大きさに比例していた** — **対処済み 2026-09-08**

**F-01-R が「プールに偽物が居た」を直したのに対し、これは「プールは正しいのに取得の仕方が偏っていた」。**

**機序**: 2-hop スキャンは `cites:B1|B2|...|B50` という1本のクエリを投げてページ送りする。**OR 集合からの抽出は、集合の各要素の大きさに比例する。** 被引用 71,804 の `Theory of the firm` と被引用 200 の bridge を OR で並べれば、返るページはほぼ前者の被引用側で埋まる——**候補の 85%（F-24）／62%（F-23）が1本を通り、50本プールのうち実際に通行があったのは 3〜5 本だけ**という観測は、これで過不足なく説明できる。**F-23 で立てた「巨大 bridge は道具（R の引用）である」という仮説は不要**（F-24 の最頻 bridge は経済学の正典＝文献だった）。**共通するのは種別ではなく、単に大きさが取得量に直結していたこと。** そして 2026-08-22 に入れた上位窓の多様化は、**この段が集め終えた後の並べ替え**なので、プールの成り立ちには一切触れていなかった（seihai の 9/08 の要約「上位窓の多様化は上位の見た目を直したが、プールの成り立ちは直していない」は正確）。

**何を変えたか**（加算的・可逆・MCP シグネチャ非変更）:

1. **bridge ごとの取得配分**（`src/pipeline/collect.py` の `collect_citation_candidates`）: プールを OR で1回投げるのをやめ、**bridge を1本ずつ問い合わせ、1本あたり最大 `max_count // 10` 件（既定 60 件なら 6 件）しか供給できない**ようにした。走査順は**プール順**（＝`_bridge_pool_from_seeds` が「複数シードが共有する bridge を先頭に、その後シード間ラウンドロビン」で作った順序＝多様性の保証そのもの）。**F-18 の facet 公平配分（`facet_fair_share`）と同じ思想を bridge 段に適用したもの。**
2. **不足分は従来の一括 OR 走査で補充**（recall の床）。1本の bridge が失敗（HTTP エラー）しても**その bridge を飛ばすだけ**で残りは走る。`CollectConfig(bridge_fair_share=False)` または `per_bridge_cap=0` で旧挙動。
3. **プール順が捨てられていたのを修正**（`src/mcp_server.py`）: 収集段には `bridges=sorted(bridges)`＝**id のアルファベット順**が渡っていた。集合に落として並べ直していたので、上記の多様性順序は取得段に届いていなかった。順序付きリストを渡すよう変更（生存確認で落ちた bridge は順序を保ったまま除去）。**旧実装では実害が出にくかった（どうせ OR で1本にまとめていた）が、配分走査では走査順が結果を決める。**
4. **診断行を1本追加**: 配分の存在と1本あたりの上限を出力に明記（`- bridge 取得配分 (F-23/F-24): …`）。**「集中度 25%」が「OR 1回の結果としての 25%」なのか「6件枠を掛けた上での 25%」なのかは、数値だけでは区別できない**ため。

**before / after 実測**（同一セッション・**同一シード名簿と同一 bridge プール**に対して `bridge_fair_share` だけを切り替えて交互実行。計器は既存の `bridge_concentration`。probe: `scripts/` には入れず scratchpad で実行）:

| 指標 | 新フィクスチャ `theme_seihai_retrigger_hysteresis.json`（F-24 のテーマ族） | 既存フィクスチャ `theme_seihai_strategy_generation.json`（F-01 のテーマ族） |
|---|---|---|
| **最頻 bridge が全候補に占める割合** | 35% → **25%** | 27% → **25%** |
| **実際に通行のあった bridge** | 17 本 → **27 本** | 21 本 → 18 本 |
| **候補あたり平均共有 bridge** | 1.55 → **2.08 本** | **1.00 → 1.85 本** |
| **共有 bridge 1本のみの候補** | 44/60 → **32/60** | **60/60 → 42/60** |
| 上位10件の占有率 | 20% → 30%（**悪化。F-25 として起票**） | 0% → 50%（同上） |
| 2-hop 段の OpenAlex 呼び出し | 2 回 → **11 回** |  |
| after 候補のうち before にも居た件数 | 17/60 | 10/60 |

**中身の before / after（上位10件・こちらが本題）**:

- **F-24 テーマ族**: before の上位10件は `The Pricing of Options` / `Does corporate social responsibility affect the cost of capital?` / `Costs of Equity and Earnings Attributes` / **`Dermatologist-level classification of skin cancer`** / **`Mastering the game of Go without human knowledge`** ＝コーポレートファイナンスの正典と、無関係な深層学習の巨大ハブ。after の首位は **[The Deflated Sharpe Ratio: Correcting for Selection Bias, Backtest Overfitting, and Non-Normality](https://doi.org/10.3905/jpm.2014.40.5.094)**（Bailey & López de Prado 2014）＝**呼び手のテーマ（選抜バイアスと反復試行の過小課金）に正面から当たる1本**で、`Building Diversified Portfolios that Outperform Out of Sample` も入った。
- **F-01 テーマ族（品質多様性・新規性探索）**: before の上位は `Four concepts for resilience` / `Brain Intelligence` / `ICLabel（脳波の独立成分分類器）` / `Autonomous chemical research with LLM` ＝**主題と無関係**。after は `Bayesian Optimization for Quality Diversity Search` / `Automatic modular design of robot swarms based on repertoires of behaviors generated via novelty search` / `Evolutionary Statistical System Based on Novelty Search` / `Efficient Quality Diversity Optimization` ＝**テーマのキーワードそのもの**。⇒ **集中度の数値の動きは小さいが、返る中身は別物になった。**

**効かなかった／注意すべきこと（黙って消さない）**:

- **F-24 の 85% は再現していない。** 本日のフィクスチャは 9/08 の seihai run と同じ*テーマ族*だが同じ*名簿*ではなく、before 値は 35% / 27% だった。⇒ **「85% → 25%」と読んではいけない。実測されたのは「同一名簿・同一プールに対する before/after の差」だけ**である。85% の条件そのものは次回の実運用 run で再測される。
- **上位窓の占有率は悪化した**（20%→30% / 0%→50%）。これは新しい欠陥ではなく、**元からあった `diversify_head_by_bridge` の枠の穴が、平均共有 bridge が 1 を超えたことで表面化した**もの（**F-25** として起票）。上位窓の中身自体は上記のとおり改善しているので、**この数値だけを見て退行と判定しない。**
- **「遠さ」は今回も改善していない。** after の上位は依然としてホームドメイン近傍（ファイナンス／進化計算）が中心である。F-01-R が残した「ホームドメイン除外がこのテーマで効いていない疑い」は**そのまま残っている**——本日の変更は集中の是正であって、異分野性の是正ではない。
- **コストは 2-hop 段で 2 回 → 11 回**の OpenAlex 呼び出し（1本あたり1ページのみ・上限は プール本数）。F-11 の polite pool 据え置きと合わせると、**429 が出やすくなる方向の変更**である。ここは次の担当が実運用の取得診断行で監視すること。

**検証**: 新規回帰5件（巨大ハブ1本＋小 bridge 20本の合成プールで **配分あり＝ハブは 40 件中 4 件しか供給できない／配分なし＝全件ハブ経由**という A/B を固定、走査がプール順であること、round-robin で埋まらないときの OR 補充、1本の bridge が例外を投げても他が走ること）。既存の「プール全体が1本の `cites:` に入る」ことを固定していたテストは `bridge_fair_share=False` の契約として維持。**420 → 425 tests: 425 pass。** 実 API は probe 2 テーマ（各 before/after）＋ `scripts/run_bybridge.py` で end-to-end 2 回。

**seihai 側への申し送り（人間判断事項）**: **S-26 / S-67 の裁定（bybridge の恒久除外）は、この変更の後で測り直してから下してほしい。** 9/08 の「観測者としての結論＝不適合が確定」は**変更前の実装に対する結論**であり、取得段の偏りという単一の機序が是正された。**再開するなら、insights に「候補あたり平均 bridge」「通行のあった bridge 本数」を記録してほしい**（上位10件占有率は F-25 のため単独では読めなくなった）。**contra 側から seihai のコード・SKILL・ドキュメントは一切触っていない。**

---


### F-13-I. bybridge — **計器そのものの故障＝粗い属性1つを緑のスカラで申告していた** — **対処済み 2026-09-04**

**直した対象は名簿ではなく計器。** 9/04 の観測（上記）で、`シード主題整合 (F-13): home分野一致 20/20 = 100%` と申告した名簿の実体が NAFTA・ブロードバンド・COVID 政策トラッカー・Fox News だった。8/28 に入れた field 限定は**設計どおり効いている**（名簿は確かに全件 `Economics, Econometrics and Finance`）。破れていたのは「Field が一致すれば主題も近い」という前提のほうで、この Field は国際貿易・労働経済・開発経済・金融工学を1つに含む。**seihai 側が指摘したとおり、これは contra 自身が S-68 で書いた「情報が無いことを正常値で表すと検出できない故障になる」と同型**であり、しかも `100%` は緑なので**呼び手が名簿を見に行かなくなる**という二次被害を持つ。

**機序の再現（実 API・2026-09-04）**: 9/04 と同じキーワード（`strategy selection` / `complementarity` / `distance` / `correlation`・`scope.field=finance`）でシード段だけを再実行し、**当日の名簿をほぼ完全に再現した**（首位が同じ *The Skill Complementarity of Broadband Internet*）。この run でも **semantic レッグは HTTP 504 で全滅し供給 0 件**——8/31 に観測された「純語彙シードで構成された名簿」の条件がそのまま再現している。

**実装（加算的・可逆・シグネチャ非変更）**:

1. **パーサが Subfield / Topic を保持する**（`src/openalex/parser.py`）。`primary_topic.subfield.{id,display_name}` と `primary_topic.{id,display_name}` を `source_meta` に追加。API 呼び出しは増えない（既に取得済みの payload の読み落とし）。
2. **`seed_domain_alignment` が Field / Subfield / Topic の3層を返す**（`src/pipeline/bridge_diagnostics.py`）。Subfield の一致率が**新しい主指標**で、Topic 分布は常に名前で印字。`semantic_count` も受け取って持ち回る。
3. **`subfield_vocabulary` / `resolve_subfield_ids`（`src/pipeline/query.py`）**: `scope.field` の自由記述を Subfield 名に**厳格照合**（完全一致、または Subfield 名が scope 文の中に語境界で現れる場合のみ）。`finance` → `Finance(2003)`。`medicine` は**解決しない**——`General Medicine` 等に緩く当たると、粗い属性に戻って同じ失明を作り直すため。解決しなければ **判定不能**（S-68）。**照合に使う Subfield 名は、その run が既に取得した metadata（語彙シード＋semantic シード＋名簿）から集める**ので、**API 呼び出しは1回も増えず、固定表が陳腐化することもない**。任意の追加として `scripts/fetch_openalex_subfields.py` が全 252 件を `data/openalex_subfields.json` にキャッシュでき、置けば語彙に併合される（**本日は F-11 の持続 429 で取得できなかったため未同梱**——下記「残る限界 (iv)」）。
4. **緑の単一スカラを出さない描画**（`render_seed_alignment`）。Field の % は「26 分類の最粗レベルで主題適合ではない」という注記と**同じ行**に置き（F-17 で決めた「ラベルは導出元と同じ行に」の再適用）、Subfield 一致・上位トピック・semantic レッグ供給数を必ず併記する。
5. **semantic レッグ供給 0 の明示**（8/31 の seihai の指摘を実装）。0 なら「主題適合は良好でも不良でもなく**未検証**」と書く。

**実測 before/after（同一名簿・実 API 由来の20件）**:

| | 表示 |
|---|---|
| **before**（9/04 に seihai が受け取った行） | `シード主題整合 (F-13): home分野一致 20/20 = 100%（home=finance）・上位分野 Economics, Econometrics and Finance 20` |
| **after** | `分野(Field) 一致 20/20 = 100%　※Field は 26 分類の最粗レベルで、主題適合ではない`<br>`サブフィールド(Subfield) 一致 **1/20 = 5%**（home サブフィールド=Finance）・上位 Economics and Econometrics 16 / General … 3 / Finance 1`<br>`上位トピック **Economic Growth and Productivity 5 / Healthcare Policy and Management 3 / COVID-19 Pandemic Impacts 2 / Global trade and economics 2**（20 件が 12 トピックに分布）`<br>`semantic レッグ供給 **0 件**`<br>`⚠ 分野は一致していてもサブフィールドが外れています（5% < 35%）…`<br>`⚠ semantic レッグの供給が 0 件です…主題適合は未検証です。` |

**閾値の較正（きりの良い数ではなく実測で決めた）**: home サブフィールド = Finance として実 API で3本測った。**ドリフト名簿 1/20 = 5%** ／ 対照「pairs trading・statistical arbitrage・cointegration」**13/20 = 65%** ／ 対照「backtest overfitting・deflated sharpe・cross-validation」**12/17 = 71%**。閾値 **0.35** は失敗側と健全側の帯の間にあり、**0.5 は健全帯に食い込む**ので採らなかった。

**★試して外れた処方（次の担当が同じ道を通らないように残す）**:

- **トピック分散（distinct topics / 最頻トピック占有率）を判別指標にする** → **却下**。実測: ドリフト名簿 12 トピック（最頻 25%）／対照 A 6 トピック（55%）／対照 B 7 トピック（41%）／別のドリフト混じり名簿 4 トピック（65%）。**健全側と失敗側の範囲が重なる**ので、どこに閾値を置いても嘘になる。集中している名簿が正しいとは限らない。
- **OpenAlex の `/text/topics` エンドポイントにテーマ文を投げて「期待トピック」を出し、名簿と突き合わせる** → **却下**。(i) **日本語のテーマ本文で HTTP 500**（seihai のテーマ本文は日本語）。(ii) 英語のキーワード列だけを投げると分類が不安定で、`strategy selection complementarity distance correlation` は **Chemistry and Stereochemistry Studies (0.83)** を返した。(iii) 決定的に悪いのは、8/24 の衝突語（`finance trade trading strategy`）に対して **Global Trade and Competitiveness** を返すこと＝**捕まえたいドリフトそのものを「期待」として承認してしまう**。
- **テーマ本文とシードの意味的距離（埋め込みのコサイン）** → **今回は不採用**。日本語本文の埋め込みには API キーが要り、鍵の新規発行はこのルーティンの範囲外。**semantic レッグの供給数の報告**が、鍵無しで測れる範囲での代替（レッグが 0 なら未検証と明言する）。

**残る限界（正直に記録）**:

- (i) **`scope.field` が Subfield 名に解決しないテーマでは判定不能のまま**。9/03 の `statistical model selection` はこの型（Field すら解決せず `0/20 = 判定不能` になった件と同根）。呼び手が `scope.field` に OpenAlex の Subfield 名（`Finance` / `Artificial Intelligence` / `Statistics and Probability` …）を1語入れると計器が働く。
- (ii) **Subfield 一致率が高くても主題適合は保証されない**。Finance の中にも無関係な主題はある。だから**上位トピックの印字を主たる読み口として設計した**——数値ではなく名前を読ませる。
- (iii) **これは計器であって処方ではない**。名簿を主題に寄せる処方（Subfield 限定でのシード検索）は**未実装**。ただし今回の実測で、その処方が効くかどうかを判定する材料（5% vs 65%/71%）が初めて揃った。
- (iv) **語彙が run 由来なので、宣言した主題の論文が名簿に1件も無い場合は「0%」でなく「判定不能」になる**（最も重いドリフトが最も弱く出る向き）。9/04 の名簿には Finance が1件あったので 5% と出た。`data/openalex_subfields.json`（`scripts/fetch_openalex_subfields.py`）を置けばこの穴は塞がるが、**本日は OpenAlex が 40 分以上 429 を返し続けて取得できなかった**（F-11 の再現）。次の担当がキャッシュを取得して置くだけで効く（コード変更不要）。

**⇒ seihai 側への申し送り**:

1. 次回 bybridge run から診断ブロックの行が変わる。ラベルは **`シード主題整合` → `シード名簿の素性 (F-13)`**（「整合」という語自体が、測っていないものを測ったと名乗っていたため）。insights には **サブフィールド一致率・上位トピック・semantic レッグ供給数**の3つを記録してほしい。
2. **`scope.field` に OpenAlex の Subfield 名を含めると計器が働く**（例: `finance` / `Artificial Intelligence`）。含めない場合は「判定不能」と出る＝**それも正しい表示**であり、緑ではない。
3. **S-67（bybridge の人間裁定）への材料**: 「効いていないが、効いていないと表示されない」という状態のうち、**表示側は直った**。次の run で `⚠ サブフィールドが外れています` が出れば、収穫ゼロの原因が上流（シード段）にあることがその場で確定する。**裁定の前に1回、この新しい診断付きの run を取ると材料が揃う**（判断は人間）。

**関連コミット**: 本エントリのコミット（`contra failure remediation 2026-09-04`）。テスト: `tests/test_seed_field_scope.py` に回帰8件追加・旧契約の1件を置換（413 → 420 tests: 420 pass）。

---

### F-17-R. byrepo — `関係度` ラベルの正体＝**Reliability（品質）の帯が「テーマとの関係度」の名で描画されていた** — **対処済み 2026-09-01**

**8/29 の精緻化がそのまま正しかった**: ラベルは係数と「逆転」していたのではなく、**係数と無関係な別の量**だった。`build_track_a_entries`（`src/pipeline/delegate.py`）は `relationship_level` を `_reliability_level(reliability_score)`＝**70 以上で高／40 以上で中／未満で低**から作っており、theme 関連度はどこにも入っていない。F-03（8/21）が順位に関連度を入れたとき、**ラベル生成側の姉妹経路が数え上げられなかった**——seihai の推測どおり。

**8/27 の3件が計算で完全に再現する**（順位スコア = Reliability × (0.35 + 0.65 × 関連度)）:

| リポジトリ | theme関連度 | 順位スコア | 逆算 Reliability | 旧ラベルの正体 |
|---|---|---|---|---|
| frouros | 1.0 | 62.0 | **62** → `_reliability_level` = **中** | 品質が「中」 |
| Kats | 0.33 | 51.0 | 51.0 / 0.5645 ≈ **90** → **高** | 品質が「高」 |
| security-investigator | 0.33 | 47.0 | 47.0 / 0.5645 ≈ **83** → **高** | 品質が「高」 |

⇒ 「唯一有用なものが中、無関係なものが高」は、**品質の帯を関係度の名で読まされた**結果である。順位は F-03 で正しく frouros を1位に置いていたので、壊れていたのは表示層だけ、という 8/27 の読みも正しい。

**実装（加算的・可逆。既存の順位・数値・シグネチャ非変更）**:
1. **`関係度` を theme 関連度の帯にする**（`src/pipeline/track_a.py` に `anchor_relevance()` / `relevance_level()` を新設し、`build_track_a_entries` がそれを使う）。帯は **高 ≥ 0.67／中 ≥ 0.35／低 < 0.35**。閾値は実バグで較正した——0.33（キーワード3語中1語ヒット）が **低** に落ちること、下限 0.35 が既存の「theme 関連度が全アンカーで低い」警告（`mcp_server.py`）と同じ値であることの2点。
2. **ラベルと導出元の数値を同じ行に描画**（`src/core/output_spec.py`）: `- **関係度**: 高（theme関連度 1.0）`。読み手が「高」の隣に 0.33 を見て矛盾に気づけなかった 8/27 の状況を、行を分けないことで構造的に潰す。
3. Reliability の帯は捨てない——`Reliability Score: 62/100` の行が従来どおり別名で残る。`_reliability_level` 関数は残置（docstring で「関係度ではない」と明記）。

**実測 before/after**（`scripts/f17_label_probe.py`＝8/27 の3件を reliability/fit を逆算して再演・実 API 不要。順位スコアと theme関連度は seihai の記録値 62.0/1.0・51.0/0.33・47.0/0.33 と一致）:

| # | リポジトリ | before（`78fb9f0`） | after |
|---|---|---|---|
| 1 | frouros | **関係度: 中** | **関係度: 高（theme関連度 1.0）** |
| 2 | Kats | **関係度: 高** | **関係度: 低（theme関連度 0.33）** |
| 3 | security-investigator | **関係度: 高** | **関係度: 低（theme関連度 0.33）** |

before 側は seihai の 8/27 の表を**文言まで再現**した。順位（1→2→3）と順位スコアは両側で不変。

**検証**: 新規回帰3件（`tests/test_anchor_rank.py`＝8/27 実例・帯境界・描画行）。**3件すべてが `78fb9f0` に対して実際に落ちることを確認**（旧契約を更新した既存テスト1件も含め 4 failed）。既存テスト1件（`test_build_track_a_entries_ranks_by_reliability`）は「ラベル＝品質帯」という旧契約を固定していたので新契約に更新（fit 無し→関連度 0→低）。全体 **410 → 413 tests: 413 pass**。**GitHub トークンが無いため実 API のエンドツーエンドは未実施**（ラベル生成は決定論の表示層なので、実測は offline 再演で足りると判断）。

**残る限界（正直に記録）**: これで `関係度` は係数を**正しく**映すようになったが、**係数そのものが「語の一致」であって「問いへの適合」ではない**（F-14 の 8/31 観測＝汎用トレーディングフレームワーク4本が全件 1.0）。したがって 8/31 型の run では**4件すべてが「高」と表示される**。それは F-17 の再発ではなく F-13/F-14 の未解決部分であり、ラベルは今後その欠陥を**隠さず見せる**。

**⇒ seihai 側への申し送り**: `structured:true` 経路の `関係度` は本日から theme 関連度の帯（高 ≥0.67／中 ≥0.35／低 <0.35）で、同じ行に係数が付く。**8/27 に決めた「3件すべて自分で読み直す」運用は維持されたい**（係数が語の一致である限り、高＝問いに答えている、ではない）。

**教訓（一般形）**: **ある量に順位を効かせる修正をしたら、同じ量を名乗るラベルの生成経路を必ず数え上げる**。F-03 は順位を直してラベルを直さず、ラベルは6日間「関係度」の名で品質を語り続けた。seihai 台帳の「姉妹経路の網羅チェック」と同型。

---

### F-19-R. delegate_finalize — 接地失敗の**「不一致」と「照合不能」を分け、材料欠落との因果を同じ文に書く** — **対処済み 2026-09-01**

**★2026-09-03: 処置後はじめて同じ失敗を実地で踏み、診断が実際に働いた（陽性の観測）。** seihai の r04 担当日に呼び手（Claude）が6件すべて `title`/`abstract`/`year`/`venue`/`cited_by_count` を落として送信 → 出力は**候補ごとに「材料欄が欠けたまま送信されています」＋「照合不能: …引用の誤りではなく材料欄の欠落が原因です」**を並べ、**そのまま全欄 echo して同じ引用で再投したところ接地検証は全件成立**（落選理由は `percentile_gate` のみになった）。⇒ **1回の往復で自己修復できた**＝処置前なら「引用が間違っている」と誤診して散文を書き直していた場面。呼び手側の再発防止は「材料は加工せず全欄 echo し、採点フィールドだけを足す」。

**seihai の診断はそのまま正しかった**（原因は呼び手の部分 echo）。したがって直す対象は**照合ロジックではなく計器**である——`5/5 失敗` を見た呼び手が**自分の引用を疑う方向へ誘導された**こと自体が欠陥だった。

**機序（コードで確認）**: `verify_grounding`（`src/pipeline/delegate.py`）は `source_quote` を `material["title"] + material["abstract"]` に対してのみ照合する。呼び手が両欄を送らなければ**干し草の山そのものが空**になり、正しい針でも `not in ""` が真になって「存在しない（逐語一致が必要）」を返していた。**空の干し草に対する不一致は不一致ではない**。しかも材料欠落の警告（`echo_completeness_warnings`）は別ブロックで「該当欄は空のまま描画されます」＝**体裁の問題として**出ており、両者の因果はどこにも書かれていなかった。

**実装（加算的・可逆。既存シグネチャ非変更）**:
1. **`照合不能` を `不一致` から分離**（`verify_grounding`）。`title`/`abstract` がどちらも空なら「照合不能: 候補の title/abstract が送られていないため source_quote を照合できません（**引用の誤りではなく材料欄の欠落が原因です**。…全欄 echo して、同じ引用のまま再投してください）」を返す。テーマ側にも同型の分岐を置いた（提出テーマ本文が空のとき）。
2. **部分 echo（title あり・abstract なし）は不一致のまま、原因候補を併記**。`※ ただし abstract が送られていません——abstract からの引用であればこれが原因です`。**全欄が揃っているときの不一致は従来どおり無条件で「存在しない」**（実際の捏造を甘くしない・回帰テストで固定）。
3. **材料欠落の警告に接地への波及を書く**（`echo_completeness_warnings`）。欠落に `title`/`abstract` が含まれるときだけ `★ この欠落は接地検証も不能にします（source_quote は送られた title/abstract に対してのみ照合されるため、正しい引用でも失敗します）` を付す。
4. **2つのブロックの因果を出力側でも結ぶ**（`src/mcp_server.py`）。`照合不能` が1件でもあれば接地失敗ブロックの見出しに `※「照合不能」は引用の誤りではありません。上の「材料欄が欠けたまま送信されています」が直接の原因です（照合は contra が保持する原本ではなく、呼び手が送った材料に対して走ります）` を挿入。判定は `has_unverifiable_failure()`。
5. **併記の書式バグを1件修正**: `履歴記録 N 件` が `extra`（欠落警告・落選内訳・接地失敗の複数行）の**後ろ**に連結されていたため、**最後の箇条書きの尻に貼り付いていた**（`…再投してください） / 履歴記録 1 件`）。カウンタは見出し行、箇条書きはその下、という順に直した。

**実測 before/after**（`scripts/f19_grounding_message_probe.py`＝seihai 2026-09-01 の投入を実 API 無しで再演。同一候補・同一引用・同一スコア）:

| 投入 | before（`875dd1d`） | after |
|---|---|---|
| (1) `id` ＋採点欄のみ | `- W…: source_quote が候補の title/abstract に存在しない（**逐語一致が必要**）` ＝ seihai が受け取った文言をそのまま再現 | `- W…: **照合不能**: 候補の title/abstract が送られていないため照合できません（**引用の誤りではなく材料欄の欠落が原因です**…）` ＋ 見出しに因果の ※ 行 ＋ 欠落警告に ★ 行 |
| (2) 全欄 echo | 接地成立（失敗ブロック無し） | 同左（**非退行**） |
| (3) title のみ echo・`abstract` 欠落（新規に検査した形） | `…存在しない（逐語一致が必要）` のみ | 同文＋`※ ただし abstract が送られていません——abstract からの引用であればこれが原因です` |

**検証**: 新規回帰テスト6件（`tests/test_grounding_contract.py` ＋ `tests/test_delegate_observability.py`）。うち**4件は `875dd1d` に対して実際に落ちることを確認**（残る2件は「全欄揃った不一致を甘くしない」「`year` だけの欠落に接地注記を付けない」という非退行ガードなので両方で通るのが正しい）。全体 **404 → 410 tests: 410 pass**。

**⇒ seihai 側への申し送り**: 本日決めた運用（raw 材料はプログラムで抜き出してそのまま echo・S-90）は**そのまま維持されたい**（処方が正しい以上、計器を直しても踏まないほうがよい）。変わるのは**踏んだときの復帰速度**で、次に材料欄を落としたときは出力が「引用ではなく echo を疑え」と最初に言う。**S-90 に「contra 側は 2026-09-01 に計器を修正済み」と1行付記できる**。

**教訓（一般形）**: **呼び手のミスを検出したメッセージは、呼び手が次に取るべき行動を名指ししない限り、誤った行動を誘発する**。「存在しない」は事実として正しいが、**呼び手が持っている仮説の中で最も高くつくもの（＝自分の散文が捏造だった）を最初に選ばせる**。F-18-R の「情報が無いことを中立値で表すな」（S-68）の姉妹形＝**原因が分かっている失敗を、原因の分からない失敗と同じ文言で報告するな**。

---

### F-18. 再現（2026-09-01・`home_converged` による facet 消失）

**3枚の facet のうち1枚（Far＝「ソフトウェアテストと変異解析」）が `home_converged` で棄却され 0 件**になった。返却内訳＝[1] 進化計算 50→10、**[2] ソフトウェアテスト＝棄却**、[3] 実験進化学 50→33。**★8/28（S-73）で指摘した「診断ブロックが『結果への影響なし』と申告する」欠陥は直っている**——本日は `⚠ 収穫0の facet: ...。この距離段は今回の材料に含まれていません（A2 の3距離が実質的に潰れている状態）` と正しく警告し、**単独呼び出しで投げ直す処方まで添えていた**。⇒ **様式としては再発だが、計器としては改善が確認できた**（seihai 側は今回は投げ直さず、3距離の穴を承知のうえで採点した＝残る2距離が近傍・最遠を押さえていたため）。

**残る課題**: `home_converged` は「ホームドメインに収束した」という判定だが、**ソフトウェアテストは home=Computer Science の内側にある**ので、A2 の距離設計上は正しい Far でも機械的にホーム扱いされる。⇒ **home 判定が分野（field）粒度なので、同分野内の遠い距離段を表現できない**（F-13 の「分野スコープは主題スコープではない」と同じ根）。

---

### F-18-R. byserendipity — 最遠 facet の収穫 0 の真因＝**3枚目の facet が検索されていなかった**（上限到達で break） — **対処済み 2026-08-28**

**seihai の2仮説はどちらも外れ**（それ自体が成果として記録する価値がある）。(a)「OpenAlex が類似度で足切りしている」でも (b)「ホームドメイン除外が巻き込んだ」でもなく、**故障は contra 側の収集ループ**にあった。

**真の機序**（`_collect_track_b_semantic` / `src/pipeline/collect.py`）: facet を**順番に**引き、各 facet の候補を連結し、`if len(works) >= max_count: break` で打ち切っていた。semantic エンドポイントは1リクエストで最大 50件を返し、上限は 60件。⇒ **facet 1（〜50件）+ facet 2（〜10件）で 60 枠が埋まり、facet 3 は検索リクエスト自体が発行されない。** A2 距離プロトコルは facet を Near → Far → **Very Far** の順に並べるよう指示しているので、**構造的に必ず最遠の1枚が餓死する**——プロトコルが買おうとしていた当のものが、実装の都合で毎回捨てられていた。

**seihai の観測がそのまま署名になっていた**: 「**facet は3枚なのにリクエストは2件**」（8/28 の取得診断）は取りこぼしでも通信失敗でもなく、**3枚目を呼んでいないという事実の直接の表示**だった。「単独で投げ直すと 42件返る」も同じ事実の裏面（単独なら上限に達する前に必ず引かれる）。

**実装**（加算的・可逆）:
1. **全 facet を必ず検索し、上限を facet 間でラウンドロビン配分する**（`_interleave_facet_buckets`）。件数の少ない facet は未使用枠を他へ譲るので、**候補総数は減らない**（実測でも 60 のまま）。`CollectConfig(facet_fair_share=False)` で旧挙動に戻せる。
2. **facet 別内訳を診断ブロックに常時出す**（`stats_out` → `_facet_breakdown_line`）: 各 facet の `返却 / ホーム除外・重複後 / 提出` と、**収穫0の facet を名指しする警告**。取得失敗・棄却も facet 名つきで出る。⇒ seihai の処方 (1) をそのまま実装。
3. **`run_stats_caveat` の「結果への影響なし」を撤回**: この行は HTTP 応答しか見ておらず、段や facet ごとの欠損を保証しない旨を明記して facet 別内訳へ誘導。⇒ seihai の処方 (2)（S-68 と同型の「情報が無いことを中立値で表すな」）。

**実測 before/after**（8/28 の r05/F9 と同一の3 facet ＝ 臨床非劣性 / 群集生態学の限定類似性 / SE の等価変異体・実 API・同一セッションで A/B）:

| | facet1 提出 | facet2 提出 | **facet3（Very Far）提出** | OpenAlex リクエスト | 候補総数 |
|---|---|---|---|---|---|
| before（旧挙動） | 42 | 18 | **0** | **2**（3枚目は未発行） | 60 |
| after（本対処） | 20 | 20 | **20** | **3** | 60 |

**8/27・8/28 の seihai 実測（43/17/0・44/16/0）を before 側がほぼそのまま再現**した。after では、**seihai が単独呼び出しでしか引けなかった等価変異体の文献が、3枚同居のまま上位に返る**——`Are mutants a valid substitute for real faults in software testing?` / `The Impact of Equivalent Mutants` / `Automatically detecting equivalent mutants and infeasible paths`（うち1本は 8/28 の delegate_finalize を通過して結論に効いた当の文献）。新規候補は 60件中 **22件が before に無かったもの**。

**MCP 本番経路（`byserendipity raw_only`）でも実機確認**: 3/3 facet 採用・20/20/20・診断ブロックに `★facet 別内訳: [1] … 返却 50 / ホーム除外・重複後 41 / 提出 20 ・ [2] … ・ [3] software engineering (equivalent mutants): 返却 50 / … / 提出 20` が出る。

**⇒ seihai 側への申し送り**: **「Very Far は単独呼び出しで引く」という当面の運用処方は不要になった**（3枚同居で引ける）。ただし単独呼び出しは今後も有効な手段なので、禁止ではなく「必須ではなくなった」。**次回の raw_only run では診断ブロックの `★facet 別内訳` を必ず記録してほしい**——0件の facet が出たらそれは今度こそ検索側の性質であって、contra の打ち切りではない。

**新規テスト** `tests/test_facet_fair_share.py` 8ケース（旧挙動を `facet_fair_share=False` で回帰として固定・公平配分・薄い facet の枠譲渡・単一 facet 不変・失敗 facet の記録・内訳行の文言3件）。全 **390 tests: 390 pass**。

**同型の残件（起票のみ・未対処）**: `_collect_track_b_lexical`（LLM キー経路のフォールバック）にも同じ形の `if len(works) >= max_count: break` がある。こちらは `per_query = max_count // len(queries)` の割当があるため通常は最後のクエリまで届くが、1ページが per_query を超えて返ると同じ餓死が起きうる。**本番の委譲経路ではないので今回は触らず、様式として記録する。**


### F-01-R. bybridge — 巨大ハブ吸着の**真の機序＝存在しない参照 id（phantom bridge）** — **対処済み 2026-08-25**

**7週間「巨大ハブ」と呼んできたものは、ハブですらなかった。**

**特定の経緯**: 本日、8/24 の観測テーマ族を固定化した新フィクスチャ `data/samples/theme_seihai_trade_collision.json` で bybridge を実行し、**旧様式を10回目として再現**した——交差候補 59/60 が最頻 bridge `W4285719527` 1本を通過、上位は "Using thematic analysis in psychology"(190,298) / TAM(66,478) / G\*Power。**ただし今回シード名簿は主題側だった**（CAPM / Tail Risk in Momentum Strategy Returns / Forecasting Crashes / Information Aggregation with Asymmetric Asset Payoffs）。⇒ **F-13（語彙衝突）は F-01 の必要条件ではない**ことがここで確定した（S-26 の run 3/3 が裁定する予定だった問いに、先に答えが出た形）。

次に、F-02 の計器が出していた**「最頻 bridge の名前が解決できず生の id のまま表示される」**という見落としていた兆候を追った。`W4285719527` を OpenAlex に問い合わせると:

| 問い合わせ | 結果 |
|---|---|
| `GET /works/W4285719527` | **HTTP 404** |
| `filter=ids.openalex:W4285719527` | **count 0**（＝作品として存在しない） |
| `filter=cites:W4285719527` | **count 4,906,577**（＝490万件の参考文献リストに載っている） |

**機序**: `referenced_works` は生の参考文献リストである。OpenAlex はレコードを統合・削除するが、**古い id は引用側の参考文献リストに残り続ける**。それは共通の祖先文献ではなく**書誌データの傷跡**であり、しかも巨大になりうる。2-hop スキャンはプール全体を1本の `cites:` フィルタに OR で流し込むので、**この phantom 1本が結果集合を丸ごと飲み込み**、出力は「ホームドメインを除いた OpenAlex 最多被引用作品リスト」に退化する。5週分の観測に出てきた TAM(43,317) / Felsenstein(41,579) / SEM 二段階法(40,027)、そして "AT&T Labs という書誌データの欠損レコード" は、すべてこの退化の表れとして整合する。

**大きさは判別に使えない（中心性ペナルティを再度否定する実測）**: 同じ50本プールの中に**死んだ id は5本**あり、被引用数は **4,906,577 / 4,194 / 2,749 / 554 / 59** とばらばらだった。一方、生きている最大の bridge は **Fama-French(27,948)・GARCH(22,513)・Fama 1970(15,789)＝主題ドメインの正統な基礎文献**である。**degree 上限や中心性ペナルティは、小さい phantom 4本を見逃したうえで本物の祖先文献を罰していた。** 判別軸は大きさではなく**解決可能性**だった。（2026-08-18 に計器が中心性ペナルティを否定した判断は、ここでも正しかった。）

**何を変えたか**（加算的・可逆）:

1. **bridge 生存確認ゲート**（新規 `filter_live_bridges` / `resolve_ids_batched` in `src/pipeline/bridge_diagnostics.py`、配線は `src/mcp_server.py` `_execute_bybridge`）: プールの id を **1回のバッチ照会**（`ids.openalex:` を50件ずつ OR）で解決し、**解決しない id をプールから外す**。除外本数と id を診断ブロックに明示。`bridge_liveness:false` で旧挙動。**seihai 側の処方「採用の前に生存確認」（F-12 のシード側ゲート）を、そのまま1ホップ先へ適用したもの。**
2. **fail-open を二重に**: 通信失敗時は**1本も落とさない**。加えて「全件 dead」という応答も**真実ではなく不良応答として扱い**、落とさない（プールを空にすると F-12 型の沈黙した収穫ゼロになるため）。
3. **`collect_citation_candidates` に `bridges=` 引数を追加**（既定 None＝従来どおり自前でプール構築）。**これが無いと本体が内部でプールを作り直し、除外したはずの phantom が `cites:` フィルタに戻る**——実装中にこの取りこぼしを踏んだので明記しておく。

**before / after 実測**（同一テーマ `theme_seihai_trade_collision.json`・`--raw --seed-count 20`・同一セッション内で `bridge_liveness` を切り替えて交互実行。指標は 2026-08-18 の F-02 計器がそのまま出したもの）:

| 指標 | before | after |
|---|---|---|
| bridge プール | 50 本 | 45 本（**死んだ id 5本を除外**） |
| **最頻 bridge が全候補に占める割合** | **98%** | **35%** |
| 最頻 bridge が上位10件に占める割合 | 90% | **70%**（残件） |
| **実際に通行のあった bridge** | **2 本** | **19 本** |
| 候補あたり平均共有 bridge | 1.00 本 | **1.57 本** |
| 共有 bridge 1本のみの候補 | **60/60 件** | **40/60 件** |
| **最頻 bridge の正体** | **`W4285719527`＝存在しないレコード（490万件の参考文献リストに残る id）** | **「Risk, Return, and Equilibrium: Empirical Tests」(Fama & MacBeth・被引用 15,196)＝主題ドメインの基礎文献** |
| bridge 寄与のあるシード | 実質2本の bridge に集中 | 20件中16件が寄与（11/13/13/12/10/8/5/4/4/3/2/2/1/1/1/1 本） |
| 上位候補の中身 | thematic analysis(190,298) / TAM(66,478) / Social Media / G\*Power ＝**分野横断の汎用方法論引用雲** | The Debt-Equity Choice / Estimating Standard Errors in Finance Panel Data Sets / Corporate Governance and Equity Prices ＝**実証ファイナンス** |

**健全なプールでは何も起きない（対照実験）**: 既存の固定テーマ `theme_seihai_strategy_generation.json` で同じ A/B を回すと、死んだ id は2本（`W6682701150` / `W2962687375`）見つかるが**どちらも通行量ゼロ**で、**出力は before/after で完全に一致した**（集中度 27%・通行 bridge 21本・上位候補も同一）。⇒ ゲートは phantom が居るときだけ噛み、居なければ無害。

**効かなかった／未達のこと（黙って消さない）**:

- **「遠さ」は改善していない。** after の上位はファイナンス・会計の実証論文＝**主題のホームドメインに近い**。phantom を外したことで 2-hop スキャンが**構造的に正しい動作**（Fama の基礎文献を共有する論文を返す）に戻っただけで、**それは「異分野の交差候補」ではない**。ホームドメイン除外（`dominant_field_ids`）がこのテーマで効いていない疑いがあり、**これが次の層**。
- **上位10件窓の集中は 90% → 70% で残る**（2026-08-22 に入れた `diversify_head_by_bridge` の枠は「同一 bridge 最大2件/10」なので、70% は枠が効いていないことを意味する）。**次の担当はここを見ること。**
- **materials 経路だけ実機（実 API）確認ができていない**。raw 経路・structured 経路は本日ゲート有効で実行・確認したが、materials を叩こうとした時点で **OpenAlex が持続的な HTTP 429 を返す状態**に入った（F-11 に追記）。materials は収集段を raw/structured と完全に共有し、分岐は `ranked_all` 以降の描画だけで、ユニットテスト（`test_materials_mode_returns_scoreable_json_with_bridge_signals`）はゲート込みで通っている。それでも**実 API で見ていないという事実は残す**。

**検証**: 新規 `tests/test_bridge_liveness.py` **9ケース**（実測された phantom の除外／被引用数 4,906,577〜59 の死んだ id 5本すべての除外＝大きさで判別していないことの固定／**id を短縮形で返すと全 bridge 統計がゼロになる回帰の固定**＝実装初回に実際に踏んだ／通信失敗の fail-open／全件 dead 応答の fail-open／空入力で API を叩かない／50本プールが1コール／120本が3コールに分割され切り捨てられない／重複 id）。全体 **382 tests: 382 pass**。実機は raw（before/after 各1回）・structured 各1回。

**ついでに直したテストの脆さ（本件とは別の既存不具合）**: `tests/test_mcp_history.py` の bybridge end-to-end 群は収集段はスタブしていたが、**id 解決系（`resolve_work_labels`）のネットワーク呼び出しは素通りしていた**。両者とも fail-soft なので今まで表面化しなかったが、**OpenAlex が 429 を返す状態では F-11 の取得診断行が結果本文に追記され、materials ケースが自分の JSON を parse できずに落ちる**。**未変更の HEAD でも同じく落ちることを `git stash` で実測**したうえで、autouse fixture で両ヘルパをスタブし、この群をオフラインで決定論的にした（実行時間 16.7s → 0.9s）。**次の担当が「382 pass」を基準線として信用できるようにするための処置。**

**seihai 側への申し送り（人間判断事項）**: **bybridge 呼び出し停止（S-26）の根拠だった主症状は、今回はじめて原因ごと除去された。** 7週間の観測すべてと整合する単一の機序が特定され、対照実験で「健全なプールには無害」も確認済み。**再開を検討してよい状態**。ただし上記のとおり「候補の遠さ」は未改善なので、**再開するなら「異分野性が戻ったか」を測る試験的な再開**が妥当（上位が実証ファイナンスのまま＝ホームドメイン近傍なら、それが次の起票になる）。**contra 側から seihai のコード・SKILL は触っていない。**

---


### F-02. bybridge — 使ったシードが出力に含まれない（S-11）— **対処済み 2026-08-18**

**何を変えたか**: `bybridge_collect` の出力に**実行診断ブロック**を追加した（新規 `src/pipeline/bridge_diagnostics.py`＋`src/mcp_server.py` 配線）。既定 ON。`diagnostics: false` で従来の件数1行に戻せる（加算的・可逆）。MCP のシグネチャは非破壊（引数追加のみ）。

出るようになったもの:

1. **使用シード全件** — タイトル / 年 / 掲載 / DOI（無ければ OpenAlex id）/ 被引用数 / **bridge 寄与本数**（そのシードの参考文献が bridge プールに何本入ったか）
2. **交差候補が通った bridge 上位5本** — bridge の**タイトルと被引用数**（OpenAlex 1コールでまとめ解決・失敗時は id のまま。診断の失敗が本体を落とさない）/ その bridge を引用した交差候補数・シード数
3. **bridge 集中度** — 最頻 bridge が交差候補全体・**上位N件**の何%を占めるか / 候補あたり平均共有 bridge 本数 / 共有1本のみの候補数 / 実際に使われた bridge 本数

3 は F-01 が7週間**手作業で数えていた「上位10件すべて共有 bridge 1本」を数値化したもの**＝処方の before/after を機械的に比較できる。交差候補ゼロの経路でもシードは出す（「シードが主題外」と「ホームドメイン除外が効きすぎ」は、シードを見ないと区別できないため）。

**before / after 実測**（同一テーマ・`no_history=true`・raw 経路。テーマは `data/samples/theme_seihai_strategy_generation.json` として固定化した＝次回以降も同じ入力で測れる）:

| | before（＝`diagnostics:false`） | after |
|---|---|---|
| 診断出力 | `収集診断: シード 20 件 / bridge プール 50 本 / 交差候補 60 件` の**1行のみ** | 同じ1行＋**シード20件の全リスト**＋**通行 bridge 5本（名前と被引用数つき）**＋集中度3指標 |
| 上位10件 | 全件「共有bridge: 1本」＝F-01 を8回目として再現（冠動脈疾患・エストロゲン補充・悪性黒色腫…） | 同じ（**ランキングは一切変えていない**。これは計器であって処方ではない） |
| 失敗の切り分け | **不可能** | **即断**: 最頻 bridge=「Can lifestyle changes reverse coronary heart disease?」(被引用 2,128)、上位10件の 100%・全候補の 50% がここ由来。シード側は `bridge 寄与` が **20件中18件で 0 本** |

**この計器が初日に出した成果**: 上の `bridge 寄与` 列だけで **F-01 の診断（巨大ハブ吸着 ⇒ 中心性ペナルティ）が誤りだと確定した**。真因は新規起票の **F-07**（重複 proceedings 2件がプール50枠を占領）。**処方を先に入れていたら8週目も「効かなかった」で終わっていた**——`docs/field_observations_seihai.md` の運用方針「計器が先、処方が後」が、初回でそのまま回収された形。

**検証**: 新規 `tests/test_bridge_diagnostics.py` 11ケース（純関数の集中度・bridge 通行量・シード射影・ラベル解決の1コール束ね／例外時フェイルソフト／描画の切り詰めと空入力）。全経路を実機で実行——raw / structured（キー無し4部組み立て）/ 交差候補ゼロ。全体 **302 tests: 301 pass**（唯一の失敗 `tests/test_git_collect.py::test_lma_floor_never_lowers_a_fresh_score` は**本変更前の HEAD でも失敗する既存不具合**。`git stash` で確認済み・別件）。

---

---

### F-07. bybridge — 重複した名簿型 proceedings 2件が bridge プール50枠を占領する — **対処済み 2026-08-21**

**再現（9週目）**: 処置前に同一テーマ（`data/samples/theme_seihai_strategy_generation.json`）で実行し、**F-07 を寸分違わず再現**した。シード20件のうち `PRIORITY AREAS OF RESEARCH IN THE SCIENTIFIC ACTIVITY OF TEACHERS`(DOI `10.46299/isg.p.2024.1.8`) と `Proceedings of the VIII International Scientific and Practical Conference`(DOI `10.46299/isg.2024.1.8`) の**2件だけが bridge 寄与 50 本**、残る18件は**全件 0 本**。**既知の様式の N 回目の再現もデータとして記録する。**

**何を変えたか**（`src/pipeline/collect.py` の `_bridge_pool_from_seeds` のみ。呼び出し側・MCP シグネチャ・スコア・順位付けのロジックには一切触れていない）:

1. **重複シードレコードの折り畳み**（新規 `_seed_group_ids`）。「2シード以上が引用する ref ＝強い bridge」という前提は**シードが別々の著作であるときだけ**成立する。折り畳み条件は (a) 正規化 DOI 一致、(b) 正規化タイトル一致、(c) **参考文献集合の Jaccard ≥ 0.9（双方 10 本以上）**。(c) が本命——DOI の `.p` 挿入のような書誌側の癖に依存せず、「同じ文献表を持つ2レコードは同じ著作」という事実だけを使う。同日の実行では **`Arborist.jl` の2レコード（Open MIND / Zenodo）も (b) で folding された**。
2. **1シード群あたりの pool 占有上限 `cap // 4`（=12/50）を、共有 ref 階層(1)にも適用**。従来の上限はラウンドロビン段(2)にしか無く、(1) が cap を単独で埋め切ると**多様性保証が一度も走らない**——これが7週間の失敗の実体だった。
3. **上限は「天井」ではなく「公平規則」**。他に出せるシードが居なければ最後の backfill が従来どおりプールを満たす（`test_single_seed_still_fills_the_pool` / `test_backfill_keeps_pool_full_when_others_are_ref_poor`）。単一シード入力・参考文献の乏しい入力での挙動は不変＝**加算的・可逆**。

**before / after 実測**（同一テーマ・`--raw --seed-count 20 --bridge-count 5`・同一セッション内で `git stash` により処置前後を交互実行。指標は 2026-08-18 に入れた F-02 の計器がそのまま出したもの）:

| 指標 | before | after |
|---|---|---|
| **最頻 bridge が上位10件に占める割合** | **100%** | **0%** |
| 最頻 bridge が全候補に占める割合 | 50% | 52%（**ほぼ不変＝残件**） |
| 実際に通行のあった bridge | 6 本 | **13 本** |
| 共有 bridge 1本のみの候補 | 60/60 件 | 56/60 件 |
| 候補あたり平均共有 bridge | 1.00 本 | 1.07 本 |
| **bridge 寄与のあるシード数** | **2/20**（各 50 本＝プール全枠） | **8/20**（9/9/9/9/8/8/6/1 本） |
| 主題に最も近いシード `AlphaAgent` の bridge 寄与 | **0 本** | **9 本** |
| 重複 proceedings 群の占有 | **50/50 枠（100%）** | **9/50 枠（18%）** |
| 最頻 bridge の正体 | 「Can lifestyle changes reverse coronary heart disease?」(被引用 2,128)＝名簿の参考文献 | **「Returns to Buying Winners and Selling Losers」(Jegadeesh & Titman・被引用 11,577)＝モメンタム／市場効率＝主題ドメインの文献**。2位「Efficient Capital Markets: II」(Fama・5,219) |

**読み方**: 処置は **bridge プールの中身を「名簿の参考文献リスト」から「主題ドメインの基礎文献」に置き換えた**。7週間 F-01 として記録されてきた「上位が共有 bridge 1本に潰れる」は**上位窓では消えた**。一方、**全候補レベルの集中（52%）と共起の薄さ（56/60 が共有1本）は残る**——これは重複レコードとは独立の性質で、F-01 の残余として上に追記した。

**効かなかった／確認できなかったこと（黙って消さない）**: 交差候補の**主題適合性そのものは、この処置では改善が確認できていない**。after の上位10件は依然として被引用数の大きい他分野論文（微生物叢のレアファクション・agriGO・遺伝子発現の代理変数解析 等）で占められる。**プールは主題化したが、候補側の順位は被引用数優位のまま**＝bybridge の順位付けには F-03（byrepo の「関連度が順位に効かない」）と同型の問題が残る可能性がある。**これは今回の処置の失敗ではなく、処置によって初めて切り分けられるようになった次の層**である。

**検証**: `tests/test_collect_citation.py` に6ケース追加（F-07 の形そのもの＝重複2件×150refs＋18シードの再現／重複が共有階層を作らないこと／共有階層にも上限が効くこと／単一シードでプールが満たされること／参考文献の乏しい相手での backfill／**わずかに ref を共有するだけの別論文は折り畳まれない**こと）。全体 **308 tests: 307 pass**（唯一の失敗 `tests/test_git_collect.py::test_lma_floor_never_lowers_a_fresh_score` は 2026-08-18 と同じ既存不具合・本変更と無関係）。実機で raw 経路を before/after 各1回実行。

**seihai 側への申し送り（人間判断事項）**: bybridge は S-26 で呼び出し停止中。**停止理由だった F-07/F-01 の主症状（上位が共有 bridge 1本・分野外の名簿由来ハブ）は解消**したので、**呼び出し再開の判断材料は揃った**。ただし上表のとおり「候補の主題適合性」は未改善なので、**再開するなら「上位窓の多様性が戻ったかの再観測」を目的にした試験的な再開**が妥当。contra 側から seihai のコード・SKILL は触っていない。


---

---

### F-03. byrepo — 関連度がランキングに効かない — **対処済み 2026-08-21（同日2回目・ユーザー立ち会い）**

**何を変えたか**:

1. **関連度を乗算項として順位に導入**（`src/pipeline/track_a.py` 新設 `annotate_anchor_rank`/`anchor_rank_key`）: `順位スコア = Reliability × (0.35 + 0.65 × relevance)`。relevance は各ソースが**既に計算していた** `theme_fit_score` の正規化（GitHub は max30・HF/Kaggle は max20）。**GitHub の theme_fit は "backwards compatibility metric" として計算だけされ順位に入っていなかった**——F-03 の「ラベルは出るが順位に効かない」の実体そのもの。床 0.35 は実観測バグ2件（8/17: 84vs79、8/20: 86vs83）が弱い関連（fit10/30）でも逆転する値で較正し、テストに固定した
2. **README 照合の切り詰めバグ**: `_theme_fit_score` が README 先頭2000字しか見ていなかった。実測: **confseq（テーマど真ん中の標準実装）の "sequential test" は5737字目に初出＝fit 0 に潰れていた**
3. **README 全文照合の逆バグを実測で発見→密度正規化**: 全文照合に変えたところ **frankensqlite（Rust製SQLite・180KB README）が fit30・relevance 1.0** に——巨大 README は何にでも当たる。実データ較正（本物の言及=confseq 1回/7.6KB・POPPER 2回/8.8KB、偶発=frankensqlite 6回/180KB・deer-flow 1回/117KB）に基づき、README ヒットは **10,000字あたり出現数で部分点**、name/description/topics ヒットは全点とした
4. **順位スコアと relevance を出力に明示**（F-02 と同じ「計器も一緒に」）＋ **keywords 5件上限を MCP スキーマに明記**（8/20・8/21 に2回踏まれた `InputValidationError` の説明欠落を解消）

**before / after 実測**（8/21 観測と同型のテーマ「逐次検定・anytime-valid・SPRT の実装」・github・pool10・実 API）:

| | before | after |
|---|---|---|
| 首位 | **bytedance/deer-flow（★80.4k・エージェントハーネス＝無関係）**＝8/21 観測の再現 | deer-flow は **relevance 0.03 で7位**に降格 |
| 真に関連する候補 | POPPER 7位 / confseq 最下位（fit 0＝切り詰めバグ） | **POPPER（逐次 e-value 検証・rel43）が quality88 の deer-flow を逆転**。confseq は fit 10 に回復 |
| 偽 relevance | —（naive 全文照合の中間版では frankensqlite が 1.0） | frankensqlite 0.1 / deer-flow 0.03＝**巨大 README の偶発ヒットが数値上も「弱い」と出る** |

**残る正直な限界（この処置では直っていない）**:
- **プール自体の質**: 検索クエリは include 先頭1語しか使わない（`build_track_a_git_query`）ため、`confseq`/`splink` 級の本命がそもそもプールに入りにくい。順位付けは直ったが**土俵に上がる段が未処置**
- **弱関連クラスタ内は quality 支配のまま**: confseq は rel48 が足を引き relevance 0.33 では首位に届かない。ただし呼び手が **"confidence sequence" をキーワードに入れれば description 直撃＝fit20** で首位になる構造（キーワード選択が効くようになった）
- **HF/Kaggle のカード文は密度正規化未適用**（GitHub の README でのみ実測較正した）
- **8/18 の「サブスコア全ゼロ」の正体を特定**: Kaggle の reliability は adoption+activity+license+theme_fit 構成で **Impl/Doc・LMA・Comm・Sec の柱を最初から持たない**。バグではなく描画ラベルの不一致（GitHub 用の柱名で 0 表示）。修正は未実施＝残件

**検証**: `tests/test_anchor_rank.py` 10ケース（実観測バグ2件の逆転を固定・密度較正4ケースは実測値を模写）。全体 **341 tests: 341 pass**。

**追処置 2026-08-22（ユーザー指示「発見されたバグの対策をウェブで調べて対処」）— 前日の残件4件を処置**:

1. **プール自体の質（真の律速だった）**: GitHub 検索の公式仕様を確認——リポジトリ検索は **OR 演算子対応（AND/OR/NOT 合計5個・256字まで）**、`in:name,description,readme` 修飾子あり、**既定ソートは best-match（関連度）で、現行コードは `sort=stars` を明示指定して人気順に上書きしていた**。`build_track_a_git_query` を全 include キーワードの OR 連結＋`in:` 修飾＋best-match（`sort=stars` 撤去・`GitCollectConfig.sort` で復元可）に書き換え。NOT は残り演算子予算内でのみ付与。`pushed:>` は相対日付化（ハードコード 2025-01-01 の経年ドリフト解消）。**実測 before/after（同一テーマ・実 API）**: 旧クエリ首位 deer-flow(★80k・無関係) → **新クエリ首位 `jakorostami/expectation`（"confidence sequences, sequential testing, e-processes, e-values の Python ライブラリ"＝テーマの理想解・旧クエリではプール不在）**。confseq 4位（"confidence sequence" が description 直撃で fit 20）・OpenBench(SPRT 分散検定) 5位。deer-flow/claude-skills/frankensqlite は**プールから消滅**。MCP 経由の全経路でも首位を確認
2. **HF カードの切り詰め**: `hf_collect` にも card 先頭2000字切り詰めが存在（GitHub と同型）。密度正規化（強フィールド=id/tags/pipeline/library は全点・card は10,000字あたり出現数の部分点）を適用。切り詰めは表示用途のみに残置
3. **Kaggle 柱ラベル不一致（8/18 の「全ゼロ」の正体）**: ソース別の柱内訳ヘルパ `reliability_breakdown` を新設——Kaggle/HF は Adoption/Activity/License/Theme-fit、GitHub は Impl/Doc/LMA/Comm/Sec で描画。構造化レンダラに Kaggle 分岐追加（従来は汎用 else 落ち）・LLM 経路の無条件 GitHub 柱印字を撤去
4. **順位スコアの可視化**: 構造化レンダラにも `順位スコア = Reliability × 関連度係数` 行を追加

**残る正直な限界**: best-match でもチェスエンジン系（SPRT はチェス検定の用語でもある）等のノイズは混入する（今回の実測では WolvenKit が fit 10 で3位）。ただし fit=0 の完全無関係群は下位に沈み、上から読んで最初に理想解に当たる状態にはなった。全 **349 tests: 349 pass**。

---

### F-09. delegate_finalize — 沈黙劣化と落選理由の不可視 — **対処済み 2026-08-21（同日2回目）**

**何を変えたか**: (1) echo 欠落警告 — 推奨欄（title/abstract/year/venue/cited_by_count）が欠けた候補は `⚠ 候補 W…: 材料欄が欠けたまま送信されています（…）` を出力に明示（エラーにはしない＝既存呼び手を壊さない。cited_by_count=0 は値であり欠落扱いしない）。(2) **落選1件ごとの (id, タイトル, 当たった床, 実測値, 閾値)** を「落選内訳」として出力（anomaly / hollow / percentile_gate / output_floor / not_selected の全段。`_record_rejections` を各段の境界に配線）。

**実機検証**（合成テーマ・4候補）: 出力に `落選内訳: W1003「…」: anomaly(purpose_sim) — 実測 0.1 / 閾値 0.2`、`W1004: hollow — 実測 0.3 / 閾値 0.5`、`W1002: percentile_gate — 実測 0.36 / 閾値 0.56` と echo 警告1行がすべて出た。8/19 に「呼び手が最も強いと判断した3件がなぜ落ちたか再現できない」だった状態は、**次回から出力だけで較正できる**。

---

### F-10. byserendipity — 判定器の申告が数値に反映されない — **対処済み 2026-08-21（同日2回目）**

**何を変えたか**: `_apply_causal_cap`（`src/pipeline/classify.py`）— `has_causal_pm=False`（＝ラベルが「構造対応ゆるめ」）の候補は `purpose_sim` を **partial(0.45) に上限**し、セレンディピティ積も同率で再計算。LLM 経路（judge マージ直後）と delegate post-gate の両方に配線。未判定は fail-open（従来どおり触らない）。上限前の値は `purpose_sim_uncapped` に保存。

**実機検証**: 上記 F-09 の実機実行で同時確認——観測された形そのもの（0.80×0.70=0.56・ゆるめラベル）が **0.80×0.45=0.36 に降格し、tight な候補（0.56）と同点でなくなった**。F-10 の併記観測「当たりが全部 0.56 で順位付け不能」も、ゆるめ候補が 0.36 に分離することで部分的に解消。距離軸 0.80 張り付きの検証（処方(2)）は未実施＝残件。



---

---

### F-12. bybridge — シード取得段の全滅（referenced_works 空レコードの枠占有） — **対処済み 2026-08-22（同日・ユーザー立ち会い）**

**何を変えたか**（seihai 側の処方「採用の前に生存確認」をそのまま採用）:

1. **シード生存確認ゲート**（`src/mcp_server.py` `_execute_bybridge`）: シード候補を3倍量オーバーフェッチし、**`referenced_works` が空のレコードをシードに採らない**（bridge を構造上1本も生成できないため）。除外件数は診断ブロックに「シード生存確認 (F-12)」行として明示。全候補が死にレコードの場合も「見つからず」ではなく **F-12 の形を名指しして返す**（機関リポジトリ群に当たっている可能性まで書く）。
2. **byrepo 低関連度警告**（併記観測の示唆「自己申告した関連度が低いとき、順位ではなく警告で返す」を実装）: 上位アンカーの theme 関連度が**全件 0.35 未満**のとき、出力先頭に語彙衝突（"admission" vs Kubernetes admission controller 型）の可能性を警告する1行を追加。structured / LLM 両経路。

**実測（固定テーマ `theme_seihai_strategy_generation.json`・raw 経路）**:

| 指標 | 処置前（8/21） | 処置後（8/22） |
|---|---|---|
| シード生存確認 | 無し（死にレコードが枠を占有し得る） | **24 件除外・シード20件全件が実参考文献持ち** |
| 最頻 bridge が全候補に占める割合（F-01 残余） | 52% | **27%** |
| 通行 bridge | 13 本 | **21 本** |
| 最頻 bridge の正体 | Jegadeesh & Titman（主題ドメイン） | 「Mathematical Statistics and Data Analysis」(3,097)＝ドメイン妥当な統計教科書 |
| 上位10件窓の最頻 bridge 占有 | 0% | **100%（正直に記録）** — 順位付け層（bridge_rank_key）が最頻 bridge 経由の候補を上位に集める作用。プール・候補全体の多様化と、上位窓の表示順は**別の層**であることが判明。次の残件 |

**seihai の観測run 2/3・3/3 への申し送り**: 今回の処置で「シード20件全滅→bridge 0本」の形は構造的に再発しない。ただし観測run 1/3 のもう一つの層——**日本語テーマの検索が日本語機関リポジトリの無関係レコード群に当たる**（シード検索の言語/関連度の問題）——は未処置で、生存確認は「bridge を出せないシード」を弾くだけで「主題外のシード」は弾かない。run 2/3 では診断ブロックのシード一覧で**主題適合を直接確認**されたい。

**検証**: `tests/test_mcp_history.py` に2ケース追加（死にレコードの除外と件数報告／全滅時の F-12 名指し）。全体 **351 tests: 351 pass**。実機1回。


---

---

### A1. 接地契約（quote-then-claim）— **委譲経路に実装 2026-08-22（承認済み総合対応）**

**形式（ユーザー裁定）**: LLM 役は**呼び手エージェントが代替**し、contra は**決定論検証だけ**を持つ。既存の委譲アーキテクチャ（raw_only 収集→エージェント採点→post-gate）の自然な拡張。

**契約**: エージェントが relationship / serendipity_rationale を書く場合、その主張が対応づける**テーマ側の逐語抜粋 `theme_quote`** と**候補側（title/abstract）の逐語抜粋 `source_quote`**（各10字以上）を必須添付。contra がコードで照合（全空白除去＋casefold の逐語一致）し、**照合失敗の散文は棄却して構造整形で代替・スコアは保持・失敗理由を出力に名指し**（F-09 と同じ可観測性）。`grounded_only:false` で旧挙動（可逆）。

**実機検証**: 接地済み1件＋F-04 の捏造形（「需要の変動が在庫管理…」）1件を混ぜて delegate_finalize を実行——接地済みは散文がそのまま描画、捏造は `theme_quote が提出テーマ本文に存在しない` と名指しされ散文が消えた。**F-04 の失敗様式は委譲経路では構造的に描画不能になった**。同じ実行で Phase B の purpose_pct 同点解消（0.56 同点の2件が 85>60 で正順）も確認。

**残**: contra 自身の LLM 経路（gpt-4o-mini 生成）は未保護＝キー確保後に同じ契約をプロンプト＋出力検証として移植する（A2/A3 と同時が効率的）。**seihai への申し送り**: byserendipity を `raw_only`＋自前採点＋delegate_finalize の委譲フローで呼べば、接地契約の保護を**今日から**受けられる。

**検証**: `tests/test_grounding_contract.py` 9ケース＋実機1回。全 **370 tests: 370 pass**。


---

---

### 案X・束2 — 委譲経路の本番昇格と A2/A3/C(iii) — **実装・通し稽古済み 2026-08-22**

**ユーザー裁定**: LLM キーのローテーション（案Y）ではなく**委譲経路の本番昇格（案X）**。呼び手エージェント=LLM 役、contra=収集＋決定論検証。seihai 日次 SKILL は委譲フロー（byrepo=structured / byserendipity=raw_only＋A2 facets / bybridge=materials / bynote=エージェント代替＋A3）へ書き換え済み（seihai `3d867db`）。

**束2 実装**: (1) **C(iii) シード言語ゲート**——Work.language を取得し bybridge シードは既定 'en' 以外を除外（コード欠落は fail-open・`seed_language:null` で無効・除外数を診断に明示）。(2) **A2 距離プロトコル**——raw_only facets 指示に Near/Far/VeryFar の3距離段階を義務化。(3) **bybridge `materials:true`**——構造的関連度順＋上位窓多様化済みの候補を bridge_signals 付き materials JSON で返す（byserendipity raw の対称形）。(4) **A3**——seihai SKILL 側プロトコル: bynote 類推は各件に byserendipity raw 検索で実文献1本以上を接地、引けない類推は出さない。

**通し稽古（本番形式・固定テーマ・実 API）**: エージェント（Claude）が A2 の3距離 facet を自作→raw 収集 60件（**途中の 504 を F-11 リトライが吸収し、失敗 facet は fail-soft スキップ＝実戦初動作**）→3件を採点（2桁小数・purpose_pct・接地引用つき、うち1件は故意の捏造コントロール）→delegate_finalize。結果: **採用1件はニッチ分割×生成器多様性の本物の構造対応（スコア 0.58＝格子値 0.56 の固着も消滅）・落選2件は実測値つき名指し・捏造は「theme_quote が提出テーマ本文に存在しない」で捕捉**。A1/A2/B/F-09/F-11 が1回の本番形式実行で全て動作。

**残（束3・人間判断）**: polite pool／Kaggle 資格情報／bysearch 復活。**各改修の結果（seihai の実運用 run）を見て判断**（ユーザー裁定）。全 **373 tests: 373 pass**。

---

## 2026-09-12（土・seihai 週次指針レーン）

### F-28. 全 by\* 共通 — **OpenAlex のレート制限を by\* が共有しており、先に回したツールが後続のツールを枯らす**（2026-09-12 初観測） → **診断の名指しのみ対処済み（2026-09-12）。下記「対処済み」節 F-28-R へ。資源競合そのものは未対処**

**症状**: 2026-09-12 の seihai 週次指針レーンで、`byserendipity_discover`（`raw_only` / facet 3枚）が正常完了（3 facet × 返却 50/49/48 → 候補 60）した直後から、**`bybridge_collect` が3回連続で `OpenAlexError: request failed after 3 attempts: HTTP Error 429`** で失敗した。`seed_count` を 20→12→10、`bridge_count` を 6→5 に落としても同じ。

**切り分け（重要）**: **contra を介さず、seihai 側から `api.openalex.org/works?search=...&per-page=2` を直接1回叩いても 429 が返った。** ⇒ **contra の実装・リトライ設計の問題ではなく、OpenAlex 側がこの IP に対して掛けているレート制限である。**

**含意（呼び手側の運用規約）**:

1. **by\* は1つの外部クォータを共有している。** 同一セッションで byserendipity を先に回すと、そのセッションの bybridge は枯れうる。byserendipity の `raw_only` は facet ごとに `search.semantic` をページ送りするので、**1回の呼び出しで最も多くクォータを消費する**。
2. ⇒ **規約案: bybridge を先に回す。** bybridge は 2-hop で `cites:` クエリを bridge ごとに投げるため（F-23/24-R 以降）、遅いが1本あたりの件数は小さい。順序を逆にすれば両方が完走する可能性が高い。
3. **「収穫ゼロ」ではなく「未実行」として記録する。** F-16（HTTP 400）・F-18（504）・S-112（0 件の facet を「その距離は不毛」と読まない）と同じ族で、**上流の一過性障害を候補側の性質として読まない**という既存規約の、**セッション内の資源競合バージョン**である。

**contra 側で検討の余地があるもの（seihai は被験体側のコードを直さない）**: (a) 429 を他のエラーと区別して「クォータ枯渇・時間をおいて再実行」と明示的に名指しする、(b) `mailto` 付きの polite pool を使っているかの確認、(c) by\* 横断のトークンバケットを1つ持ち、byserendipity の facet 数に応じて後続へ残量を申告する。

---

### F-21. 再現（2026-09-12・**3例目**）— `delegate_finalize` の `assumptions`

`delegate_finalize` の MCP スキーマでは `assumptions` は **required に入っていない**（`required: ["theme_overview","goal","why_problem","candidates"]`）。しかし `assumptions` を省いて呼ぶと **`InputValidationError: assumptions must be 2-5 items`** で拒否される。**同じ引数を省いた `byserendipity_discover` は通る**ので、**同一サーバ内で同じ引数の必須性が食い違っている**。

**F-21 の一般形（道具の自己記述が検証器と食い違う）の3例目**で、1例目（`approach_type: "system-building"`・9/05）と違い、2例目以降は**スキーマの `required` 配列そのもの**が実体と食い違っている＝**説明文でなく機械可読部分の乖離**。呼び手のコストは1往復（再送）で済んだが、**材料 JSON が 12KB あるため再送が高い**。

★**正直な記録**: 当方は本日これを「2例目」として書いた。**実際には 2026-09-11（前日）の seihai r05 レーンが、この同じファイルの日次表に「2例目」として既に記録していた**（`859c571`）。⇒ **今日の週次指針が指針10 として書いた「既に調べたことを思い出す仕組みが無い」に、その指針を書いた当人が同じ日に掛かった**。**追記先のファイルの直近行を読まずに追記した**のが直接原因で、**処方は F-28 や F-21 の側ではなく呼び手の手順にある**（seihai 台帳 S-117）。

---

### F-22. 再現（2026-09-12・seihai 週次指針）— 6件提出→5件が `percentile_gate` で落選

| 候補 | 実測 | 閾値 | 当方の評価 |
|---|---|---|---|
| Futility stopping in clinical trials (2012) | 0.419 | 0.611 | **今週の中心的な収穫**（欠けていた片側＝決着しない側の発火規則の正典） |
| Quantifying the bias ... early stopping for futility (Stat Med 2017) | 0.445 | 0.611 | **自分の処方への反証装置**（完走側の推定も歪む・解析式付き） |
| Informing the selection of futility stopping thresholds (2008) | 0.382 | 0.611 | 閾値較正の実例 |
| Optimal Giving-Up Times and the MVT (1982) | 0.546 | 0.611 | 一律の諦め時間は最適でない |
| Foraging as an evidence accumulation process (2019) | 0.564 | 0.611 | 採餌と逐次検定の合流点 |
| **Overharvesting in human patch foraging (PNAS 2023)** | **0.61** | 0.611 | **通過**（距離 0.86 × 構造 0.71） |

**落選5件のうち3件が「今週いちばん実務的」と当方が判断したもの。** `距離 × 構造` である以上これは仕様どおりの挙動で、F-22 の既存の記述（近い分野の当たりは構造的に通れない）に**7〜12点目の実測**を足すもの。**当方は落選のまま採用する。以後この様式の記録は1行に留める**（毎回同じことが起きるため、再現の情報量が飽和した）。

---

### F-17-R の「残る限界」の再現（2026-09-12・byrepo）— 関連度 0.0 が上位に載り、主題直撃が「低」になる

テーマ＝futility 打ち切り／conditional power／群逐次境界の実装アンカー。返った6件:

| 順位 | repo | 関連度 | 順位スコア | 実体 |
|---|---|---|---|---|
| 1 | `keaven/gsDesign` | **0.33「低」** | 43.6 | **群逐次設計の正典**（主題直撃） |
| 2 | `Merck/gsDesign2` | 0.33「低」 | 43.1 | 同上 |
| 3 | `jasp-stats/jaspPower` | **0.67「高」** | 42.3 | 検出力計算 GUI（star 1・Impl/Doc 9/30） |
| 4 | `samugit83/redamon` | **0.0** | 35.0 | **AI レッドチーム攻撃フレームワーク**（完全に無関係・Reliability 100） |
| 5 | `rpact-com/rpact` | 0.33「低」 | 32.9 | **確認的適応デザイン**（主題直撃） |
| 6 | `microsoft/agent-framework` | **0.0** | 30.1 | エージェント基盤（無関係・star 13,480） |

**関連度 0.0 の2件が、主題直撃の `rpact` を上回った。** F-17-R の修正で `関係度` ラベルは係数を正しく映すようになったが、**「係数が語の一致であって問いへの適合ではない」という残る限界**がそのまま観測されている。**加えて、関連度 0.0 を順位から排除しない**ため、Reliability の高い無関係リポジトリが枠を2つ消費する。**呼び手側の運用**: 関連度 0.0 の行は読まずに飛ばす（順位を信用しない）。

---

## 追記のしかた

seihai 側の日次・週次ルーティンは、当日の失敗を上記の該当する失敗様式（F-01 等）の表に1行足す。**新しい様式なら新しい F-xx を起こす**。既知の様式の N 回目の再現は、**それ自体が価値のあるデータ**（「シードを変えても再現した」が M-16 の前提を否定したように）なので、省略せず記録する。

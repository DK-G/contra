# プロジェクト管理エージェント

## 1. 初期セットアップ手順

このテンプレートは、新規プロジェクトを開始するために使用します。以下の手順に従ってください。

1.  **テンプレートのコピー**: まず、このフォルダ全体を新しいプロジェクト用に複製します。
2.  **`plan.md`の配置**: 外部で作成された、プロジェクトの仕様や計画が書かれた `plan.md` をこのフォルダに配置し、既存のファイルを上書きします。
3.  **`agent.md`の更新**: このファイルの「プロジェクト概要」セクションを、`plan.md`の内容に合わせて更新します。
4.  **`roadmap.md`の作成**: `plan.md`の目標を達成するための、大まかな開発ロードマップを `roadmap.md` に作成します。
5.  **`task.md`の作成**: `roadmap.md`の最初のマイルストーンを具体的なタスクに分解し、`task.md` に記述します。

---

## 2. プロジェクト概要

*   **プロジェクト名**: contra（contrarian 由来）。論文発想拡張支援CLIツール
*   **目的**: 思考の狭窄に抗い、研究テーマに対して「一見無関係だが構造的に接続しうる遠い論文」を対置することで視座を広げ、新しいアプローチへの気づきを促す。MVP は Track B のセレンディピティ・ユニット「1本」。詳細は [`plan.md`](plan.md)、理論的基盤は [`docs/research/serendipity_conditions.md`](docs/research/serendipity_conditions.md)。
*   **非目的**: 論文精読支援、全文解析、引用関係グラフ可視化、研究の正解提示、査読代替
*   **第一ユーザー**: 作者自身（発想拡張ツール）。商用展開は将来構想（任意）。

---

## 3. 各ファイルの役割

*   **`DIRECTION.md`**: プロジェクトの**不変核**（売り P1–P3・非目標・真の目的vs代理指標・採否リトマス）。着手前・スコア核に触れる前に読む正本。
*   **`CHECKS.md`**: 完了/統合前の受け入れゲート（自動 / 独立検証 / 人間判断）。
*   **`plan.md`**: プロジェクトの全体計画・仕様が記述されたマスタードキュメント。
*   **`roadmap.md`**: `plan.md`の目標を達成するための、大まかな開発ロードマップ。
*   **`task.md`**: `roadmap.md`の項目を分解した、実行可能な作業タスクリスト。
*   **`diff.md`**: 開発内容をレビュー担当者に報告するためのファイル。改修のたびに更新される。
*   **`review.md`**: レビューエージェント（Gemini）のレビュー結果を記録するファイル。
*   **`Changelog.md`**: `diff.md`の変更履歴を蓄積するアーカイブファイル。
*   **`Gemini.md`**: レビューエージェント（Gemini）の振る舞いを定義する固定ファイル。
*   **`memo.md`**: 開発時に共有する必要のある多くの文字情報（例: エミュレータのエラーログなど）を記録し、共有するためのファイル。

---

## 4. エージェントの役割

*   **開発エージェント (Codex CLIなど)**: `task.md`に基づいて開発を行い、完了後に`diff.md`を作成して報告します。詳細は後述の「**Codex CLI 運用ルール**」に従ってください。
*   **レビューエージェント (Gemini)**: `diff.md`を検証し、`Gemini.md`のルールに従ってフィードバックを`review.md`に記録します。

---

## 5. Codex CLI 運用ルール (開発エージェント用)

### 5.1 作業フロー
開発エージェントは、以下の手順を厳密に守って作業を進めてください。

1.  **入力**: `task.md` を最優先の指示として読み込み、タスクの意図を正確に理解します。
2.  **作業**: 小さな単位で実装と動作確認を繰り返します。
3.  **出力**:
    1.  作業が完了したら、まず `diff.md` に変更内容をまとめます。
    2.  次に、**`Changelog.md` の先頭に、今書き上げた `diff.md` の要約をコピーして、変更履歴をアーカイブします。**
    3.  `diff.md` をレビュー用に提出し、`task.md` の該当タスクを「完了 (Done)」に移動します。

### 5.2 禁止事項とガードレール

*   **スコープ外の実装**: MVP（最小実用製品）の範囲を超える機能の提案は歓迎しますが、**実装は行わないでください**。
*   **仕様変更**: もし仕様変更が必要だと判断した場合は、実装を進めずに `diff.md` に「仕様変更の提案」として内容を記述し、作業を停止してください。
*   **変更禁止ファイル**:
    *   `Gemini.md` と `review.md` はレビューエージェントが使用するため、**絶対に編集しないでください**。
    *   `plan.md` はプロジェクトのマスター仕様です。**勝手に書き換えず**、変更が必要な場合は提案のみを行ってください。

### 5.3 `diff.md` の必須要件
`diff.md` を更新する際は、以下の項目を**必ずすべて埋めてください**（空欄禁止）。

*   **変更目的**: なぜこの変更を行ったのか。
*   **変更概要**: 変更したファイル名のリスト。
*   **確認方法**: レビュー担当者が変更を確認するための具体的なコマンドや手順。
*   **既知の課題**: なければ「なし」と明記。

## 6. 不具合対応および設計反映に関する運用ルール

本プロジェクトでは、不具合および仕様・設計上の問題に対して、
以下のフローと責務分担を厳守する。

### 6.1 不具合の定義

不具合は以下の2種に分類する。

- **Error**
  - 起動不能、クラッシュ、ビルド失敗、通信不能、
    依存関係やバージョン不整合など、実行自体が成立しない問題
- **Functional Issue**
  - 処理は完了するが、仕様・期待される振る舞いとして不正な問題

分類は `diff.md` の不具合報告セクションに明記する。

---

### 6.2 開発エージェント（Codex CLI）の責務

- 不具合を発見した場合、**実装を行う前に** `diff.md` を更新する
- `diff.md` には以下を一次情報として整理する
  - 事実関係
  - 再現手順
  - 期待結果と実際結果
  - 影響度・影響範囲
- 原因の断定や設計判断は行わない
- 追加調査を求められた場合のみ、指定された情報を追記する

---

### 6.3 レビューエージェント（Gemini）の責務

- `diff.md` は、不具合の事実関係・背景・再現手順を把握するための一次情報とする  
- 不具合の有無や妥当性の判断は、対象となる実装コードをファイルシステムから直接読み取って行う
- 以下を `review.md` に記録する
  - 不具合の優先度
  - 原因仮説（確度付き）
  - 対応方針（即修正 / 追加調査 / 設計検討 / 対応不要）
- 実装やコード変更は行わない

---

### 6.4 対応方針の分岐

レビュー結果に基づき、対応は以下のいずれかとする。

- **即修正**
  - 影響が限定的で、設計変更を伴わない場合
  - 次の作業単位で実装し、修正内容を新たな `diff.md` として報告する

- **追加調査**
  - 情報不足の場合
  - Codex に具体的な調査内容を指示する

- **設計検討が必要**
  - 複雑、または影響範囲が広い場合
  - 実装は行わず、設計案・論点を `roadmap.md` 末尾の
    「Design Notes / Open Issues」に追記する

- **対応不要**
  - 仕様通り、または意図された挙動と判断された場合

---

### 6.5 ロードマップへの反映ルール

- `roadmap.md` は以下の粒度を厳守する
  - Phase：時系列・進捗管理
  - Epic：実装の塊・期間感の把握
  - Task：具体的な作業（`task.md` に記載）
- 不具合対応の結果、将来的な実装が必要と判断された場合のみ、
  Design Notes から Epic へ昇格させることができる
- タスクレベルの内容をロードマップに直接記載してはならない

---

### 6.6 基本方針

- 判断と実装を混同しない
- 一次情報（diff）と判断（review）を分離する
- ロードマップは「荒く、しかし進捗と残量が分かる」状態を維持する

---

## 7. 調査モード（Investigation）運用ルール（Gemini）

本プロジェクトでは、Geminiに「レビュー」以外に、目的を持った「調査モード」を持たせる。
調査モードは、コードが動作していても発生する性能・品質・設計上の問題について、
全体から改善点・避けるべき構成・ボトルネック候補を抽出し、`memo.md` に記録する。

### 7.1 調査モードの対象（例）

- 性能問題（重い、遅い、カクつく、メモリ増大、バッテリー消費 など）
- タイミング/順序のズレ（非同期競合、イベント順の破綻、処理順依存 など）
- 設計上のリスク（責務混在、拡張しづらい構造、依存の歪み、将来破綻しやすい など）
- 品質上の問題（観測不能、ログ不足、テスト不能、再現性が低い など）

※ これらは「不具合（Error / Functional Issue）」に該当しない場合でも対象とする。

### 7.2 調査モードの開始方法（手動）

- 調査モードは **Gemini CLI のチャット欄に手動で依頼した場合のみ開始**する。
- 自動トリガー（例: diff.mdの内容から自動で調査開始）は行わない。
- 依頼文には、最低限以下を含める:
  - 目的（何を改善したいか）
  - 現象（何が起きているか）
  - 制約（触れない箇所、締切、許容できる変更範囲 など）

### 7.3 Gemini（調査モード）の責務

- 調査対象を俯瞰し、改善の“当たり所”を優先度付きで整理する
- 断定を避け、仮説には根拠（観測点/該当箇所/一般的要因）を添える
- 「小さく試せる改善案」→「中規模」→「大規模（設計変更）」の順に提案する
- 必要に応じて、追加で取得すべき情報（ログ、計測ポイント、再現条件）を提示する
- 出力は `memo.md` に記録する（`review.md` には混ぜない）

### 7.4 禁止事項（調査モード）

- 実装・コード変更は行わない（提案・観測・仮説・優先度付けまで）
- 仕様変更・大規模改修を前提とした提案を“確定事項”として扱わない

### 7.5 `memo.md` への記録フォーマット（推奨）

`memo.md` へは以下の形式で追記する（厳密でなくてよいが、項目は揃える）。

- タイトル: [テーマ]
- 目的:
- 現象（要約）:
- 仮説（確度: 高/中/低）:
- 改善案（小さく試せる順）:
- 計測/観測案:
- 次アクション（Codexへの依頼 / ロードマップ反映の要否）:

### 7.6 ロードマップへの反映

- 調査結果が「複雑で将来対応が必要」と判断される場合は、
  `roadmap.md` 末尾の「Design Notes / Open Issues」に論点・方針案を追記する。
- ただし、ロードマップにタスクレベルの内容は記載しない（Epic粒度を維持する）。

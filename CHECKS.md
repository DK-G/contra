# CHECKS — contra 受け入れゲート

> 完了/統合の前に通す関門。★各項目は検証可能タスク。★全文常駐でなく「完了時に開く」。
> contra は既に `quality_eval.md`（A–E ルーブリック）・`tests/`・probe・数値捏造ガードを持つ。本書はそれを3層に束ねる。

## A. 自動ゲート（速い・機械）

- [ ] `python -m pytest tests/` が緑
- [ ] **数値捏造ガード**のテストが緑（HYPOTHESIS が Abstract 外の数値を出さない）
- [ ] **秘密スキャン**: API キー / メールアドレスが `output/`・git に漏れていない（コード直書き禁止＝spec §4）
- [ ] 外部ライブラリ追加なし（stdlib のみ）／`src/core/models.py` のデータクラス構造を変えていない
- [ ] （ギャップ）Linter 未導入 → 任意で `ruff` 等の軽量導入を検討（「テスト通過≠品質」対策）

## B. 独立検証パス（最高レバレッジ）

- [ ] スコア核・ゲート・本数挙動に触れたら、`quality_eval.md` を**別実行**で回し A–E と Done 条件 1–4 が退行なし
      （`--single --score-votes 3`、4テーマ＋自己テーマ、analogy-rich/poor 混在）
- [ ] hollow（カテゴリ一致のみ）/ myopia（近接）が**混入していない**ことを出力で確認

## C. 人間判断（自動化できない核）

- [ ] DIRECTION の P1/P2/P3 を強化している（薄めていない）
- [ ] 真の目的（質）を、代理（本数・purpose_sim 単独）より優先している
- [ ] 根本原因を直した（テーマ依存の応急ハードコードでない＝Step 8 教訓）
- [ ] gold-plating していない（指定外の機能・過剰一般化を足していない）
